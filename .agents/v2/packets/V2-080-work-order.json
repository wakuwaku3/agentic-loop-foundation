{
  "schema_version": "v1",
  "id": "wo-v2-080",
  "kind": "work-order",
  "created_at": "2026-08-26T17:05:00Z",
  "correlation_id": "v2-080-live-pooled",
  "problem_brief_ref": "pb-v2-080",
  "design_packet_ref": "dp-v2-080",
  "observable_outcome": "The representative Provider's live record is judgeable again at the commit that will be judged, paid for ONCE, and the working directory an Invocation declares is confirmed to reach the real child. Measured at judging commit e013b6d52330 against observation commit d9f98ae573e617aafd82d87fa1c35f9bd6e26ffb, the published 76-path exercise file set intersects the 288-path diff in EXACTLY 19 paths, so under gate rule G6b V2-075's record is stale and the M5 gate cannot be re-judged on it -- that staleness is one of the two grounds on which M5 failed as V2-026. This task re-runs TestProviderLiveVerticalSlice's nine subtests against the real claude CLI under the NEW approved record .agents/v2/provider-preflight/V2-080-provider-live-claude-pooled.json (max_invocations 48, its own empty ledger), adds a proof that the working directory V2-077 introduced actually becomes the real child's working directory, republishes the exercise file set as it stands now, creates AND PUSHES a durable ref for the new observation commit, and measures the intersection at the judging commit to be empty. The response to a threshold being reached is to stop and report, never to raise it.",
  "allowed_paths": [
    "internal/runner/provider_live_test.go",
    "docs/operations/provider-live-claude.md",
    ".agents/v2/evidence/V2-080-provider-live-claude-pooled.json",
    ".agents/v2/evidence/artifacts/V2-080-live-exercise-files.json",
    ".agents/v2/evidence/index.json"
  ],
  "prohibited_paths": [
    ".agents/v2/provider-preflight/**",
    ".agents/v2/task-state/**",
    ".agents/v2/packets/**",
    ".agents/v2/evidence/artifacts/V2-075-live-exercise-files.json",
    ".agents/v2/evidence/V2-017-provider-live-claude.json",
    ".agents/v2/evidence/V2-063-provider-live-claude-refresh.json",
    ".agents/v2/evidence/V2-075-provider-live-claude-rebind.json",
    ".agents/v2/evidence/V2-022-provider-live-claude-dogfood.json",
    ".agents/v2/evidence/V2-022-release-live-dogfood.json",
    ".agents/v2/evidence/V2-076-m3-gate.json",
    "ci/components.json",
    "ci/**",
    "Makefile",
    "go.mod",
    "go.sum",
    "devbox.json",
    "devbox.lock",
    ".gitleaks.toml",
    "firestore.indexes.json",
    "contracts/**",
    "internal/domain/**",
    "internal/application/**",
    "internal/api/**",
    "internal/quota/**",
    "internal/store/**",
    "internal/reconciler/**",
    "internal/scheduler/**",
    "internal/release/**",
    "internal/provider/**",
    "internal/web/**",
    "internal/update/**",
    "internal/legacyimport/**",
    "internal/contracts/**",
    "internal/ci/**",
    "internal/runner/confinement.go",
    "internal/runner/supervisor.go",
    "internal/runner/provider.go",
    "internal/runner/costledger.go",
    "internal/runner/secret_broker.go",
    "internal/runner/secret_guard.go",
    "internal/runner/orchestrator.go",
    "internal/runner/control_agent.go",
    "internal/runner/protocol.go",
    "internal/runner/journal.go",
    "internal/runner/lease.go",
    "internal/runner/workspace.go",
    "internal/runner/forge.go",
    "internal/runner/log.go",
    "internal/runner/memory.go",
    "cmd/**",
    "infra/**",
    "scripts/**",
    ".github/**",
    "docs/architecture/**",
    "docs/policies/**",
    "docs/preview/**",
    "docs/product/**",
    "docs/stable/**",
    "docs/operations/v2-task-dag.md",
    "docs/principles.md",
    "docs/v2-owner-console.md",
    "README.md",
    "AGENTS.md",
    "/home/takushi/.local/state/agentic-loop/v2/**"
  ],
  "contracts_changed": [],
  "acceptance_criteria": [
    "A1. SCOPE BOUNDARY, written into the evidence record BEFORE any source file is edited and BEFORE any provider invocation is made, as a five-row table, with the ordering itself recorded. V2-087 produced perfect content for this table but built it afterwards and had to report it as its one unmet item; V2-088 did it in the right order and its first commit is the records. Do it the V2-088 way. (1) What this task makes TRUE: the representative Provider's live record is judgeable at the commit that will be judged, under a new approved record with its own empty ledger, and the working directory an Invocation declares is confirmed by a real child. (2) What it deliberately does NOT do: it widens the exercise by no subtest, amends no earlier record by one byte, reuses no earlier ledger, raises no threshold, and changes no production source file -- internal/runner/confinement.go, supervisor.go, provider.go, costledger.go and secret_broker.go are all prohibited. (3) What it RECORDS but does not fix. (4) What only a LATER task can establish: that codex and opencode behave the same way, which is V2-028's three-Provider exercise and needs an authentication this machine does not have -- measured 2026-08-26, `codex login status` returns \"Not logged in\" and `opencode auth list` returns \"0 credentials\"; and anything about a real Cloud Run revision, which is V2-014/V2-054's GCP grade. (5) That this task passes no release gate on its own: it SUPPLIES V2-081, which judges M5.",
    "A2. Baseline, measured by you, and every number in this Work Order is expected to be stale. The last sixteen tasks each found at least one disagreement and V2-087 found five. Re-measure with `devbox run --pure -- sh -c 'go test -list \\'.*\\' ./internal/runner ./internal/provider ./internal/contracts'` and fresh greps, use YOUR measured value, and report every disagreement. Values read by the coordinator for you to contradict: the published set is 76 paths; the diff from d9f98ae573e617aafd82d87fa1c35f9bd6e26ffb to e013b6d52330 is 288 paths; the intersection is 19 paths; TestProviderLiveVerticalSlice is at internal/runner/provider_live_test.go:257 with nine subtests and the live gate at :49; the four existing ledgers stand at 11/16, 23/24, 11/12 and 12/12.",
    "A3. Re-measure the staleness yourself before spending one invocation, and record the argv and the verbatim intersection. Use the reproduction argv published in .agents/v2/evidence/artifacts/V2-075-live-exercise-files.json under `reproduction`. If the intersection is EMPTY at the current HEAD, the record is already fresh and this task must NOT spend the ledger: stop and escalate, because an observation nobody needs is unrecoverable spend. Record the measured value either way.",
    "A4. The approved record is read, never written. Read .agents/v2/provider-preflight/V2-080-provider-live-claude-pooled.json and record its digest, its limits (max_invocations 48, max_total_cost_usd 40.00, worst_case_reservation_usd 1.00), its declared base_names (HOME, PATH) and its empty granted_names, and confirm `devbox run --pure -- sh -c 'go test -count=1 -run TestProviderPreflightLedger ./internal/contracts'` is green. .agents/v2/provider-preflight/** is PROHIBITED: you may not edit this record, and you may not create another one.",
    "A5. Execution environment identifiers, recorded because none of them survive a clone. Record `hostname`, `uname -sr`, `readlink -f /home/takushi/.local/bin/claude`, `/home/takushi/.local/bin/claude --version` and `go version` as resolved inside devbox run --pure. State explicitly in the record that the ledger file lives OUTSIDE the repository at the path the approved record declares, that it is not in git, and that another machine must issue its OWN preflight record rather than copy this one.",
    "A6. The live gate is opened deliberately and the zero-invocation probe runs first. `devbox run --pure` strips environment variables, so AGENTIC_LOOP_LIVE_PROVIDER=1, HOME and PATH must each be passed with `-e` and never as a shell prefix; PATH must include /home/takushi/.local/bin so the child can find claude. Run the gate probe subtest first and record that it consumed ZERO invocations, proving the gate and the harness work before any spend.",
    "A7. The exercise, exactly nine subtests and not one more. Run TestProviderLiveVerticalSlice: I1_happy_journey, I2_wire_and_identity, I3_credential_isolation, I4_lease_continuity, I5_I6_crash_resume, I7_real_transport_failure, I8_real_cancel, caseB_reuses_I1_journal_zero_invocations, ledger_snapshot. Record the verdict line for each and the argv actually used. Do NOT widen the exercise: a wider exercise costs more AND enlarges the published file set, which enlarges the surface later work can invalidate.",
    "A8. The working-directory proof, and it must come from the real child. V2-077 changed the mechanism: ProcessSupervisor's cmd.Dir would chdir before `unshare` runs, so internal/runner/confinement.go emits a `cd` INSIDE the wrap script after the mounts (confinement.go around :186, :199 and :217 -- re-measure). No live child has ever confirmed it. Add an assertion to internal/runner/provider_live_test.go that the working directory the Invocation declares IS the real claude process's working directory, reading a value the CHILD produced and not one the harness knows. internal/runner/confinement.go is PROHIBITED: prove the shipped mechanism, do not change it. If the value cannot be obtained from the child, record the attempt and mark it UNPROVEN -- never assert it from the harness.",
    "A9. Transient provider failures counted and reported honestly. Every past observation consumed one to three invocations on transient failures. Record the count, the ledger sequence numbers consumed, and the verbatim failure text IF and ONLY IF it contains no provider response content. A transient failure is never reported as a subtest failure and never omitted.",
    "A10. Thresholds reported, never raised. Record the final ledger state verbatim from `cat` of the declared ledger path: the invocation count, the settled total, and the halted flag. If the ledger reaches max_invocations or trips the anomaly threshold, the exercise STOPS for inspection, the record is written with result `partial`, and you report it as NEITHER a success nor a failure. You may not raise a limit, re-initialise a ledger, clear halted, or edit any file under /home/takushi/.local/state/agentic-loop/v2/. Record also the measured fact that the four earlier ledgers' settled actual_usd sum to 0.00 USD each, because the flat-rate subscription settles every invocation at zero, so the count is the only threshold that has ever bound.",
    "A11. Republish the exercise file set as it stands NOW, at .agents/v2/evidence/artifacts/V2-080-live-exercise-files.json. Recompute it with the `reproduction.recompute_the_compiled_set` argv published in V2-075's artifact, record the path count and the diff against V2-075's 76 paths, and preserve the artifact's shape -- in particular each subtest name stays wrapped in an OBJECT rather than a bare string, because the repository's secret scan flags the adjacency of a high-entropy value to certain keywords. V2-075's artifact is PROHIBITED and must stay byte-unchanged.",
    "A12. Create AND PUSH a durable ref for the new observation commit: `refs/live-observations/v2-080-provider-live-claude`. This is an acceptance item, not a courtesy, and the reason was paid for today: V2-075's observation commit d9f98ae is NOT reachable from v2, its ref had never been pushed, and six files reference it -- a clone on another machine would have lost the commit and made G6b's diff permanently unmeasurable for the record M3's gate rests on. Record `git ls-remote origin 'refs/live-observations/*'` showing BOTH refs present. This is the ONLY push you may make: do not push a branch.",
    "A13. Measure the intersection at the judging commit and record it as EMPTY, with the argv. An unmeasured intersection FAILS G6b -- an argument that nothing important changed is not a measurement. Also run at least one POSITIVE CONTROL showing the same procedure reports a NON-empty intersection against a deliberately constructed diff list, so a green result is known not to be vacuous.",
    "A14. The completion convention, and it is the trap that failed the M3 gate. After the file set is measured and published, touch NOTHING inside it -- not a comment, not a line of whitespace. V2-018 failed because one doc-comment line in internal/runner/provider_live_test.go was edited after V2-063 measured the runner key, which moved the key and made the live record stale by its own declaration. internal/runner/provider_live_test.go is INSIDE the set, so all edits to it must land BEFORE the measurement, and the measurement must be the last source-affecting act.",
    "A15. No prompt and no provider response is recorded in ANY form, including truncated or hashed excerpts. Provider-issued execution identifiers (session_id) MAY be recorded. No credential value appears in any file, record or commit message. Confirm with a grep over your record and artifact for any marker or instruction text, and with `devbox run --pure -- sh -c 'gitleaks dir .agents --config .gitleaks.toml'` after the evidence commit -- never `gitleaks dir .`, which reports 26 findings out of the gitignored build/evidence/** subtree.",
    "A16. Determinism where determinism is possible, and honesty where it is not. A live provider run is not deterministic and no duration may be asserted as a threshold -- record every measured duration as an OBSERVATION. But no fixed sleep, no wall-clock timer and no goroutine may be added to any test, every clock stays injected, and every wait is a bounded deadline poll reusing an idiom already in internal/runner. Under gate rule G7 a skipped subtest is never a pass: if the live gate is closed the run measures nothing, and such a run is recorded with status `skipped`.",
    "A17. Evidence, exactly ONE record: .agents/v2/evidence/V2-080-provider-live-claude-pooled.json, id ev-v2-080-provider-live-claude-pooled, component runner, task_id V2-080, correlation_id v2-080-live-pooled. Every acceptance item traceable to a named check recording the argv actually executed, the working directory, the timeout and the measured result. Register it by an appended entry in .agents/v2/evidence/index.json, which is a SINGLE-LINE compact JSON file -- append only, touch no other task's entry, and create no second record. `devbox run --pure -- sh -c 'go test -count=1 ./internal/contracts'` must be green with the record in place.",
    "A18. Evidence keys, for the issued record only. Read the `runner` key with `devbox run --pure -- sh -c 'make evidence-keys'` on the final tree with every touched file already `git add`-ed, because the manifest checks read `git ls-files` and an unstaged file is invisible. Measure exactly ONE key. Under gate rule G6a key equality is provenance and NOT a pass/fail condition -- do not measure all 23, do not copy a key from an earlier record, and do not treat key movement as a verdict. The freshness basis is the INTERSECTION, not the key.",
    "A19. Chain position, recorded as facts for the coordinator. This task creates and removes NO component edge: internal/runner/provider_live_test.go is a test file on already-declared edges, and ci/components.json, Makefile, go.mod, go.sum and devbox.* are prohibited -- a red manifest, dependency-coverage or key-closure check therefore means the design was departed from, so STOP and escalate. Record that this task SUPPLIES V2-081, which re-judges M5, and that V2-084, V2-086 and the five measured walls (V2-089 to V2-093) will move files inside the published set and will make this record stale for LATER gates -- which is correct and expected, because a gate judges at a commit, exactly as M3 did through V2-076, and M6 requires all three Providers and gets its own observation on the machine that has them.",
    "A20. Verification, and what you do not run. Your set is `devbox run --pure -- sh -c 'go test -race -count=1 ./internal/runner ./internal/provider ./internal/contracts'`, `go vet ./...`, `gofmt -l internal/runner`, `make component-runner`, and `gitleaks dir .agents --config .gitleaks.toml` after the evidence commit. Do NOT run the full `make check`: the coordinator runs it once at integration. Judge every devbox run by its VERDICT LINE, never by the shell exit status, which is unreliable in this repository; note also that `devbox run --pure -- <bare-name>` is treated as a devbox script name, so wrap real commands as `devbox run --pure -- sh -c '...'`."
  ],
  "validation": {
    "argv": [
      "devbox",
      "run",
      "--pure",
      "--",
      "sh",
      "-c",
      "go test -race -count=1 ./internal/runner ./internal/provider ./internal/contracts"
    ],
    "working_directory": ".",
    "timeout_seconds": 3600
  },
  "constraints": [
    "Work only inside the assigned isolated task worktree. The ONLY push authorised is the durable ref refs/live-observations/v2-080-provider-live-claude; never push a branch.",
    "Never use git reset, git checkout --, git stash or git commit --amend.",
    "This task IS authorised to invoke the claude CLI, and only through TestProviderLiveVerticalSlice under the approved record .agents/v2/provider-preflight/V2-080-provider-live-claude-pooled.json. codex and opencode are NOT authorised, not even --version: measured 2026-08-26, neither is authenticated on this machine.",
    "Never raise a limit, never re-initialise a ledger, never clear the halted flag, and never edit any file under /home/takushi/.local/state/agentic-loop/v2/. Reaching a threshold is a fail-closed stop for inspection that is neither a success nor a failure; the only remedy is a new owner-issued record.",
    "Amend no earlier record. .agents/v2/provider-preflight/**, V2-017's, V2-063's and V2-075's evidence records, V2-075's exercise-file-set artifact and V2-076's M3 gate record must all stay byte-unchanged.",
    "Do not record a prompt or a raw provider response in any form, including truncated or hashed excerpts. Provider-issued execution identifiers (session_id) may be recorded. Never record a credential value in any file, record or commit message.",
    "The child process environment is exactly the approved record's declared base_names (HOME, PATH); granted_names stays empty; the CLI reads its own credential store. Do not add an environment variable and do not grant a name.",
    "Change no production source file. internal/runner/confinement.go, supervisor.go, provider.go, costledger.go, secret_broker.go and secret_guard.go are prohibited -- A8 proves the shipped mechanism, it does not modify it.",
    "Do not widen the exercise beyond the nine subtests plus the working-directory proof. A wider exercise costs more invocations and enlarges the published file set, which enlarges the surface later work can invalidate.",
    "After the exercise file set is measured and published, touch nothing inside it. This is the trap that failed the M3 gate as V2-018, where one doc comment moved a key.",
    "An unmeasured intersection fails G6b. An argument that nothing important changed is not a measurement, and a positive control is required so a green result is known not to be vacuous.",
    "Under gate rule G7 a skipped subtest is never a pass. If the live gate is closed the run measures nothing and is recorded with status skipped.",
    "Do not write a module-path string literal into any source or test file: internal/ci's manifest check reads such a literal as an undeclared edge and turns make check red. Build any needed path by concatenating a prefix with a suffix.",
    "Build every secret-shaped test fixture by concatenating a prefix with a suffix so the pattern exists at run time and not in the file, and preserve V2-075's artifact shape in which each subtest name is wrapped in an object -- the secret scan flags the adjacency of a high-entropy value to certain keywords.",
    "Do not edit .agents/v2/task-state/**, any .agents/v2/packets/** file, any other task's evidence record, or any index.json entry belonging to another task.",
    "Do not run the full devbox run --pure -- make check; the coordinator runs it once at integration. Judge every devbox run by its verdict line, never by the shell exit status. devbox run --pure strips environment variables, so AGENTIC_LOOP_LIVE_PROVIDER, HOME and PATH must each be passed with -e.",
    "Under gate rule G6a per-component evidence key equality is not a pass/fail condition. Measure exactly the one runner key; do not measure all 23 and do not treat key movement as a verdict. The freshness basis is the intersection.",
    "Never write a verification as passed that was not actually executed. If any acceptance item cannot be satisfied, stop and escalate to the tech_lead rather than fabricating, weakening or reinterpreting it.",
    "Every line number and every count in this Work Order was read at e013b6d52330 and is expected to be stale. Re-measure each one yourself, use the measured value, and report every disagreement in the evidence."
  ],
  "failure_stop": true,
  "tasks": [
    "V2-080"
  ],
  "priority": 80
}
