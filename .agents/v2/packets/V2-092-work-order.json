{
  "schema_version": "v1",
  "id": "wo-v2-092",
  "kind": "work-order",
  "created_at": "2026-08-27T02:30:00Z",
  "correlation_id": "v2-092-m5-gate",
  "problem_brief_ref": "pb-v2-092",
  "design_packet_ref": "dp-v2-092",
  "observable_outcome": "One evidence record decides M5 and it may decide against it. The judge writes .agents/v2/evidence/V2-092-m5-gate.json (id ev-v2-092-m5-gate, component m5-gate) plus one index entry, and edits nothing else: no source, no test, no task-state, no other task's record. G1 is judged against the six properties of section 12.8 and against no task list. P1 and P2 are SAFETY, each carried by a closure or an enumeration cited by file and line and re-executed, never by a single occurrence (section 8.1 erratum 2). P3 to P6 are LIVENESS, each carried by an eligible record check AND a non-test reachability site. G2 is one measurement: at least one index entry whose component is exactly release-live-dogfood and whose result is passed. Measured at cd48424: exactly one, ev-v2-022-release-live-dogfood, result partial. The 5 passed / 7 skipped / 0 failed split is re-derived by the judge from contracts/release-contract/foundation-capabilities.json with two independent predicates that must agree (4/3/5 at cd48424) and with section 8.3's reverse constraint measured both ways; V2-095's claim about it is not read as evidence. G6b is measured on M5's exercise only -- TestFoundationPreviewLocalDogfood in ./internal/api, compiled set 88 paths at cd48424, intersection against ev-v2-022's observation 34 -- recomputed, compared with the published set, with controls, and the commit order checked. Both records V2-095 issues are verified, and this record states plainly that release-live-dogfood is guarded by NO machine check because provider_preflight.go:160 selects only provider-live-, and that widening it is prohibited because it would fail ev-v2-022 retroactively. G6a is a green make check the judge runs itself. A FAIL is a complete deliverable: result failed, the ground named by measurement, no Stable promotion, release_eligible still false, V2-081 left unresolved and a successor to be named by the coordinator -- and NO task id proposed, because M5's remediation budget stands at 5 of 5.",
  "allowed_paths": [
    ".agents/v2/evidence/V2-092-m5-gate.json",
    ".agents/v2/evidence/index.json"
  ],
  "prohibited_paths": [
    "internal/**",
    "internal/api/**",
    "internal/application/**",
    "internal/domain/**",
    "internal/release/**",
    "internal/runner/**",
    "internal/reconciler/**",
    "internal/provider/**",
    "internal/scheduler/**",
    "internal/store/**",
    "internal/store/memory/**",
    "internal/store/firestore/**",
    "internal/contracts/**",
    "internal/contracts/provider_preflight.go",
    "internal/ci/**",
    "internal/quota/**",
    "internal/update/**",
    "internal/legacyimport/**",
    "internal/web/**",
    "internal/infra/**",
    "cmd/**",
    "cmd/control-plane/**",
    "cmd/runner/**",
    "cmd/bootstrap/**",
    "cmd/ci-plan/**",
    "cmd/legacy-import/**",
    "contracts/**",
    "contracts/schemas/**",
    "contracts/fixtures/**",
    "contracts/openapi/**",
    "contracts/release-contract/**",
    "contracts/release-contract/foundation.json",
    "contracts/release-contract/foundation-capabilities.json",
    "ci/**",
    "ci/components.json",
    "Makefile",
    "devbox.json",
    "devbox.lock",
    "go.mod",
    "go.sum",
    "Containerfile",
    "firebase.json",
    "firestore.indexes.json",
    ".gitleaks.toml",
    "scripts/**",
    ".github/**",
    "infra/**",
    ".codex/**",
    "docs/**",
    "docs/architecture/**",
    "docs/operations/**",
    "docs/operations/v2-task-dag.md",
    "docs/product/**",
    "docs/preview/**",
    "docs/stable/**",
    "README.md",
    "AGENTS.md",
    ".agents/v2/task-state/**",
    ".agents/v2/packets/**",
    ".agents/v2/DECLARATION-GAP.md",
    ".agents/v2/HANDOFF.md",
    ".agents/v2/DEFERRED.md",
    ".agents/v2/README.md",
    ".agents/v2/historical/**",
    ".agents/v2/provider-preflight/**",
    ".agents/v2/evidence/artifacts/**",
    ".agents/v2/evidence/V2-022-release-live-dogfood.json",
    ".agents/v2/evidence/V2-022-provider-live-claude-dogfood.json",
    ".agents/v2/evidence/V2-026-m5-gate.json",
    ".agents/v2/evidence/V2-081-m5-gate.json",
    ".agents/v2/evidence/V2-095-release-live-dogfood.json",
    ".agents/v2/evidence/V2-095-provider-live-claude-dogfood.json"
  ],
  "contracts_changed": [],
  "acceptance_criteria": [
    "A1. THE FIVE-ROW SCOPE-BOUNDARY TABLE GOES INTO THE EVIDENCE RECORD BEFORE ANY OTHER WORK, AND IT IS COMMIT 1. Five rows, each a list and not a sentence: (1) what this judgment makes true; (2) what it deliberately does not do; (3) what it measured, found broken and did NOT fix, with file and line; (4) which paths it may write, copied verbatim from allowed_paths; (5) which paths it may not, with the reason each prohibition exists. The requirement is the ORDER: V2-087 completed this table, put it in all four of its records and measured everything, and still reported the order as its single shortfall, because a table written afterwards can always be made consistent with whatever was touched and is therefore not evidence that a boundary was kept. REVISION IS PERMITTED under section 12.3: if the boundary must widen, record the PRE-REVISION table verbatim, the revision time and the revision reason in this same record, then widen in place. Widening in place with a recorded history is PREFERRED over proposing a new task. DO NOT PROPOSE A TASK ID: M5's remediation budget stands at 5 of 5 and a sixth task is a signal that the milestone is mis-cut, which is the coordinator's decision and not this gate's output.",
    "A2. RE-MEASURE THE BASELINE YOURSELF; EVERY NUMBER BELOW IS EXPECTED TO BE STALE. Each was measured by the designer at HEAD cd48424 with the command given. V2-086 reported nineteen disagreements against its packet, V2-084 fourteen, V2-085 twelve, and V2-095's designer found four of Sol's premises false plus a false row in the gap ledger. Report BOTH values for every disagreement and use the measured one. Run and record: (a) `git rev-parse HEAD` and `git status --short` (must print nothing) -- the judging commit; (b) the index: entry count 171, zero sha256 mismatches, file-set bijection true, one line, release_eligible false; (c) component release-live-dogfood has exactly ONE entry, ev-v2-022-release-live-dogfood, result partial, and component provider-live-claude-dogfood exactly one, also partial, and component m5-gate two, both failed; (d) `grep -n 'HasPrefix(component' internal/contracts/provider_preflight.go` -> exactly line 160, literal provider-live-; (e) `grep -rn 'release-live' --include='*.go' .` -> TWO lines at cd48424 (internal/api/live_dogfood_test.go:2115 and :2180, both string literals), and the same grep piped through `grep -v _test.go` -> 0; (f) the capability partition 4/3/5 of 12 by BOTH predicates of A9; (g) the nine-row P3 table of A6; (h) the compiled set of ./internal/api = 88 paths, and the intersection with the diff from 39d2e809b02527d1e8b56f44cbb4fc20473344ef = 34 over a 168-path diff; (i) `grep -c '^--- SKIP'` = 26 and `grep -cE '^ *--- SKIP'` = 49 for ./internal/store/firestore with no emulator, both = 2 for ./internal/api; (j) `devbox run --pure -- sh -c 'make evidence-keys'` -> 23 components, api = 730944a79feeccaa04d32aa24c574644ab6ac2c9b0f4e88aebc2a51aed637b01; (k) 94 task-state files, 72 complete, 15 blocked, 1 running, 6 failed (V2-007, V2-018, V2-023, V2-024, V2-026, V2-081); (l) `devbox --version`, `go version`, `git --version`, `gh --version`, the kernel from `uname -srm` and the machine identifier; (m) the four line bindings of A4 and A5. Report every disagreement in the record.",
    "A3. G1 IS THE PROPERTY SET AND NOTHING ELSE. Write six checks named p1 through p6, in that order. Each states: satisfied or not satisfied; SAFETY CLAIM or LIVENESS REQUIREMENT, re-derived from the wording of the property in section 12.8 rather than copied from its 種類 column (report any disagreement); and the ground. Write ONE separate check, named milestone-task-provenance, that records the state of M5's tasks as PROVENANCE and states in terms that no task's status is a condition of this verdict -- section 12.1 measured that the old G1 turned every discovery into unreachability (+48 complete against +48 new over two days, fan-out 1.0, 40 planned tasks now 96). DO NOT ADD A SEVENTH PROPERTY. A finding that binds to no property goes into row (3) of the A1 table.",
    "A4. P1 IS SAFETY AND THE REFUSAL SITE IS NAMED, RE-LOCATED AND RE-EXECUTED. P1 is 'a docs-only drift stops promotion'. Show, by reading the file, that internal/release/pipeline.go's Promote calls VerifySource BEFORE the gate, the store and the route -- measured at cd48424: VerifySource :39, PromotionGate :52, Store.Put :55, Router.Promote :58 -- so a refusal cannot leave a half-advanced route. Then re-locate BY NAME, and record the line each actually occupies, the two assertions section 12.8 binds: TestPipelinePromoteRefusesFabricatedMatchingDocsDigest (measured internal/release/pipeline_test.go:60 -- a fabricated MATCHING DocsDigest is still refused) and TestPipelinePromoteDocsDriftHermeticWithNegativeControl (measured :101 -- hermetic, with the mandatory negative control). Run both by name with `-race -count=1 -v` and transcribe the verdict lines with zero SKIP. A safety claim may NOT be passed on a single occurrence: state that the ground is the negative control plus the digest-matched refusal, not one observed rejection.",
    "A5. P2 IS SAFETY, ITS GROUND IS THE CLOSURE, AND THE LIVE RECORD'S SINGLE PROMOTION IS REFUSED AS GROUND. P2 is 'a candidate without full capability evidence does not become Stable'. Read internal/domain/release.go: measured at cd48424 the loop at 98-102 returns the wrapped ErrEvidenceIncomplete NAMING the missing capability at :100. Re-locate by name and count, yourself: internal/domain/release_test.go:122 TestPromotionRejectionsIsolateEachRefusalClass -- `sed -n '122,225p' ... | grep -c '^\\t\\t{Reject'` must print 12 and the distinct kinds must also number 12, which is section 8.1's '12 refusal classes'; :230 TestPromotionRejectionsAgreeWithCanPromoteOverTheCapabilityClosure (a 16384-cell grid); :372 TestPromotionRejectionsAgreeWithCanPromoteOverTheIdentityClosure. Run all three by name and transcribe. IT IS FORBIDDEN to carry P2 on the dogfood exercise's promotability-positive or promotability-negative subtests (measured internal/api/live_dogfood_test.go:379 and :378), on V2-022's A17 check, or on any single promotion or single refusal: section 8.1 erratum 2 records that its own first ground was replaced for exactly that reason, and that the closure enumerations are the stronger ground because make check re-executes them every commit under G6a.",
    "A6. P3 IS A NINE-ROW MECHANICAL TABLE PLUS THE PROMOTION PATH, AND IT MAY COME OUT EMPTY. P3 is 'a transition the domain models has a path to it other than a test' -- liveness, and section 12.1 requires it in every property set. Build the table yourself: for each of the nine RequirementCommandKind values declared at internal/domain/model.go:437-445 (start-framing, ready, start, wait, needs-input, recover, evaluate, pause, cancel), count the non-test lines that ISSUE it, meaning a composite literal with `Kind:`, not a mere mention. USE WORD-BOUNDARY ANCHORING: measured at cd48424, unanchored `grep -rn 'domain.RequirementStart'` returns 4 because it also matches RequirementStartFraming, and unanchored `domain.RequirementCancel` returns 1 which is internal/application/readmodels.go:310 comparing the STATUS RequirementCancelled; anchored with `\\b` they return 2 and 0. Measured issuer counts at cd48424: StartFraming 1 (internal/application/framing.go:191), ReadyCommand 2 (internal/application/human_input.go:648 and framing.go:374), Start 1 (internal/application/service.go:1390), NeedInput 1 (internal/application/human_input.go:519), and Wait, Recover, Evaluate, Pause, Cancel ZERO -- four of nine. Also measure the promotion path: `grep -rnE 'NewPipeline\\(|\\.Promote\\(' --include='*.go' . | grep -v _test.go` returned only the definition at internal/release/pipeline.go:29 and the internal Router.Promote call at :59 -- zero non-test callers of the pipeline -- and AttachReleaseObserver likewise had none. P3 is satisfied ONLY IF every one of the nine rows has at least one non-test issuer AND the promotion pipeline has at least one non-test caller. If any row is empty, P3 is NOT satisfied, name the empty rows, and do not repair them.",
    "A7. P4, P5 AND P6 EACH NEED TWO HALVES AND YOU SAY WHICH HALF FAILED. All three are liveness. Half (i), the record: the M5live record's check named cap-backlog-visibility carries status passed (P4), cap-human-input-request passed (P5), and cap-repository-registration AND cap-user-documentation passed (P6). Half (ii), the non-test site, measured by you and cited by file and line: for P4 the paging cursor repair at internal/store/firestore/store.go -- measured at cd48424 documentIDCursor is at 638-640 and the comment block at 630-637 records the defect (V2-095's work order says 637, which is the last comment line; report the disagreement); for P5 the needs-input issuer at internal/application/human_input.go:519 and its framing issuer at internal/application/framing.go:191; for P6 the registration and document paths the record's own checks name. If (i) holds and (ii) does not, the property is NOT satisfied and you say so: a live check carried by a test-only path is the defect P3 exists to name.",
    "A8. G2 IS ONE MEASUREMENT AND NO OTHER RECORD MAY STAND IN. Look up M5's mandatory live component in the section 5 table FIRST and quote the row -- it is release-live-dogfood -- and only then look up the index. Recompute every entry's sha256 against the file it names, assert the bijection between the index paths and the *.json files directly under .agents/v2/evidence/, assert the index is one line, and print every entry whose component is release-live-dogfood with its result. G2 is satisfied only if at least one such entry carries result passed. A record whose component is provider-live-claude (measured: ev-v2-063, ev-v2-075 and ev-v2-080 are passed) may NOT stand in for it -- that substitution is what section 8.2 records as the mistake that cost a gate, and G3 item 3 forbids the local-for-live move independently. State the same for m5-gate: the two existing entries are the failed predecessors and are not grounds.",
    "A9. RE-DERIVE THE 5/7/0 SPLIT FROM THE DECLARATION FILE, TWICE, AND MEASURE THE REVERSE CONSTRAINT BOTH WAYS. Do not read V2-095's claim about the split as evidence. Partition the 12 capabilities of contracts/release-contract/foundation-capabilities.json with TWO independent predicates and require them to agree path by id: PREDICATE A over external_dependencies -- 'Google Cloud Run' among systems, or both 'codex' and 'opencode' among providers; PREDICATE B over the promotion_prerequisites strings -- the text names Cloud Run, or names codex and opencode. Measured at cd48424 both give the identical partition: 4 deferred to D1 (cap-preview-operation, cap-stable-promotion, cap-loop-control, cap-loop-self-update), 3 deferred to M6 (cap-autonomous-resolution, cap-shared-resource-allocation, cap-provider-operation), 5 eligible (cap-repository-registration, cap-requirement-intake, cap-backlog-visibility, cap-human-input-request, cap-user-documentation), disjoint, summing to the declared count 12. Assert the REVERSE CONSTRAINT in the same pass, both directions: none of the 5 eligible names Cloud Run, codex or opencode in EITHER field, and none of the 7 deferred is one of the four capabilities section 8.3 measured as M5's own product gaps. G2 FAILS if the record's 7 skipped checks are not exactly the derived 7, if any of the 5 eligible carries a skipped check, or if any capability carries a failed check.",
    "A10. THE THREE M6 CAPABILITIES ALSO DECLARE claude, SO DERIVE THE DEFERRAL FROM CONDITION 3 AND USE THE MACHINE STATE ONLY AS CORROBORATION. Measured at cd48424, cap-autonomous-resolution, cap-shared-resource-allocation and cap-provider-operation each declare providers ['codex','claude','opencode'] -- and claude IS authenticated here, so the array alone leaves open the argument 'exercise it with claude and it is not deferred'. Quote each capability's promotion_prerequisites condition 3, which names 対象Provider（codex、claude、opencode）の実物へ接続して成功すること: the capability's OWN declared prerequisite requires connecting to codex and opencode for real, which is what makes the deferral derived rather than observed. Then re-measure, at judging time, that codex and opencode are unauthenticated on this machine and record it as CORROBORATION only. Section 8.3 forbids deriving a deferral from the observation that something could not be reached, so a derivation that rests on the authentication state would flip the moment somebody ran `codex login` -- and would be the wrong kind of fact.",
    "A11. G6b IS MEASURED ON M5's EXERCISE, RECOMPUTED, CONTROLLED, AND THE COMMIT ORDER IS PART OF IT. The exercise is TestFoundationPreviewLocalDogfood in package ./internal/api (measured internal/api/live_dogfood_test.go:286; gates AGENTIC_LOOP_LIVE_DOGFOOD=1 and AGENTIC_LOOP_LIVE_PROVIDER=1 plus FIRESTORE_EMULATOR_HOST; 17 subtests at lines 364-380, of which 12 are the capability ids). ./internal/runner's TestProviderLiveVerticalSlice under component provider-live-claude is M3's: state explicitly that it is NOT measured as freshness for this gate. Steps, all recorded with argv: (1) read the observation commit from the record and confirm it is reachable, including via the durable ref under refs/live-observations/; (2) RECOMPUTE the compiled set with `go list -deps -test` over ./internal/api taking GoFiles, CgoFiles and EmbedFiles of this module only, intersected with `git ls-files`, and compare it PATH BY PATH with the set the record publishes, reporting every difference -- measured 88 paths at cd48424, and expect about 90 after V2-095 because internal/api/ownerdocs.go and ownerdocs_test.go join package internal/api; do NOT compare against the number 88; (3) measure the intersection with `git diff --name-only <observation> <judging>` and transcribe the FULL diff path by path; (4) run a POSITIVE control over a fictional diff list, editing no tracked file, naming internal/api/live_dogfood_test.go, one internal/application source in the set and one internal/web embedded asset -- all three must print with a non-zero count; (5) run a NEGATIVE control over real diff paths outside the set -- must print zero; (6) for every intersecting path, state whether it landed BEFORE or AFTER the observation. If the intersection is non-empty, G6b FAILS regardless of the nature of the changes and you do not argue that any path was harmless. Measured context: against ev-v2-022's observation commit 39d2e809b02527d1e8b56f44cbb4fc20473344ef the intersection at cd48424 is 34 over a 168-path diff, and it has only grown (V2-026 measured 26, V2-081 measured 30); and 7 of V2-095's 22 allowed_paths are already inside the set (internal/api/api.go, internal/api/api_test.go, internal/api/live_dogfood_test.go, internal/application/readmodels.go, internal/application/allocation.go, internal/web/owner.html, internal/web/owner.js), so commit order is the binding constraint. Note in the record that the compiled group is a PACKAGE closure and is therefore identical to M2's TestControlPlanePreviewLocalLive in the same package; what distinguishes the exercises is the gate variables and the read-at-run-time group.",
    "A12. VERIFY BOTH OF V2-095's RECORDS AND SAY PLAINLY THAT ONE OF THEM IS UNGUARDED. Measured at cd48424: internal/contracts/provider_preflight.go:160 is the only HasPrefix(component, ...) site and its literal is 'provider-live-'; for the entries it selects, CheckProviderPreflightLedger enforces (1) an artifact_refs entry with artifact_id provider-preflight whose sha256 equals the preflight record's own bytes and (2) approval.approved_at strictly before observed_at. So ev-v2-095-provider-live-claude-dogfood (component provider-live-claude-dogfood) IS selected and mechanically enforced; ev-v2-095-release-live-dogfood (component release-live-dogfood) is NOT. Verify BOTH records exist, both are passed, both carry the provider-preflight binding, and recompute the sha256 of the preflight record's bytes yourself and compare it for BOTH. Then state, in the record, in plain words: the M5 ground record is outside the reach of the check meant to guard it; the reach is NOT widened; and the reason is measured -- ev-v2-022-release-live-dogfood carries exactly 4 artifact_refs (real-tree-candidate-bundle-digest, real-tree-candidate-contract-digest, real-tree-candidate-docs-digest, release-contract-foundation-json) and NONE is a provider-preflight ref, so widening the prefix would immediately fail that existing record and turn internal/contracts red, which is the retroactive rule application section 12.4 forbids. State that this is accepted, that section 9.6.1 records it, and that V2-095's second record is the compensating control. Do not edit provider_preflight.go and do not propose that anyone else does.",
    "A13. G6a: RUN THE FULL make check YOURSELF AND JUDGE IT BY THREE SIGNALS. `devbox run --pure -- sh -c 'make check'` at the judging commit on a clean tracked tree. make check emits NO 'FAILS 0' line, so the verdict is the conjunction of: zero lines matching ^FAIL; gitleaks printing 'no leaks found'; and 'workflow-pins: ... no disagreement', which is the last of check's prerequisites and therefore proves the target ran to completion. Transcribe the per-package `ok` lines including internal/store/firestore appearing TWICE -- the vacuous run from `go test ./...` and the emulator-gated component target -- so the green verdict provably includes a real, non-skipping firestore execution. PER-COMPONENT KEY EQUALITY IS PROVENANCE, NOT A VERDICT: do NOT re-derive the 23 component keys as a freshness claim and do NOT treat key movement as a finding; transcribe at most the api key as corroboration (measured 730944a79feeccaa04d32aa24c574644ab6ac2c9b0f4e88aebc2a51aed637b01 at cd48424, and it is expected to move when the evidence commit lands). If make check is red for a reason unrelated to M5, the gate FAILS on G6a and you name the failing package and target; V2-026 failed exactly this way.",
    "A14. G3, G5 AND THE GRADE. G3: state that the environment class of M5's live ground is preview-local under section 5 and G3 item 2 -- owner's real machine, real processes, real CLI, Firestore emulator -- that this IS the live evidence for those conditions and is not called a substitute, and that the record carries the environment class, the machine identifier, the emulator name and version, and the identifiers of the real external systems involved. Confirm no unit, fake, stub or contract test is used to satisfy a real-environment condition (G3 item 3). G5: assert that no evidence in the ground was produced by retrying the same candidate until it went green, and record how you checked -- the record must not contain a result overwritten after a differing earlier run.",
    "A15. G7 HAS TWO LAYERS AND EVERY COUNT CARRIES ITS PATTERN. Layer one, go test: record the live exercise's own SKIP count (expected zero) with the exact pattern used, and the repository-wide counts with BOTH patterns because both are correct and they differ -- measured at cd48424 for ./internal/store/firestore with no emulator, `grep -c '^--- SKIP'` = 26 (top level) and `grep -cE '^ *--- SKIP'` = 49 (with subtests), alongside 10 PASS and a vacuous 'ok ... 0.015s'; for ./internal/api with the live gates unset both patterns = 2, namely TestFoundationPreviewLocalDogfood and TestControlPlanePreviewLocalLive. Re-measure both and report any disagreement. Layer two, the record: count the checks whose status field is 'skipped' -- for the M5live record that is the seven ineligible-by-declaration capabilities, and those seven are a CLASSIFICATION OF SUBTESTS THAT RAN, since the exercise has one subtest per capability id. State the separation of the two layers explicitly and state that no skipped check anywhere is counted as a pass. Conflating the layers is exactly how a skip becomes a pass.",
    "A16. G4 AND THE FAILURE CHAIN, MEASURED AND NOT ASSERTED. Reproduce the machine definition of superseded from internal/contracts/contracts_test.go:279-296 (a task id referenced from the repair_of or input_refs of any COMPLETE task) over .agents/v2/task-state/*.json and print, for each failed task, whether it is superseded. Measured at cd48424: 94 files, 72 complete, 15 blocked, 1 running, 6 FAILED (V2-007, V2-018, V2-023, V2-024, V2-026, V2-081); V2-026 is referenced only from V2-028 (blocked) input_refs and V2-081 (failed) repair_of; V2-081 only from V2-092 (blocked) repair_of and V2-095 (blocked) input_refs; nothing DEPENDS on V2-026 or V2-081, so assertSupersededDependencies -- which fires only for queued or running dependents -- never fires for this chain and NO machine check enforces it either way. Record: V2-095 completing supersedes V2-081 literally, V2-092 completing supersedes V2-081 literally, and V2-026 becomes superseded only under the TRANSITIVE reading V2-092 -> V2-081 -> its repair_of names V2-026, which is exactly the reading section 6 itself uses for V2-023 through V2-025 -> V2-024 -> V2-023. State which reading you apply and apply it consistently. Record the chain V2-026 -> V2-081 -> V2-092 with what each predecessor failed on, re-read from the records: V2-026 has 13 checks of which FIVE carry status failed (g2, g6a, g6b, m5-completion-condition-mapping, gate-verdict) which is four substantive grounds plus the verdict -- report the disagreement with any account that says four checks -- and V2-081 has 18 checks with three failed and two independent grounds, G2 and G6b at 30 paths.",
    "A17. MAP THE ROADMAP'S FOUR COMPLETION CONDITIONS AND RECORD THE ONE THAT IS BOUND TO NO PROPERTY. From docs/architecture/roadmap.md M5: condition 3 (docsだけのdriftでもpromotionが止まる) is P1; condition 4 (全機能Evidenceを持つcandidateだけStableになる) is P2 and is SAFETY, per section 8.1; condition 1 (このFoundation RepositoryをPreview対象にしてdogfoodする) is LIVENESS per section 8.1 erratum 1 -- it has no 'only' and no negation, it asserts an event -- and is carried by the M5live record, failing for want of an ADMISSIBLE GROUND rather than for a low success count; condition 2 (Previewを意図的に壊し、旧StableでRequirementを再開する) is bound to NO property in section 12.8. Name condition 2's actual ground -- the dogfood exercise's channel-break-and-resume subtest, measured at internal/api/live_dogfood_test.go:376 -- report whether it passed, and state plainly that its outcome does NOT change the G1 verdict, because G1 v2 defines the gate by the declared property set and section 12.4 forbids applying a rule that did not exist at observation time. Do NOT invent a P7 and do NOT propose a task.",
    "A18. THE VERDICT CHECK, AND THE FAIL IS SPECIFIED AS COMPLETELY AS THE PASS. Write a final check named gate-verdict whose status equals the record's result. It must enumerate, separately, the conditions SATISFIED and the conditions NOT satisfied, each with the check that measured it, and it must state that no check was omitted because the verdict was already decided. IF THE GATE FAILS: result is 'failed'; the record names WHICH check carries the ground and the measurement that establishes it; it states that no Stable promotion is decided and that release_eligible remains false in both the record's index entry and the index header; it states that a failed V2-092 leaves V2-081 UNRESOLVED, that V2-026 stays unsettled under either reading, and that naming a successor is the coordinator's act; and it states that under section 12.10 M5's remediation budget stands at 5 of 5, so a sixth remediation task is NOT an available outcome and a failure for want of work is a signal that the milestone is mis-cut. NO TASK ID appears anywhere in the record, in any form, including as a suggestion or a reserved number. IF THE GATE PASSES: result is 'passed', and the record states that the canonical record of the pass is the complete transition's reason containing the literal string 'gate M5 passed' plus the list of evidence ids -- written by the coordinator, since .agents/v2/task-state/** is prohibited here -- and lists exactly the evidence ids the verdict rests on so the coordinator can copy them.",
    "A19. THE RECORD AND THE INDEX ENTRY, MECHANICALLY. Create exactly ONE record: .agents/v2/evidence/V2-092-m5-gate.json, id ev-v2-092-m5-gate, kind evidence, schema_version v1, component m5-gate, task_id V2-092, correlation_id v2-092-m5-gate, observed_at the judging instant. m5-gate appears in no component list of ci/components.json (measured: 23 components, m5-gate not among them), so declare in the first check that the evidence_key derivation for this record is the JUDGING COMMIT ITSELF, exactly as ev-v2-026-m5-gate and ev-v2-081-m5-gate did, and put that 40-character sha in evidence_key. Every check carries name, status, argv, working_directory and timeout_seconds; status is one of passed, failed, skipped and nothing else. Append exactly ONE entry to .agents/v2/evidence/index.json with id, task_id, component, result, release_eligible false, evidence_hash equal to the sha256 of the record's own final bytes, and path -- single-line compact JSON, append-only, no other entry touched, no reordering, and the index header's release_eligible stays false (the schema pins it to the constant false, so 'landing the milestone' cannot mean flipping it). After writing, re-verify that the recomputed sha256 equals the entry's evidence_hash.",
    "A20. COMMIT ORDER, SECRET SCAN AND THE POST-COMMIT RE-VERIFICATION. Commit 1: the record carrying the A1 boundary table and the A2 baseline and nothing else. Commit 2 (final): the completed record and the index entry. Before each commit, scan the subtree you are about to commit: `devbox run --pure -- sh -c 'gitleaks dir .agents --config .gitleaks.toml'`. NEVER `gitleaks dir .` -- it walks the gitignored build/evidence/** tree and reports 26 findings, after which the state of tracked files is unreadable; six tasks have been stopped by that trap, and every catch was a keyword adjacency and not a secret (a 64-character digest immediately followed by punctuation, a 40-character commit sha, and one subtest name next to another). Then, AFTER the final commit, run `devbox run --pure -- sh -c 'make check'` again and confirm it is still green on the committed state: `gitleaks git` reads committed history only, so a green check before the commit proves nothing about what you committed.",
    "A21. WHAT THIS JUDGE DOES NOT DO. It starts NO Provider CLI, makes no provider invocation, issues no live evidence record, and creates no preflight record. It does not read any cost ledger under /home/takushi/.local/state/agentic-loop/v2/ for spending and does not write one. It asserts no duration as a threshold and adds no test: any measured duration is recorded as an observation. It edits no source file, no test file, no task-state, no other task's packet or evidence, no contract, no schema, no doc. It runs the full make check because G6a requires it, and that is the only heavy command it owns.",
    "A22. REPORT EVERY DISAGREEMENT AND EVERY RESIDUAL FINDING AS A FACT. In the record, list every value where your measurement differs from this Work Order or from dp-v2-092, with BOTH numbers and the command. Record as facts, with no task id and no repair: (i) internal/contracts/provider_preflight.go:160 does not reach release-live- components, so the M5 ground record is unguarded by machine check; (ii) whether the DECLARATION-GAP.md row claiming domain.PriorityAssessment does not exist is still false -- re-run its own grep and record the result rather than editing the ledger, which is prohibited here; (iii) roadmap M5 condition 2 is bound to no property in section 12.8; (iv) any empty row of the A6 P3 table; (v) whether ev-v2-080-provider-live-claude-pooled is still G6b-fresh after V2-095's paths landed, noting that V2-018 is failed and no M3 gate is pending so nothing is invalidated today; (vi) that section 6's 'the existing three failed tasks' is now six, measured. Every one of these is a one-line edit away from being fixed, and fixing any of them would end the measurement."
  ],
  "validation": {
    "argv": [
      "devbox",
      "run",
      "--pure",
      "--",
      "sh",
      "-c",
      "make check"
    ],
    "working_directory": ".",
    "timeout_seconds": 5400
  },
  "constraints": [
    "Judge every `devbox run` invocation by its VERDICT LINE, never by the exit status: `devbox run` exit codes are unreliable in this repository. `devbox run --pure` strips environment variables, so anything the command needs must be passed with `-e`. `devbox run --pure -- <bare-name>` resolves <bare-name> against devbox.json's script list, and devbox.json defines only check, smoke, infra-policy, infra-lint and infra-validate -- so every real command is wrapped as `devbox run --pure -- sh -c '...'`.",
    "allowed_paths is a WHITELIST of exactly two files. A path mentioned anywhere in this Work Order or in dp-v2-092 but absent from allowed_paths is PROHIBITED. Writing any other file, including a scratch file inside the repository, a report, a doc or a fix, is a boundary violation regardless of how obviously correct the change would be.",
    "Do not propose a task id, anywhere, in any form. Section 12.10 puts M5's remediation budget at 5 of 5 (V2-084, V2-089, V2-090, V2-091, V2-095). A sixth remediation task is not an available outcome; if the gate fails for want of work, say that the milestone may be mis-cut and leave the cut to the coordinator.",
    "Do not add a dependency to this gate. Section 12.2 freezes gate dependencies at creation time; V2-092's dependencies are ['V2-095'] and stay that way. Measured cause: V2-018 grew from 1 to 6 dependencies and failed, V2-080 from 4 to 17 and only ran after being cut back to 9.",
    "Do not re-observe an existing live record because a rule changed. Section 12.4 applies rules at their version and forbids retroactive invalidation; measured cost of the churn is three re-observation tasks (V2-063, V2-075, V2-080) and V2-075's ledger exhausted at 12 of 12 while only 0.444129 of 8.00 USD was spent -- the resource is finite in COUNT, not in money.",
    "Never widen internal/contracts/provider_preflight.go:160, and never propose widening it. Record the gap instead. ev-v2-022-release-live-dogfood carries no provider-preflight artifact ref, so widening fails an existing record on account of a rule change.",
    "Forbidden git operations: `git reset`, `git checkout -- <path>`, `git stash`, `git commit --amend`. The prohibited SUBSTANCE is any operation that loses uncommitted work -- another agent is working concurrently in a different worktree of this repository. If history must be rebuilt, show that nothing is lost (V2-075 used `git update-ref` and reported it, which was accepted) and report it.",
    "Security: never record a prompt or a raw provider response in any form, including truncated, paraphrased or hashed excerpts. A provider-issued session_id may be recorded. Never record a credential value, and never record whether a specific credential file's contents look valid -- only that a CLI reports itself authenticated or not.",
    "Any digest written in prose is either 64 or 40 hexadecimal characters plus at most one adjacent punctuation character, and never two revisions joined by a two-dot range: that shape is what the gitleaks generic-api-key rule catches, and it has stopped six tasks.",
    "This judgment makes no state transition. .agents/v2/task-state/** is the coordinator's; the complete or failed transition, its reason string and any successor naming are the coordinator's acts, and the record supplies the material for them.",
    "Determinism: assert no duration as a threshold, add no test, introduce no sleep and no wall-clock dependency. Any measured duration is an observation recorded as such.",
    "Every line number, count and digest written into the record must be one the JUDGE measured at the judging commit, with the command recorded beside it. A number copied from this Work Order without re-measurement is a defect even when it turns out to be right."
  ],
  "failure_stop": true,
  "tasks": [
    "V2-092"
  ],
  "priority": 96
}
