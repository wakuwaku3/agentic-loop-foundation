{
 "schema_version": "v1",
 "id": "wo-v2-049",
 "kind": "work-order",
 "created_at": "2026-08-24T22:58:21Z",
 "correlation_id": "v2-049-failure-taxonomy",
 "problem_brief_ref": "pb-v2-049",
 "design_packet_ref": "dp-v2-049",
 "observable_outcome": "The domain failure taxonomy declares all seventeen documented classes, the M4 enumeration test witnesses the agreement instead of the drift, the provider package's separate taxonomy is still asserted to be distinct, and no transition behaviour changed.",
 "allowed_paths": [
  "internal/domain/release.go",
  "internal/reconciler/failure_matrix_test.go",
  ".agents/v2/evidence/V2-049-failure-taxonomy.json",
  ".agents/v2/evidence/index.json"
 ],
 "prohibited_paths": [
  "internal/domain/control.go",
  "internal/domain/lease.go",
  "internal/domain/model.go",
  "internal/domain/id.go",
  "internal/domain/core_test.go",
  "internal/domain/invariant_model_test.go",
  "internal/domain/source_guard_test.go",
  "internal/provider/**",
  "internal/application/**",
  "internal/store/**",
  "internal/api/**",
  "internal/release/**",
  "internal/runner/**",
  "internal/contracts/**",
  "contracts/**",
  "ci/**",
  "Makefile",
  "go.mod",
  "go.sum",
  "devbox.json",
  "devbox.lock",
  ".github/**",
  "infra/**",
  "scripts/**",
  ".gitleaks.toml",
  ".agents/v2/task-state/**",
  ".agents/v2/packets/**",
  "docs/**"
 ],
 "contracts_changed": [],
 "acceptance_criteria": [
  "A1. internal/domain/release.go declares exactly three additional FailureClass constants whose values are promotion-partial, secret-suspected and contract-incompatible, taken verbatim from the failure model document, bringing the total to seventeen. Nothing else in the file changes: no function, no transition, no struct field.",
  "A2. Every pre-existing test in internal/domain still passes under the same name, and the exhaustive closure tests from the M1 gate are untouched. Report the before and after top-level test counts.",
  "A3. The drift assertion in internal/reconciler/failure_matrix_test.go is updated to assert that the domain declares seventeen classes and that no documented class lacks a constant. The assertion must be written so that removing a constant fails it; prove that by temporarily removing one constant, recording the failure message, and restoring it. Do not commit the mutation.",
  "A4. The same test still asserts that internal/provider declares its own separate FailureClass type carrying values such as contract-incompatible and timeout, and that this is a different taxonomy rather than a duplicate of the domain one.",
  "A5. The bucket tables of the M4 enumeration keep their totals and their disjointness; the three classes stay in the deferred bucket with their existing owners, because adding a constant does not converge a scenario.",
  "A6. devbox run --pure -- go test -count=1 ./internal/domain ./internal/reconciler ./internal/application ./internal/runner ./internal/release ./internal/contracts is green, and devbox run --pure -- make check is green when run after committing.",
  "A7. A new evidence artifact .agents/v2/evidence/V2-049-failure-taxonomy.json (id ev-v2-049-failure-taxonomy, task_id V2-049, component domain, result passed) records the argv actually executed, including the temporary-removal proof of A3, and is registered with exactly one appended entry in .agents/v2/evidence/index.json whose evidence_hash equals the sha256 of the artifact bytes. The evidence_key is the domain component key read on the final committed tree where make check is green.",
  "A8. No file outside allowed_paths changes."
 ],
 "validation": {
  "argv": [
   "devbox",
   "run",
   "--pure",
   "--",
   "make",
   "check"
  ],
  "working_directory": ".",
  "timeout_seconds": 2400
 },
 "constraints": [
  "Work only inside the assigned isolated task worktree; never push.",
  "Never use git reset, git checkout --, git stash or git commit --amend.",
  "Do not merge the provider taxonomy into the domain one and do not touch internal/provider.",
  "Do not change any transition function, classifier or behaviour; this is a constant-block change plus a test expectation.",
  "Do not touch the task-state directory; the coordinator records transitions.",
  "Read the component evidence key only after make check is green on the final committed tree, and remember that a file not yet added to the index is invisible to that key."
 ],
 "failure_stop": true,
 "tasks": [
  "V2-049"
 ],
 "priority": 55
}
