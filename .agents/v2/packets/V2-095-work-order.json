{
  "schema_version": "v1",
  "id": "wo-v2-095",
  "kind": "work-order",
  "created_at": "2026-08-26T22:30:00Z",
  "correlation_id": "v2-095-m5-gap-closure-and-redogfood",
  "problem_brief_ref": "pb-v2-095",
  "design_packet_ref": "dp-v2-095",
  "observable_outcome": "M5's mandatory live ground becomes eligible and fresh. Measured at a602b98: of the four gaps M5 owns, TWO are already closed in the shipped tree and the dogfood exercise already carries their closing probes, verdict-free. cap-human-input-request: framing.go:191 is the only non-test issuer of domain.RequirementStartFraming, and live_dogfood_test.go:1070-1212 walks captured->framing->needs-input->ready over the real base URL asserting every clause of the declared success condition. cap-backlog-visibility's ground E22-11: closed at store.go:637, and the probe pages the Backlog to exhaustion over the route's own cursor. Both probes log that they issue no verdict and name the M5 re-dogfood as its owner. This task: (1) adds the confirmation items cap-backlog-visibility's own failure condition requires and no surface carries -- per-row Increment status and next_action through the two EXISTING batch ports, the Requirement's own StableSnapshot, and the per-candidate rank, reason and score inputs allocationReport already computes at allocation.go:625 and discards -- plus the repository_id filter (E22-7) through the EXISTING RequirementIDsForRepository port; (2) adds ONE owner document route whose allowlist is the assembled documentation-role member set, closing E22-10, and re-measures GET /v1/release/state now that V2-091 attaches the observer; (3) closes cap-repository-registration with NO production change, reading the real GitHub and git through the existing clients and submitting the bounded Observation to the existing :observe route so executability leaves unobserved; (4) issues TWO records, release-live-dogfood and provider-live-claude-dogfood, both passed, five capability checks passed, seven skipped as ineligible-by-declaration derived from the declaration file, zero failed -- under a NEW owner-issued preflight record, at an observation commit anchored by a pushed durable ref, with the exercise file set published and its intersection measured with controls.",
  "allowed_paths": [
    "internal/api/api.go",
    "internal/api/api_test.go",
    "internal/api/live_dogfood_test.go",
    "internal/api/ownerdocs.go",
    "internal/api/ownerdocs_test.go",
    "internal/application/readmodels.go",
    "internal/application/readmodels_test.go",
    "internal/application/allocation.go",
    "internal/application/allocation_test.go",
    "internal/web/owner.html",
    "internal/web/owner.js",
    "internal/contracts/capability_evidence_test.go",
    "internal/contracts/capability_declaration_test.go",
    "contracts/openapi/openapi-v1.yaml",
    "contracts/release-contract/foundation.json",
    "docs/preview/capabilities.md",
    "docs/preview/stable-diff.md",
    ".agents/v2/provider-preflight/V2-095-provider-live-claude-redogfood.json",
    ".agents/v2/evidence/V2-095-release-live-dogfood.json",
    ".agents/v2/evidence/V2-095-provider-live-claude-dogfood.json",
    ".agents/v2/evidence/artifacts/V2-095-live-exercise-files.json",
    ".agents/v2/evidence/index.json"
  ],
  "prohibited_paths": [
    "ci/**",
    "ci/components.json",
    "Makefile",
    "go.mod",
    "go.sum",
    "devbox.json",
    "devbox.lock",
    "Containerfile",
    "firestore.indexes.json",
    "firebase.json",
    "internal/domain/**",
    "internal/release/**",
    "internal/store/**",
    "internal/scheduler/**",
    "internal/runner/**",
    "internal/reconciler/**",
    "internal/provider/**",
    "internal/quota/**",
    "internal/update/**",
    "internal/legacyimport/**",
    "internal/ci/**",
    "internal/infra/**",
    "cmd/**",
    "cmd/control-plane/**",
    "cmd/runner/**",
    "cmd/bootstrap/**",
    "cmd/ci-plan/**",
    "cmd/legacy-import/**",
    "internal/application/service.go",
    "internal/application/service_test.go",
    "internal/application/framing.go",
    "internal/application/framing_test.go",
    "internal/application/human_input.go",
    "internal/application/human_input_test.go",
    "internal/application/caller.go",
    "internal/application/requested_by_test.go",
    "internal/application/ports.go",
    "internal/application/repository.go",
    "internal/application/release_surface.go",
    "internal/application/release_surface_test.go",
    "internal/application/publication.go",
    "internal/application/provider_registry.go",
    "internal/application/outbox.go",
    "internal/application/idempotency.go",
    "internal/application/errors.go",
    "internal/application/runner_version.go",
    "internal/application/stop_matrix_test.go",
    "internal/application/source_guard_test.go",
    "internal/api/auth.go",
    "internal/api/auth_test.go",
    "internal/api/repositories.go",
    "internal/api/repositories_test.go",
    "internal/api/operator_record.go",
    "internal/api/operator_record_test.go",
    "internal/api/live_local_test.go",
    "internal/web/web.go",
    "internal/web/owner.css",
    "internal/contracts/contracts_test.go",
    "internal/contracts/release_contract_test.go",
    "internal/contracts/provider_preflight.go",
    "contracts/schemas/**",
    "contracts/fixtures/**",
    "contracts/release-contract/foundation-capabilities.json",
    "contracts/release-contract/README.md",
    "docs/preview/index.md",
    "docs/preview/README.md",
    "docs/stable/index.md",
    "docs/product/**",
    "docs/architecture/**",
    "docs/operations/**",
    "README.md",
    "AGENTS.md",
    "scripts/**",
    ".github/**",
    ".gitleaks.toml",
    "infra/**",
    ".codex/**",
    ".agents/v2/task-state/**",
    ".agents/v2/packets/**",
    ".agents/v2/DECLARATION-GAP.md",
    ".agents/v2/HANDOFF.md",
    ".agents/v2/DEFERRED.md",
    ".agents/v2/README.md",
    ".agents/v2/historical/**",
    ".agents/v2/provider-preflight/V2-017-provider-live-claude.json",
    ".agents/v2/provider-preflight/V2-022-provider-live-claude-dogfood.json",
    ".agents/v2/provider-preflight/V2-063-provider-live-claude-refresh.json",
    ".agents/v2/provider-preflight/V2-075-provider-live-claude-rebind.json",
    ".agents/v2/provider-preflight/V2-080-provider-live-claude-pooled.json",
    ".agents/v2/evidence/V2-022-release-live-dogfood.json",
    ".agents/v2/evidence/V2-022-provider-live-claude-dogfood.json",
    ".agents/v2/evidence/V2-081-m5-gate.json",
    ".agents/v2/evidence/artifacts/V2-072-live-exercise-files.json",
    ".agents/v2/evidence/artifacts/V2-075-live-exercise-files.json",
    ".agents/v2/evidence/artifacts/V2-080-live-exercise-files.json"
  ],
  "contracts_changed": [
    "contracts/openapi/openapi-v1.yaml",
    "contracts/release-contract/foundation.json"
  ],
  "acceptance_criteria": [
    "A1. THE SCOPE-BOUNDARY TABLE GOES INTO BOTH EVIDENCE RECORDS BEFORE ANY SOURCE FILE IS EDITED, AND COMMIT 1 IS THE RECORDS. Five rows, each a list and not a sentence: (1) what this task makes true; (2) what it deliberately does not do; (3) what it measured, found broken and did NOT fix, with the file and line; (4) which paths it may write, copied from allowed_paths; (5) which paths it may not, with the reason each prohibition exists. The requirement is the ORDER: V2-087 completed this table, put it in all four of its records and measured everything, and still reported the order as its single shortfall because it wrote the table after implementing -- an after-the-fact table can always be made consistent with whatever was touched, so it is not evidence that a boundary was kept. REVISION IS PERMITTED (section 12.3): if the boundary must be widened, record the PRE-REVISION table verbatim, the revision time and the revision reason in the same records, then widen in place. Widening in place with a recorded history is PREFERRED over proposing a new task; do NOT propose a task id, because M5's remediation budget stands at 5 of 5 and a sixth task triggers a judgement that the milestone is mis-cut.",
    "A2. RE-MEASURE THE BASELINE YOURSELF. EVERY NUMBER IN dp-v2-095 AND IN THIS WORK ORDER IS EXPECTED TO BE STALE. Each was read by the designer at HEAD a602b98 with the command given below; V2-086 reported nineteen disagreements against its packet and V2-085 twelve. Report BOTH values for every disagreement and use the measured one. Run and record: (a) `grep -rn 'domain.RequirementStartFraming' --include='*.go' . | grep -v _test.go` -- expect 2 lines, framing.go:14 comment and framing.go:191 issuer; the same for RequirementNeedInput (expect 2, human_input.go:17 and :519), RequirementReadyCommand (expect 5), RequirementStart (expect 2), and Wait, Recover, Evaluate, Pause and Cancel (expect 0 each). (b) `sed -n '455,505p' internal/domain/model.go` -- confirm start-framing is admitted from captured only and needs-input from framing, active and evaluating. (c) `grep -n 'HasPrefix(component' internal/contracts/provider_preflight.go` -- expect exactly line 160 and the literal provider-live- ; and `grep -rn 'release-live' --include='*.go' .` -- expect 0. (d) `grep -n 'capabilityEvidenceReverse(claims' internal/contracts/capability_evidence_test.go` and the record-collection loop above it -- expect the hard-coded V2-022 scope. (e) `python3` over contracts/release-contract/foundation-capabilities.json printing each capability's external_dependencies systems and providers -- expect 4 declaring Google Cloud Run, 3 declaring codex and opencode, 5 declaring neither. (f) the RequirementView field list in internal/application/readmodels.go and the nextAction signature and call site. (g) `grep -n 'MaxPageSize =' internal/application/readmodels.go` (expect 100) and `grep -n 'MaxCandidates' internal/scheduler/scheduler.go` (expect 100). (h) `grep -rn 'AttachReleaseObserver|NewSourceReleaseObserver' --include='*.go' . | grep -v _test.go` -- and if V2-091 has landed this will DISAGREE with the packet's zero; measure V2-091's constructor name and its environment variable names and record them, because dp-v2-095 d11 could not know them. (i) `grep -rn --include='*.go' 'docs/stable|docs/preview' internal/api internal/web | grep -v _test.go` -- expect 0. (j) the five cost ledgers under /home/takushi/.local/state/agentic-loop/v2/ : entry-state counts, the purpose histogram of V2-022's, and the settled and reserved monetary contributions per internal/runner/costledger.go:349-368. (k) `gh --version`, `gh auth status`, `git --version`, `claude --version`, `firebase --version`, `go version` and the machine identifier. (l) `devbox run --pure -- make evidence-keys` for the api component's current value. (m) the number of entries in .agents/v2/evidence/index.json -- the packet measured 171 and V2-081 measured 163. (n) the compiled closure of ./internal/api intersected with git ls-files -- the packet measured 88 paths. Report every disagreement in both records.",
    "A3. DERIVE THE PRODUCT AUTHORITY FIRST AND STOP IF YOUR READING DIFFERS. For each of the twelve capabilities, quote from contracts/release-contract/foundation-capabilities.json its user_action, observable_result, outcome_conditions.success, outcome_conditions.failure, external_dependencies.systems and external_dependencies.providers. Then state, per capability, whether the failure condition imports the observable_result items (cap-backlog-visibility's does: '必須の確認項目が欠落する'; cap-repository-registration's does not: it names only 識別情報 and 実行可否). Every verdict below is judged against those quoted clauses and against nothing else. If your reading of any clause differs from dp-v2-095 d2, STOP and report before writing code.",
    "A4. THE DEFERRAL PARTITION BECOMES A DETERMINISTIC TEST. Add internal/contracts/capability_declaration_test.go. It reads contracts/release-contract/foundation-capabilities.json and asserts, by capability id and not by count alone: the set declaring 'Google Cloud Run' among external_dependencies.systems is exactly {cap-preview-operation, cap-stable-promotion, cap-loop-control, cap-loop-self-update}; the set declaring both 'codex' and 'opencode' among external_dependencies.providers is exactly {cap-autonomous-resolution, cap-shared-resource-allocation, cap-provider-operation}; the two sets are disjoint; the remaining set is exactly {cap-repository-registration, cap-requirement-intake, cap-backlog-visibility, cap-human-input-request, cap-user-documentation}; and the three sizes sum to the declared capability count. Assert the REVERSE CONSTRAINT in the same table: no member of the third set declares Google Cloud Run, codex or opencode. Add a negative control over synthetic decoded input proving each assertion can fail. Do NOT read the machine's authentication state in this test: that half is not deterministic and belongs in the live record.",
    "A5. GENERALISE THE UNDER-REPORTING GUARD, AND PROVE THE GENERALISATION BOTH WAYS. In internal/contracts/capability_evidence_test.go, remove the single-task-id scope from the reverse direction so it judges every record the index names, and widen the record-collection loop accordingly. Before doing it, MEASURE that this is non-breaking with the command in A2 and record the result: scan every index entry for checks whose name equals one of the declared capability ids and report which records carry any -- the designer measured exactly one, ev-v2-022-release-live-dogfood, with 12 such checks of which 1 is passed, and zero elsewhere including all five provider-live records. Then run the real-tree test and record zero violations. Then add a negative control that drives the generalised reverse rule with a synthetic record whose task id is NOT V2-022, reporting a passed capability the synthetic contract does not cite, and assert EXACTLY ONE violation; and a positive half where citing it yields zero. A generalisation without that control is vacuous. Do NOT edit internal/contracts/contracts_test.go or release_contract_test.go: both are prohibited.",
    "A6. cap-backlog-visibility, PART 1: THE MISSING CONFIRMATION ITEMS. In internal/application/readmodels.go extend the LIST projection so each row carries, in addition to what it carries today: each Increment's STATUS as well as its id; the next action, computed by the EXISTING nextAction function called with the same arguments the detail view gives it and with NO new variant of it; and the Requirement's Preview/Stable reflection, projected from the domain.Requirement.StableSnapshot the list already reads, reported as an explicit absence when the snapshot is zero and never as a plausible release. The two reads this needs are the EXISTING batch ports IncrementsForRequirements and ExecutionsForIncrements, called ONCE each per page and never once per row. Cap the total number of increment ids collected across the page at MaxPageSize and set the existing Truncated flag truthfully when the cap binds; a page whose increments exceed the cap must SAY it was bounded rather than silently drop rows. Do not add a port, do not add a store query, do not change the existing field names or their json tags, and do not change RequirementDetailView.",
    "A7. cap-backlog-visibility, PART 2: 優先度とその根拠, AS A PROJECTION OF WHAT IS ALREADY COMPUTED AND DISCARDED. In internal/application/allocation.go, project the scheduler's own per-candidate decisions into the queue summary response. allocationReport already calls scheduler.Decide over one bounded page of at most scheduler.MaxCandidates and keeps only a reason histogram; plan.Decisions already carries RequirementID, Rank, Score, Assigned, Reason and Inputs. Add NO read: consume what is in hand. Each projected entry reports the requirement id, the rank, whether it was assigned, the reason from scheduler.AllReasons' closed set when it was not, and the score inputs actually used INCLUDING UsedAssessment, which is measured false for every candidate today because BuildAllocationSnapshot sets neither Priority nor Assessment -- report that fact rather than hiding it. A Requirement not among the candidates the scheduler ranked carries NO entry, and the response says the projection was bounded and why; never emit a rank for a Requirement no decision covered. Assert that the reason value is always a member of scheduler.AllReasons and that an unknown reason is a refusal rather than a silent bucket, preserving the existing fail-closed behaviour at the waiting-bucket mapping.",
    "A8. cap-backlog-visibility, PART 3: THE REPOSITORY FILTER (E22-7). Add an optional repository_id query parameter to GET /v1/requirements. It resolves through the EXISTING RequirementIDsForRepository port and nothing else: internal/store/** is prohibited, so if you find yourself needing a new query or a new Firestore index the design was departed from -- STOP and report. Requirements: an unknown repository id returns an empty list, NOT the unfiltered page, which is the exact defect E22-7 measured; the parameter composes with page_size and cursor; the bound the port already reports is surfaced rather than hidden; a malformed value is a 400 with the existing invalid_request shape and the existing message style. Assert in internal/api/api_test.go that with and without the parameter the pages differ for a Requirement linked to one Repository, and that the filtered page never contains an unlinked Requirement.",
    "A9. cap-user-documentation, PART 1: THE OWNER DOCUMENT ROUTE, WITH A DERIVED ALLOWLIST. Add internal/api/ownerdocs.go. Two GET routes on the EXISTING Authenticator and RoleOwner seam, under the existing /owner/ prefix, with no new authentication mechanism and no new header: an index that names the channel resolved by the release package and the version of the assembled contract and lists the documents available; and a per-document route. The set of documents served is EXACTLY the documentation-role member set of the bundle assembled from the release source root that V2-091's wiring configures -- the same set the exercise lists as dogfoodDocSet -- looked up as a SET MEMBERSHIP TEST, never by joining a caller-supplied path onto a root. A path outside the set is refused before any file is opened; a traversal attempt is refused by the same test; a non-owner caller is refused exactly as /owner/ already refuses one. With NO release source configured both routes answer the SAME 503 shape GET /v1/release/state answers, with an explicit code, and never a default root -- internal/application/release_surface.go already records why a defaulted root is a lie, and V2-091's acceptance asserts that behaviour. internal/release/** is prohibited: reuse its assembly and channel resolution, do not reimplement or hand-write the member list. Declare both routes in contracts/openapi/openapi-v1.yaml, which release-contract.md condition 8 requires for an owner API path.",
    "A10. cap-user-documentation, PART 2: THE 503 IS V2-091's AND MUST NOT BE WIRED TWICE. Measure whether V2-091's release-source wiring has landed. If it has NOT, STOP and escalate: do not add a second wiring path, do not attach an observer from internal/api, and do not default a root. If it has, record its constructor name and its environment variable names as A2 measurements, start the dogfood process WITH that configuration so the route the documents describe actually answers, use the SAME configuration for A9's routes so there is one release-source configuration and not two, and then REVERSE the inverted assertion in the exercise that currently fails on purpose if GET /v1/release/state ever returns 200 -- reverse it, do not delete it, and record in both records that the wiring belongs to V2-091 and that this task added none.",
    "A11. cap-user-documentation, PART 3: THE DOCUMENTS MUST AGREE WITH THE BEHAVIOUR, AND THREE INVARIANTS MAKE THE OBVIOUS EDIT FATAL. Update docs/preview/capabilities.md and docs/preview/stable-diff.md so they describe the behaviour at THIS commit and nothing more (section 12.5). Four passages the designer measured become false when this task lands and must be corrected: capabilities.md line 80 (the 503 measurement), line 150 (cap-user-documentation's two reasons and its 証跡なし), and stable-diff.md lines 33-34 and 64-65 (the unwired observer and the missing document route). THE TRAPS, all evaluated inside the live exercise over the real doc set: the exercise counts fenced code blocks across the five documents and FAILS unless the total is ZERO, so do NOT write a fenced example of the new route; release.VerifyCapabilityAnchorBijection requires the anchor set of capabilities.md to equal the contract's capability ids in BOTH directions; release.VerifyRequiredSections requires stable-diff.md to keep all four required Preview sections; and VerifyNoStableToPreviewLinks must still hold. docs/preview/index.md and docs/stable/index.md are PROHIBITED on purpose so that a release-marker edit is a build failure rather than a discovery -- if you believe one of them must change, STOP and report it as a finding in row (3).",
    "A12. cap-repository-registration: CLOSE IT WITH ZERO PRODUCTION CHANGE. This capability needs no source edit at all; internal/runner/** and internal/api/repositories.go are PROHIBITED so that a change to them means the closure was widened rather than reached. Inside the live exercise, after the existing registration of the real repository: resolve the real gh executable and the real git executable to ABSOLUTE paths through the existing runner clients; read the real repository read-only through the existing forge reachability path, exactly as internal/runner/forge_live_test.go does, with the guarded base environment and an EMPTY granted set; POST the resulting bounded Observation to the EXISTING runner-role /v1/repositories/{repository_id}:observe route with the runner token; then read GET /v1/repositories/{repository_id} and assert executability has LEFT the unobserved state, that its reason names the Observation rather than the absence of one, and that the recorded Observation carries the forge node id and the default branch that came from the real read. Prove the no-unauthorised-change clause the way the existing live exercise proves it: the only argv the read path can produce is the read-and-version pair, and nothing creates, modifies or deletes any branch, ref, tag, pull request, issue or repository. Record gh version, git version, both resolved absolute paths, the forge node id and the observed default branch. Record the three observables internal/application/repository.go renders as not-implemented, with their stated reasons, as measured absences that this capability's declared failure condition does not import.",
    "A13. cap-human-input-request: ISSUE THE VERDICT AND CHANGE NO PRODUCTION LINE. internal/domain/**, internal/application/framing.go and internal/application/human_input.go are all PROHIBITED. The existing probe already asserts every clause of the declared success condition; do not rewrite it, do not add a second walk, and do not weaken any of its assertions. What this task adds is the verdict: quote the success condition, map each clause to the assertion in the probe that discharges it by line, and record the version chain the probe measures. If you conclude that closing this capability needs a source edit, the measurement in dp-v2-095 d1 has changed -- STOP and report rather than extending the boundary.",
    "A14. NEW OWNER-ISSUED PREFLIGHT RECORD, AND NO EXISTING LEDGER IS TOUCHED. Create .agents/v2/provider-preflight/V2-095-provider-live-claude-redogfood.json with task_id V2-095, its OWN limits.ledger_path under /home/takushi/.local/state/agentic-loop/v2/ that does not exist yet, and accounting that starts from an empty ledger. Name NO existing record and reuse NO existing ledger: the four existing ledgers stand at 11/16, 23/24, 11/12 and 12/12 and V2-080's at 10/48 -- re-measure all five and report disagreements. Raising a spent record's thresholds, re-initialising a ledger, clearing a halted flag and editing any ledger file are ALL PROHIBITED; internal/runner/costledger.go re-verifies the preflight digest and the three limit values on every reservation, so editing an approved record invalidates its ledger instead of raising the cap, and the only remedy is a new record. limits.max_invocations is 24 and limits.max_total_cost_usd is 20.00, and the record's notes must show the arithmetic: one full run of the exercise is exactly THREE reservations (measured from V2-022's purpose histogram: autonomous-resolution 9, graceful-stop-live-child 7, resume-after-channel-rollback 7, over seven runs), the graceful-stop reservation can NEVER settle because that purpose kills the child, four runs are planned (two for the stability pair, one to break a tie rather than leave it standing, one to confirm after any post-observation fix) which is 12, the measured reserved-never-settled fraction across all five ledgers is 17 of 67 or 25.4 percent, and V2-022's measured cost was 23 reservations for seven runs or 3.29 per run -- so 24 admits seven runs at the measured rate, i.e. nearly twice the plan. Show the monetary arithmetic too, because it is NOT true that money is far away for the reason usually given: an UNSETTLED reservation blocks its full 1.00 USD and a settled one blocks min(actual, reserved), so V2-022 stood at 1.008563 plus 7.00 plus the 1.00 worst-case reservation, which is 9.008563 of 20.00 -- 45 percent of the money at 96 percent of the count. environment.base_names is exactly HOME and PATH and granted_names is EMPTY. approval cites the owner's standing authorization with subject_path .agents/v2/packets/provider-standing-authorization.json and subject_digest computed with sha256sum at this commit; approved_at must be STRICTLY BEFORE observed_at. Commit this record BEFORE the observation and do not change its bytes afterwards: the exercise reads it at run time and its digest is bound into both evidence records.",
    "A15. THE OBSERVATION: WHAT IS RUN, IN WHAT ORDER, AND WHAT STOPS IT. Before the FIRST provider invocation: `devbox run --pure -- go test -race -count=1 ./...`, `devbox run --pure -- go vet ./...`, `devbox run --pure -- sh -c 'gofmt -l .'` with EMPTY output, the touched `make component-*` targets, and everything committed. V2-022 was forced into re-observation because internal/api/live_dogfood_test.go needed a whitespace-only gofmt fix AFTER its runs; that file is inside the exercise's own compiled set, so any edit after the observation forces re-observation under G6b regardless of its nature. Then start the Firestore emulator for real through scripts/firestore-emulator.sh and run the full exercise TWICE as two separate processes on the IDENTICAL final commit, each with -count=1, recording the complete argv and both verdicts: `devbox run --pure -e AGENTIC_LOOP_LIVE_DOGFOOD=1 -e AGENTIC_LOOP_LIVE_PROVIDER=1 -e HOME=/home/takushi -- scripts/firestore-emulator.sh go test -count=1 -v -timeout 3600s -run TestFoundationPreviewLocalDogfood ./internal/api` plus whatever gate variables the forge and git halves and V2-091's release-source configuration require, each passed with -e because `devbox run --pure` strips the environment. Judge every devbox run BY ITS VERDICT LINE, never by the shell exit status. HANDLING A DISAGREEMENT BETWEEN THE TWO RUNS: if it is confined to a subtest whose capability is ineligible-by-declaration -- cap-autonomous-resolution is the one that disagreed in V2-022, and its capability is now deferred -- transcribe BOTH observations, record the failure class and code, and do NOT let it make the record partial; the budget admits a third run to break the tie, and a tie must be BROKEN rather than left standing, which is the failure V2-022's own A19 reported about itself. If the disagreement touches ANY of the five M5-owned capability subtests, that is a FAIL-CLOSED STOP: escalate, do not retry, do not overwrite, do not quarantine. At least one settled invocation carrying a provider-issued session_id must exist across the run or the provider record may not claim a real CLI was reached. Reaching the record's threshold is a fail-closed stop for inspection, reported as neither a success nor a capability failure.",
    "A16. TWO EVIDENCE RECORDS, AND THE LEDGER TEST IS THE DEFINITION OF DONE. Create exactly TWO: .agents/v2/evidence/V2-095-release-live-dogfood.json (id ev-v2-095-release-live-dogfood, component release-live-dogfood) and .agents/v2/evidence/V2-095-provider-live-claude-dogfood.json (id ev-v2-095-provider-live-claude-dogfood, component provider-live-claude-dogfood). Both: task_id V2-095, correlation_id v2-095-m5-gap-closure-and-redogfood, result PASSED, observed_at the real observation instant, the observation commit recorded. The release-live record carries TWELVE capability-named checks -- exactly five passed (cap-requirement-intake, cap-repository-registration, cap-backlog-visibility, cap-human-input-request, cap-user-documentation), exactly seven SKIPPED, ZERO failed -- plus the A-numbered checks of this Work Order. Each skipped check is named exactly the capability id, and its summary quotes the declared external system that derives the deferral (Google Cloud Run for the four whose evidence belongs to D1; codex and opencode for the three whose evidence belongs to M6) and records the machine measurement that codex and opencode are unauthenticated here, re-measured at observation time. The check status enum is exactly passed, failed and skipped -- there is no ineligible member -- so `skipped` IS the representation of ineligible-by-declaration, and the record must state that plainly: no skipped check may be read as a pass (G7), no deferred capability may be called failed (section 8.3), and no M5-owned gap may be called deferred (section 8.3's reverse constraint). BOTH records carry an artifact_refs entry with artifact_id provider-preflight whose sha256 equals the sha256 of the preflight record's own bytes. The provider-live record has that binding and the approved_at-before-observed_at ordering ENFORCED by internal/contracts.CheckProviderPreflightLedger; the release-live record does NOT, because provider_preflight.go:160 selects only components beginning provider-live- -- so the release-live record carries a check that recomputes the digest, compares it, and STATES that no check verifies this for its component. Record `devbox run --pure -- go test -race -count=1 ./internal/contracts` green as the definition of done. Append exactly two entries to .agents/v2/evidence/index.json.",
    "A17. THE EVIDENCE KEY IS PROVENANCE, NOT A COMPOSITE, AND NOT A THING TO INVENT. Both records' evidence_key is the `api` component's key -- the component of the exercising package -- read with `devbox run --pure -- make evidence-keys` on the FINAL tree with every touched file already git-added, because the manifest reads git ls-files. Do NOT declare a composite key computed from constituent component keys: V2-022 did, and its own A23 check measured that the premise was FALSE because V2-045's transitive-roots redesign put contracts/** and .agents/** inside the closures of api, application, control-plane, runner and store-firestore, so five of eleven constituents moved and the composite was stale by construction the moment the evidence commit landed. State in both records that under G6a the recorded key is the record of provenance and that freshness for these records is G6b's measured intersection, and that the api key is expected to move when the evidence commit lands for exactly the reason V2-022 measured.",
    "A18. PUBLISH THE EXERCISE FILE SET AS AN ARTIFACT, IN THREE GROUPS, WITH REPRODUCTION ARGV. Create .agents/v2/evidence/artifacts/V2-095-live-exercise-files.json naming the exercise (package ./internal/api, test TestFoundationPreviewLocalDogfood, its gate variables and its subtest list in execution order), the observation commit, and three path groups. GROUP compiled: every package of this module reported by `go list -deps -test ./internal/api`, contributing GoFiles, CgoFiles and EmbedFiles only, intersected with `git ls-files` -- the designer measured 88 paths at a602b98, spanning internal/runner 20, internal/application 15, internal/api 10, internal/update 8, internal/provider 7, internal/domain 6, internal/release 5, internal/contracts 5, internal/web 4, internal/store/firestore 3, internal/scheduler 3, internal/store/memory 1 and internal/quota 1; note that internal/web/owner.html, owner.js and owner.css are in the set as EmbedFiles, so a console edit is inside the exercise. Do NOT add dependency packages' TestGoFiles: go list already folds internal/api's own test files into that variant's GoFiles. GROUP read_at_run_time: every repository path the exercise OPENS while running -- the preflight record, the standing authorization, contracts/release-contract/foundation.json, the five documentation members, and whatever else you measure -- each with the reason it is read. GROUP excluded_with_reason: the two evidence records, .agents/v2/evidence/index.json, this artifact, and contracts/release-contract/foundation.json, each with its reason; foundation.json's reason is that internal/contracts/capability_evidence_test.go's forward rule requires a cited record to already exist and carry that capability's passed check, so its evidence_ids cannot precede the record, and the exclusion is BOUNDED by the measurement in A20. Publish reproduction argv for three operations, following the shape .agents/v2/evidence/artifacts/V2-080-live-exercise-files.json already publishes: recompute the compiled set, read the published set, and measure the intersection against a judging commit passed as the single positional argument.",
    "A19. G6b IS MEASURED WITH CONTROLS, AND A ZERO WITHOUT A POSITIVE CONTROL IS NOT ACCEPTED. At the final judging commit, run the artifact's own intersection argv and record the output verbatim. Then run a POSITIVE control over a fictional diff list -- editing no tracked file -- that names internal/api/live_dogfood_test.go, one internal/application source in the set and one internal/web embedded asset, and assert all three print with a non-zero count. Then run a NEGATIVE control over real diff lines that are outside the set and assert zero. Record all three numbers. Transcribe the full observation-to-judgement diff, path by path, into the release-live record. Also record, as a fact and not as a task, that three of this task's paths -- internal/application/allocation.go, internal/application/readmodels.go and internal/application/release_surface.go if it is touched at all -- are inside the 103-path set published for V2-080, so ev-v2-080-provider-live-claude-pooled is no longer G6b-fresh after this task lands; V2-018 is terminal and no M3 gate is pending, so nothing is invalidated today, but a future M3 re-judgement will need its own re-observation. Do NOT propose a task id.",
    "A20. THE ONE BUNDLE MEMBER THAT MUST FOLLOW THE OBSERVATION, AND THE MEASUREMENT THAT BOUNDS IT. contracts/release-contract/foundation.json's evidence_ids for the five passed capabilities must cite ev-v2-095-release-live-dogfood, and the seven skipped capabilities keep their EMPTY arrays. That edit cannot precede the record. So: run `git diff <observation-commit> <judging-commit> -- contracts/release-contract/foundation.json` and assert the diff contains ONLY additions inside evidence_ids arrays -- the release string, the capability id list, every status field, the field order and every other byte identical -- and transcribe the diff into the record. Every OTHER bundle member (the documentation files, contracts/openapi/openapi-v1.yaml and everything else) lands BEFORE the observation. No capability may be given status stable, and no evidence id may be written for a capability whose check is skipped.",
    "A21. THE OBSERVATION COMMIT GETS A DURABLE REF THAT IS ON THE REMOTE. Push exactly one ref and nothing else: `git push origin <observation-sha>:refs/live-observations/v2-095-release-live-dogfood`. Then re-read the remote with `git ls-remote origin 'refs/live-observations/*'` and transcribe the output, which must show this ref alongside v2-075-provider-live-claude and v2-080-provider-live-claude. Do NOT push a branch. V2-075's observation commit was never pushed, is unreachable from v2, is referenced by six files, and a clone would have lost it -- which would have made G6b permanently unmeasurable for that record. Record the ref name, the sha and the ls-remote output in both records.",
    "A22. DETERMINISM IS ACCEPTANCE FOR EVERYTHING EXCEPT THE PROVIDER PATH. No fixed sleep, no wall-clock timer, no goroutine and no randomness in any new or modified test. Every clock injected. Every wait a bounded deadline poll reusing the exercise's EXISTING dogfoodPoll idiom -- do not add a second waiting mechanism. Run the whole non-live suite with -race. The new list projection, the summary projection, the repository filter and the document allowlist are pure functions of values already read, so their tests are table tests over decoded inputs with no clock at all; if one of them needs a clock, the design was departed from. The live run is NOT deterministic: every duration measured during the observation is recorded as an OBSERVATION and never asserted as a threshold, and the two-run pair is the disclosure mechanism, not a determinism claim.",
    "A23. G7: A SKIPPED TEST IS NEVER A PASS, AND THE PATTERN GOES BESIDE THE NUMBER. Record the live exercise's own skip count as ZERO with the exact grep pattern used. Record the repository-wide counts with BOTH patterns, because both are correct and they differ: for internal/store/firestore with no emulator `grep -c '^--- SKIP'` gives 26 top-level and `grep -cE '^ *--- SKIP'` gives 49 including subtests -- re-measure both and report any disagreement; for ./internal/api with the live gates unset both patterns give 2, the two gated live tests. Start the Firestore emulator for real through scripts/firestore-emulator.sh for every run that claims a real store; a run without it is not an emulator run and may not be recorded as one. State explicitly in the record that the seven SKIPPED capability CHECKS are a record-layer classification of subtests that all RAN, and that the go test verdict carries zero SKIP lines -- conflating those two layers is how a skipped test becomes a pass.",
    "A24. THE GRADE, AND THE SENTENCES THAT MAY NOT BE WRITTEN. State once in both records: the environment class is preview-local -- the owner's own machine, the real cmd/control-plane process, the real claude CLI, the real gh and git CLIs, the Firestore emulator as canonical store -- with the machine identifier, the emulator name and version, every resolved CLI absolute path and version, the go toolchain, the devbox version, the worktree path, the branch and the observation commit. Under G3 item 2 preview-local IS the real-environment grade for conditions about the Loop's own observable behaviour and is not called a substitute; under G3 item 1 the GitHub and Git halves are satisfied by the real identifiers A12 records. State what preview-local cannot establish and whose it is: the IAP boundary, scale-to-zero, real Firestore permissions and contention, the deploy path and every Cloud Run fact belong to D1. FORBIDDEN VERBATIM in both records and in any summary: 'this was observed on Cloud Run', 'the deployed environment', 'a Stable promotion occurred', 'all twelve capabilities were exercised', 'all capabilities passed', any sentence asserting a capability whose check is skipped, any description of the seven skipped capabilities as failed, and any description of the four M5-owned gaps as deferred. Grep both records for each and record the zero.",
    "A25. ADDITIVE, PROVEN BY MEASUREMENT, WITH THE COMMAND FOR EACH. Report an empty diff for: internal/domain, internal/release, internal/store, internal/scheduler, internal/runner, internal/reconciler, internal/provider, internal/quota, internal/update, internal/legacyimport, cmd, internal/application/service.go, internal/application/framing.go, internal/application/human_input.go, internal/application/caller.go, internal/application/ports.go, internal/application/repository.go, internal/api/repositories.go, internal/api/auth.go, internal/web/web.go, ci/components.json, Makefile, go.mod, go.sum, devbox.json, devbox.lock, firestore.indexes.json, firebase.json, contracts/schemas, contracts/fixtures, contracts/release-contract/foundation-capabilities.json, docs/preview/index.md and docs/stable/index.md. ci/components.json and Makefile are PROHIBITED because no import edge moves: the new file internal/api/ownerdocs.go is inside the api component's existing root internal/api/**, internal/api already imports internal/application and internal/release is reached through internal/application exactly as it is today, and internal/contracts' two test files add no non-test edge. ci/** sits in all_on_change, so touching it moves all 23 component keys. A RED MANIFEST MEANS THE DESIGN WAS DEPARTED FROM -- stop and report rather than declaring the edge.",
    "A26. VALIDATION, AND YOU DO NOT RUN THE FULL make check. The coordinator runs it once at integration. Yours: `devbox run --pure -- go test -race -count=1 ./...`; `devbox run --pure -- go vet ./...`; `devbox run --pure -- sh -c 'gofmt -l .'` with empty output; `devbox run --pure -- make component-api`, `-application`, `-web`, `-internal-contracts`, `-contracts` and `-docs`; and, AFTER the evidence commit, `devbox run --pure -- gitleaks dir .agents --config .gitleaks.toml` with its verdict recorded. NEVER run `gitleaks dir .`: it scans the gitignored build/evidence/** that make evidence-all writes and reports 26 findings, so the tracked state becomes unreadable. `make smoke` is NOT required because no cmd/ binary changes and cmd/** is prohibited; if you believe smoke is needed, a cmd file was touched and the boundary was crossed. Judge every devbox run BY ITS VERDICT LINE. `devbox run --pure` strips environment variables, so pass them with -e and never as a shell prefix; devbox.json declares exactly five shell scripts (check, smoke, infra-policy, infra-lint, infra-validate) so other names fall through to the binary, but wrap anything with shell metacharacters as `devbox run --pure -- sh -c '...'`.",
    "A27. CHAIN POSITION, RESIDUAL FINDINGS AND WHAT STAYS UNISSUED, RECORDED AS FACTS FOR THE COORDINATOR AND NOT AS TASKS. Record: (i) internal/contracts/provider_preflight.go:160 selects only components beginning provider-live-, so no check enforces the preflight binding on a release-live- record; the file is prohibited here and the gap is in the check's reach, not in this task's records. (ii) DECLARATION-GAP.md L92b claims domain.PriorityAssessment does not exist and its own grep, re-run at this commit, returns the type at internal/domain/model.go:66 plus its consumers in internal/scheduler -- the row is false and the ledger is prohibited here, so record the correction rather than editing it. (iii) The ledger rows this task closes by their claim text -- the unattached release observer, and the absence of an HTTP path routing documents by channel and version -- so a later single pass can strike them. (iv) internal/application/repository.go renders three of cap-repository-registration's six declared observables as not-implemented with stated reasons; the declared failure condition does not import them, and no repair is attempted here. (v) The observable_result item that no surface can carry today is the multi-factor priority assessment feeding the ranking: BuildAllocationSnapshot supplies neither Priority nor Assessment, so UsedAssessment is false for every candidate, and section 5 assigns that connection to V2-030 in M7 -- this task reports the fact in the response rather than fabricating a factor. (vi) Section 12.2 freezes gate dependencies: V2-092 consumes this task and its dependencies are already fixed, so design nothing that would require adding one. DO NOT PROPOSE A TASK ID. M5's remediation budget stands at 5 of 5 and a sixth task triggers a judgement that the milestone is mis-cut; a finding outside this boundary goes into row (3) of the A1 table, or the boundary is widened in place under section 12.3.",
    "A28. COMMIT ORDER IS PART OF THE DESIGN AND IS ITSELF AN ACCEPTANCE ITEM. Commit 1: the two evidence records carrying the A1 boundary table and the A2 baseline, before any source file is edited. Commit 2: the preflight record. Then the deterministic repairs and their proofs as their own commits (the two internal/contracts tests; the list and summary projections; the repository filter; the document routes and the openapi declaration; the console links). Then the documentation edits. Then gofmt, vet and the whole non-live suite green and committed. Then the observation, and the durable ref pushed. Then the final commit: the two records' full contents, the exercise file set artifact, the index entries and foundation.json's evidence_ids -- and nothing else, so the observation-to-judgement diff is exactly those five paths. Verify after that commit that the intersection measurement in A19 holds; if it does not, the order was broken and re-observation is required regardless of the nature of the change."
  ],
  "validation": {
    "argv": [
      "devbox",
      "run",
      "--pure",
      "--",
      "go",
      "test",
      "-race",
      "-count=1",
      "./internal/api",
      "./internal/application",
      "./internal/contracts",
      "./internal/web",
      "./internal/release",
      "./internal/domain",
      "./internal/scheduler",
      "./internal/store/memory",
      "./internal/runner"
    ],
    "working_directory": ".",
    "timeout_seconds": 5400
  },
  "constraints": [
    "allowed_paths IS A WHITELIST. Only the twenty-two paths listed there may be created or modified, including the preflight record, both evidence records, the exercise-file-set artifact and .agents/v2/evidence/index.json. ANY FILE NOT ON THAT LIST IS PROHIBITED EVEN WHERE THIS WORK ORDER'S PROSE MENTIONS IT. If a path you believe you need is absent, either widen the boundary in place under section 12.3 with the pre-revision table, the time and the reason recorded, or stop and report.",
    ".agents/v2/evidence/index.json is single-line compact JSON and append-only. Add exactly this task's two entries. Do not reformat it, do not reorder it, and do not touch any other task's entry.",
    "NEVER WRITE A VERIFICATION AS PASSED THAT WAS NOT EXECUTED. If an acceptance item cannot be satisfied, STOP AND ESCALATE. Do not weaken it, do not reinterpret it, do not substitute a narrower assertion, and do not mark it satisfied by an adjacent check. In particular: a capability check is `passed` only if every clause of its quoted success condition was measured in the run being recorded.",
    "NEVER RECORD A CAPABILITY AS PASSED WHOSE DECLARED SUCCESS CONDITION WAS NOT MET IN FULL. The five passed checks are cap-requirement-intake, cap-repository-registration, cap-backlog-visibility, cap-human-input-request and cap-user-documentation, and if any one of them cannot be honestly recorded passed, the record is NOT written as passed to make G2 succeed -- it is reported, with the clause that failed, and escalated. A record that is passed by relabelling is worse than a record that is partial.",
    "DO NOT RECORD A PRODUCT GAP AS A DEFERRED CAPABILITY. The deferral set is exactly seven and is derived from external_dependencies in contracts/release-contract/foundation-capabilities.json, which is PROHIBITED so the set cannot be grown by editing a declaration. cap-repository-registration in particular is a product gap M5 owns: it declares GitHub, Git, Firestore and owner UI, declares no provider at all, and both GitHub and Git are measurably available and authenticated on this machine, so 'could not reach' is false and section 8.3 admits no deferral.",
    "DO NOT WAIT ON A CAPABILITY THAT IS INELIGIBLE BY ITS OWN DECLARATION. The seven deferred capabilities do not block this task and must not be exercised toward a verdict. Do not attempt a Cloud Run fact, do not attempt to authenticate codex or opencode, and do not invoke them.",
    "DO NOT RE-OBSERVE BEFORE THE FOUR GAPS ARE CLOSED. The observation is the LAST thing that happens, after every repair, every documentation edit, gofmt, vet and the whole non-live suite are green and committed. Eleven of this task's source paths are inside the exercise's own compiled set, so an edit after the observation forces re-observation under G6b regardless of its nature.",
    "DO NOT RAISE A SPENT RECORD'S THRESHOLDS AND DO NOT RE-INITIALISE A LEDGER. Issue the new record instead. Editing an approved record, editing a ledger file, clearing a halted flag or re-initialising a ledger to reset a count are ALL PROHIBITED. The four existing ledgers stand at 11/16, 23/24, 11/12 and 12/12, and V2-080's at 10/48; not one byte of any of them may change.",
    "SIZING IS BY RESERVATION COUNT, NOT BY MONEY. internal/runner/costledger.go refuses when the entry count plus one exceeds max_invocations, so a reservation counts whether or not it settles, and one purpose in this exercise can never settle by construction. Do not size from the settled per-invocation figure alone: an unsettled reservation blocks its full worst-case amount against the monetary ceiling.",
    "JUDGE EVERY devbox run BY ITS VERDICT LINE, NEVER BY THE SHELL EXIT STATUS. `devbox run --pure` strips environment variables, so pass every one with -e and never as a shell prefix. Wrap anything with shell metacharacters as `devbox run --pure -- sh -c '...'`.",
    "DO NOT RUN THE FULL `make check`. The coordinator runs it once at integration. Your verification is the go test -race run, go vet, gofmt -l with empty output, the touched make component-* targets, and gitleaks dir .agents after the evidence commit.",
    "AFTER COMMITTING THE EVIDENCE, RUN `devbox run --pure -- gitleaks dir .agents --config .gitleaks.toml` YOURSELF AND RECORD THE VERDICT. NEVER run `gitleaks dir .`: it scans the gitignored build/evidence/** and reports 26 findings. `gitleaks git` reads only committed history, so scan the subtree you are about to commit BEFORE committing it as well. This trap has stopped six tasks and every catch was keyword adjacency, not a secret: a 64-character digest immediately after punctuation, a 40-character commit sha, and one subtest name sitting next to another.",
    "SECURITY, VERBATIM. Never record a prompt or a raw provider response in ANY form, including truncated or hashed excerpts. A provider-issued session_id MAY be recorded. Never record a credential value, and never record a forge token, a gh token, a git credential or any part of one. Build every secret-shaped fixture by concatenating a prefix with a suffix. The child's environment is exactly the approved record's base_names, granted_names stays EMPTY, and the CLI reads its own credential store. The forge read is read-only by construction: the only argv it may produce is the read-and-version pair, and nothing may create, modify or delete any branch, ref, tag, pull request, issue or repository.",
    "DO NOT WRITE A MODULE-PATH STRING LITERAL INTO ANY SOURCE OR TEST FILE. The manifest check reads it as an undeclared edge and turns make check red. Concatenate a prefix with a suffix if one is unavoidable.",
    "ANY DIGEST WRITTEN IN PROSE IS 64 OR 40 HEX CHARACTERS PLUS AT MOST ONE PUNCTUATION CHARACTER, and never two revisions joined by a two-dot range. Keep a long hex string off any line that also contains a secret-scan trigger word; that adjacency, not a secret, is what the scan flags.",
    "FORBIDDEN GIT OPERATIONS, because they lose uncommitted work: `git reset`, `git checkout -- <path>`, `git stash`, `git commit --amend`. If history must be rebuilt, use an operation that loses nothing, show that nothing was lost, and report it. Push EXACTLY ONE thing and nothing else: the observation commit to refs/live-observations/v2-095-release-live-dogfood. Never push a branch.",
    "ci/components.json AND Makefile ARE PROHIBITED. Measured: no import edge moves. internal/api/ownerdocs.go is inside the api component's existing root, internal/api already reaches internal/release through internal/application, and the two internal/contracts files are tests. ci/** is in all_on_change, so touching it moves all 23 component keys. A RED MANIFEST MEANS THE DESIGN WAS DEPARTED FROM.",
    "DO NOT WIRE THE RELEASE OBSERVER A SECOND TIME. cmd/** and internal/application/release_surface.go are PROHIBITED. The wiring is V2-091's; measure it, configure it, reuse it for the document routes, and if it is absent STOP AND ESCALATE rather than adding a second producer or a default root.",
    "DO NOT WIDEN THE DOMAIN OR THE STATE MACHINE. internal/domain/** is PROHIBITED on purpose: cap-human-input-request must be closed with zero domain change, so a domain edit is proof the design was departed from. internal/scheduler/** is prohibited for the same reason -- the closed reason set and the Decision record must be reused, not extended.",
    "DO NOT INVENT A STORE QUERY OR AN INDEX. internal/store/** and firestore.indexes.json are PROHIBITED on purpose: the repository filter must go through the EXISTING RequirementIDsForRepository port, and a need for a new query means the design was departed from. A firestore.indexes.json edit would also move the store-firestore key and the candidate digests.",
    "DO NOT HAND-WRITE THE DOCUMENT ALLOWLIST. internal/release/** is PROHIBITED on purpose: the served set must be the documentation-role member set of the assembled bundle, looked up as set membership, so it is derived from the tree rather than chosen. A path-joining handler is a traversal and is refused as a design.",
    "DO NOT MAKE ANY OWNER READ UNBOUNDED. One page of at most the requested page_size, one batch text read, one batch link read, exactly two new batch reads capped at MaxPageSize increment ids across the page with the existing Truncated flag set truthfully, and ZERO new reads in the queue summary. Record the measured read and write counts of one list request and one summary request against internal/quota's reservations and assert both stay inside them.",
    "DO NOT REPORT AN UNMEASURED VALUE AS A PLAUSIBLE ONE. A Requirement the scheduler did not rank carries no rank and says why; a zero StableSnapshot is reported as absent, not as a release; a bounded count says it is a lower bound. This is the idiom the owner console already states and the read models already follow.",
    "DETERMINISM IS ACCEPTANCE, NOT PREFERENCE. No fixed sleep, no wall-clock timer, no goroutine and no randomness in any new or modified test. Clocks injected. Every wait a bounded deadline poll reusing the exercise's existing poll idiom. A measured duration is an observation and never an asserted threshold. Run the non-live suite with -race.",
    "A DISAGREEMENT BETWEEN THE TWO RUNS ON ANY OF THE FIVE M5-OWNED CAPABILITY SUBTESTS IS A FAIL-CLOSED STOP. Escalate. Do not retry it away, do not overwrite an observation, do not delete one, do not quarantine one. G5 refuses a record that overwrote a varying result with a retry.",
    "RECORD FINDINGS; DO NOT SPAWN TASKS. A defect measured outside this boundary goes into row (3) of the A1 table as recorded-but-not-fixed, or is noted as already held by .agents/v2/DECLARATION-GAP.md. DO NOT PROPOSE A TASK ID. M5's remediation budget stands at 5 of 5; a sixth task triggers a judgement that the milestone is mis-cut, and section 12.3's widening-in-place is the intended out.",
    "DO NOT EDIT .agents/v2/task-state/**, any other task's packet, any other task's evidence record, any existing preflight record, any existing exercise-file-set artifact, .agents/v2/DECLARATION-GAP.md, .agents/v2/HANDOFF.md, .agents/v2/DEFERRED.md, docs/product/**, docs/architecture/**, docs/operations/**, contracts/schemas/**, contracts/fixtures/** or contracts/release-contract/foundation-capabilities.json. Documentation drift found in a prohibited file is a row (3) finding.",
    "EVERY LINE NUMBER, COUNT AND CLAIM IN THIS WORK ORDER WAS MEASURED BY THE DESIGNER AT HEAD a602b98 AND IS EXPECTED TO BE STALE. Re-measure, use the measured value, and report every disagreement in both records. Three disagreements with the designer's own brief are already known and confirmed by measurement, and are recorded here so they are not re-derived as new: provider_preflight.go admits the prefix provider-live- ONLY and never release-live- ; V2-084 is complete and merged into v2 rather than landing; and DECLARATION-GAP.md L92b's claim about domain.PriorityAssessment is false at this commit."
  ],
  "failure_stop": true,
  "tasks": [
    "V2-095"
  ],
  "priority": 95
}
