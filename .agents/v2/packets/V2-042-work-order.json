{
 "schema_version": "v1",
 "id": "wo-v2-042",
 "kind": "work-order",
 "created_at": "2026-08-24T11:38:36Z",
 "correlation_id": "v2-042-ledger-digest-allowlist",
 "problem_brief_ref": "pb-v2-042",
 "design_packet_ref": "dp-v2-042",
 "observable_outcome": "make check is green at the committed HEAD with the secret-scan allowlist covering exactly two shapes inside .agents/v2/ -- 64-hex reference digests and anchored ledger reference identifiers -- proven by four positive controls, and the change is recorded as evidence in the index.",
 "allowed_paths": [
  ".gitleaks.toml",
  "docs/architecture/validation.md",
  ".agents/v2/evidence/V2-042-ledger-digest-allowlist.json",
  ".agents/v2/evidence/index.json"
 ],
 "prohibited_paths": [
  ".agents/v2/task-state/**",
  ".agents/v2/packets/**",
  ".agents/v2/evidence/V2-025-contracts.json",
  ".agents/v2/evidence/V2-008-candidate-aggregate.json",
  ".agents/v2/evidence/V2-041-secrets-gate.json",
  ".agents/v2/evidence/V2-009-release-contract.json",
  ".agents/v2/evidence/V2-040-candidate-aggregate.json",
  "ci/**",
  "contracts/**",
  "internal/**",
  "cmd/**",
  "Makefile",
  ".github/**",
  "infra/**",
  "scripts/**"
 ],
 "contracts_changed": [],
 "acceptance_criteria": [
  "A1. .gitleaks.toml keeps extend.useDefault and keeps every allowlist AND-scoped. Allowlist one covers path .agents/v2/ with the anchored value pattern ^[a-f0-9]{64}$. Allowlist two covers path .agents/v2/ with the anchored value pattern ^(ev|wo|dp|pb|ts)-v2-[0-9]{3}(-[a-z0-9-]+)?$. No rule is removed or globally disabled, no third allowlist is added, and neither value pattern is unanchored.",
  "A2. Positive control one: a commit adding a 64-hex secret assignment outside .agents/v2/ is still reported by generic-api-key. Record the finding count.",
  "A3. Positive control two: a commit adding a high-entropy secret that matches neither allowlisted shape inside .agents/v2/ is still reported. Record the finding count.",
  "A4. Positive control three: a commit adding a value shaped like a ledger reference identifier outside .agents/v2/ is still reported. Record the finding count.",
  "A5. Positive control four: with every probe commit removed, devbox run --pure -- gitleaks git --no-banner --redact reports no leaks found at the committed HEAD.",
  "A6. devbox run --pure -- make check passes every target including secrets, run after committing so the scan reads committed history.",
  "A7. docs/architecture/validation.md records that the allowlist covers two ledger shapes -- the reference digests and the reference identifiers mandated by the canonical schemas -- and states the measured reason the identifier shape needs allowing: generic-api-key captures the value following a keyword, and the evidence id ev-v2-041-secrets-gate contains the keyword substring secret, so an adjacent reference id in a refs array is captured as if it were the secret.",
  "A8. A new evidence artifact .agents/v2/evidence/V2-042-ledger-digest-allowlist.json (id ev-v2-042-ledger-digest-allowlist, task_id V2-042, component environment) records all four positive controls with their measured counts and the corrected diagnosis, and is registered in .agents/v2/evidence/index.json with a sha256-matching evidence_hash; devbox run --pure -- go test ./internal/contracts is green.",
  "A9. No file outside allowed_paths changes. In particular no file under .agents/v2/task-state/ is edited to work around the finding."
 ],
 "validation": {
  "argv": [
   "devbox",
   "run",
   "--pure",
   "--",
   "make",
   "check"
  ],
  "working_directory": ".",
  "timeout_seconds": 2400
 },
 "constraints": [
  "Work only inside the assigned task worktree.",
  "Never use git reset, git checkout --, git stash or git commit --amend; probe commits are removed by the coordinator or by reverting the specific file with a fresh edit.",
  "Do not report a control as passed unless it was actually executed."
 ],
 "failure_stop": true,
 "tasks": [
  "V2-042"
 ],
 "priority": 95
}
