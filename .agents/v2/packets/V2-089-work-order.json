{
 "schema_version": "v1",
 "id": "wo-v2-089",
 "kind": "work-order",
 "created_at": "2026-08-26T15:40:00Z",
 "correlation_id": "v2-089-claim-requires-a-claimable-requirement",
 "problem_brief_ref": "pb-v2-089",
 "design_packet_ref": "dp-v2-089",
 "observable_outcome": "Claiming refuses when the Requirement the Increment belongs to is not in a state that admits work, and every fixture that passes today because claiming never asked is moved into a state the product describes. Measured at 9bf0d8c: claiming ALREADY asks, for exactly one status -- internal/application/service.go:1139-1142 refuses when the parent is needs-input, through requirementAwaitsHumanInput (internal/application/human_input.go:683-695), and internal/api/api.go:1253-1259 maps that error to 409. This task widens that one question to the complement of the admitting set, in the same transaction, at the same line, with no new read and no new port. The set is FOUR of eleven statuses -- ready, active, waiting, recovering -- and it is DERIVED: docs/product/definition.md:111 requires a worker to claim an executable Requirement, docs/architecture/domain-model.md:266 defines active as one or more Increments in progress, and internal/domain/model.go:485-489 already admits domain.RequirementStart from exactly ready, recovering and waiting. It is declared ONCE in a new file internal/application/claimable.go and cross-checked against the domain over the whole status axis. The migration was measured by EXECUTION: with the guard in place, 45 top-level tests fail in 17 files across 4 packages, a 46th in an 18th file and a 5th package under the real Firestore emulator, 64 claim attempts are refused over 62 Increments, and the offending parent status was captured in 60 of 60 observable cases and nothing else in any. One production file moves with them: internal/runner/orchestrator.go's RunFakeJourney claims while the parent stays captured, and must reach ready through Service.StartFraming and V2-084's Service.CompleteFraming -- the one place the admitting state is proved reachable rather than seeded. Nothing changes for a claim the state admits, asserted for all four admitting statuses where the lease, the execution and the fencing token are real.",
 "allowed_paths": [
  "internal/application/claimable.go",
  "internal/application/claimable_test.go",
  "internal/application/service.go",
  "internal/application/service_test.go",
  "internal/application/allocation_test.go",
  "internal/application/provider_registry_test.go",
  "internal/application/publication_test.go",
  "internal/application/repository_test.go",
  "internal/application/runner_version_test.go",
  "internal/application/stop_matrix_test.go",
  "internal/application/framing_test.go",
  "internal/api/api.go",
  "internal/api/api_test.go",
  "internal/api/live_dogfood_test.go",
  "internal/reconciler/fencing_journey_test.go",
  "internal/reconciler/stop_journey_test.go",
  "internal/runner/orchestrator.go",
  "internal/runner/orchestrator_test.go",
  "internal/runner/control_agent_test.go",
  "internal/runner/crash_test.go",
  "internal/runner/forgepublish_test.go",
  "internal/runner/journey_test.go",
  "internal/runner/lease_test.go",
  "internal/runner/secret_broker_test.go",
  "internal/runner/provider_live_test.go",
  "internal/store/firestore/store_test.go",
  ".agents/v2/evidence/V2-089-application.json",
  ".agents/v2/evidence/V2-089-api.json",
  ".agents/v2/evidence/V2-089-runner.json",
  ".agents/v2/evidence/V2-089-reconciler.json",
  ".agents/v2/evidence/V2-089-store-firestore.json",
  ".agents/v2/evidence/index.json"
 ],
 "prohibited_paths": [
  "ci/components.json",
  "ci/**",
  "Makefile",
  "go.mod",
  "go.sum",
  "devbox.json",
  "devbox.lock",
  ".gitleaks.toml",
  "internal/domain/**",
  "internal/application/human_input.go",
  "internal/application/human_input_test.go",
  "internal/application/readmodels.go",
  "internal/application/readmodels_test.go",
  "internal/application/ports.go",
  "internal/application/caller.go",
  "internal/application/errors.go",
  "internal/application/idempotency.go",
  "internal/application/outbox.go",
  "internal/application/allocation.go",
  "internal/application/publication.go",
  "internal/application/repository.go",
  "internal/application/provider_registry.go",
  "internal/application/release_surface.go",
  "internal/application/release_surface_test.go",
  "internal/application/runner_version.go",
  "internal/application/source_guard_test.go",
  "internal/application/requested_by_test.go",
  "internal/application/provider_compatibility_test.go",
  "internal/api/auth.go",
  "internal/api/auth_test.go",
  "internal/api/repositories.go",
  "internal/api/repositories_test.go",
  "internal/api/operator_record.go",
  "internal/api/operator_record_test.go",
  "internal/api/live_local_test.go",
  "internal/runner/journal_test.go",
  "internal/runner/source_guard_test.go",
  "internal/runner/stop_test.go",
  "internal/runner/supervisor.go",
  "internal/runner/supervisor_test.go",
  "internal/reconciler/orphan_test.go",
  "internal/reconciler/reconciler.go",
  "internal/reconciler/control_verifier.go",
  "internal/reconciler/verification.go",
  "internal/store/memory/**",
  "internal/store/firestore/store.go",
  "internal/store/firestore/source_guard_test.go",
  "internal/store/firestore/envelope_test.go",
  "internal/store/firestore/quota_integration_test.go",
  "internal/quota/**",
  "internal/web/**",
  "internal/provider/**",
  "internal/scheduler/**",
  "internal/release/**",
  "internal/update/**",
  "internal/legacyimport/**",
  "internal/contracts/**",
  "internal/ci/**",
  "internal/infra/**",
  "contracts/**",
  "firestore.indexes.json",
  "firebase.json",
  "cmd/**",
  "infra/**",
  "scripts/**",
  ".github/**",
  "docs/**",
  "README.md",
  "AGENTS.md",
  ".agents/v2/task-state/**",
  ".agents/v2/packets/**",
  ".agents/v2/provider-preflight/**",
  ".agents/v2/evidence/artifacts/**",
  ".agents/v2/evidence/V2-022-release-live-dogfood.json"
 ],
 "contracts_changed": [],
 "acceptance_criteria": [
  "A1. SCOPE BOUNDARY, and it is built FIRST. Write the five-row table into ALL FIVE evidence records BEFORE any source file is edited, and record the order in which you did it. V2-087 produced perfect content for this table but built it afterwards and had to report it as its one unmet item; V2-088 did it in the right order and its first commit is the records. (1) What this task makes TRUE: Service.Claim refuses an Increment whose parent Requirement is not in ready, active, waiting or recovering; that refusal is a 409 conflict at the transport; it succeeds unchanged from all four admitting statuses; every fixture that passed because the question was not asked now names the status it establishes; V2-090's pause actually stops new claims, which is docs/product/definition.md:143's '新しい仕事のclaimを止める'; and V2-084's F1 is closed for the cancelled case. (2) What it deliberately does NOT do: it does not touch internal/domain, does not add a status or a command kind, does not change Service.Renew, does not revoke or expire a lease already held, does not terminate an Execution, does not refuse Plan or Prepare, does not add a port, adds no openapi path or response code, adds no product text -- docs/** is prohibited -- and changes nextAction by not one byte. (3) What it RECORDS but does not fix: an Increment can still be planned and prepared under a Requirement that admits no work, because Service.Plan writes the parent without a domain command; internal/application/readmodels.go:309-332's nextAction still reports 'run next increment' at :324-325 for a ready Increment whose parent admits nothing, so the read model advertises an action the claim refuses; a claim from waiting or recovering succeeds and leaves the parent there, so the aggregate still contradicts domain-model.md:266; and the two cells where docs/architecture/domain-model.md:268 and :271 list `active` as a target that internal/domain/model.go:486 does not admit. (4) What only a LATER task can establish: that a refusal stops work already in flight, which needs the propagation and the stop-completion observation docs/product/definition.md:164 defines; that waiting and recovering are journey-reachable, which needs issuers for domain.RequirementWait and domain.RequirementRecover; and that the live dogfood journey and the live provider journey actually pass, since neither may be executed here (A22). (5) That this task passes NO release gate on its own.",
  "A2. BASELINE, measured by you, and every number below is expected to be STALE. State it plainly in the evidence: each of the last fourteen tasks found at least one disagreement with its Work Order's numbers and V2-087 found five. Re-measure every one with `devbox run --pure -- go test -list '.*' ./<pkg>` and with fresh greps, USE the measured value, and report every disagreement rather than the value written here. Top-level test counts read by the coordinator at 9bf0d8c with `grep -hc '^func Test' <pkg>/*_test.go | paste -sd+ | bc`, for you to contradict: internal/domain 54, internal/application 187, internal/api 63, internal/contracts 33, internal/reconciler 16, internal/runner 118, internal/store/memory 16, internal/store/firestore 36, internal/ci 11. Line counts: internal/application/service.go 1729, internal/application/human_input.go 696, internal/application/service_test.go 1048, internal/application/stop_matrix_test.go 373, internal/api/api.go 1351, internal/api/api_test.go 4660, internal/domain/model.go 672, internal/runner/orchestrator.go 273, internal/runner/orchestrator_test.go 82, contracts/openapi/openapi-v1.yaml 1044. Record the before and after top-level test list for internal/application, internal/api, internal/runner, internal/reconciler and internal/store/firestore; the after list must be the before list plus exactly the tests this Work Order adds, with no existing test deleted and none renamed. One number is already known to disagree with a packet this task builds on -- V2-084's four-read-transactions-per-budget-day, invalidated by V2-087 (A23) -- and finding it stale is the expected outcome.",
  "A3. THE MEASURED PREMISE, re-confirmed and recorded before any edit, with the argv for each. (a) Claiming already asks, once: quote internal/application/service.go:1130-1142 verbatim with its measured line numbers, and record that requirementAwaitsHumanInput is declared at internal/application/human_input.go:683-695, that it returns false for an empty RequirementID and false for a record that is not found, and that its ONLY other caller is Service.Renew at internal/application/service.go:284-288 (`grep -rn 'requirementAwaitsHumanInput' --include='*.go' .` returns 4 lines). (b) internal/domain/model.go:485-489 admits domain.RequirementStart from exactly RequirementReady, RequirementRecovering and RequirementWaiting -- quote the branch and its allowed(...) call at :486 verbatim. (c) internal/api/api.go:1253-1259 maps application.ErrAwaitingHumanInput to 409 conflict -- quote it, including the comment, because your one added disjunct inherits its reasoning. (d) Non-test Claim callers number THREE: internal/api/api.go:1070, internal/runner/orchestrator.go:57 and the internal/application/service.go:1276 alias (`grep -rn '\\.Claim(\\|\\.ClaimIncrement(' --include='*.go' . | grep -v _test.go`). (e) contracts/openapi/openapi-v1.yaml:144-149 already declares '409' among claims:acquire's responses. If (a) or (b) is no longer true -- if some other task widened the guard or the start branch first -- STOP and escalate rather than adding a second mechanism.",
  "A4. THE PRODUCT DERIVATION, one citation per admitted and per refused status, and a status without one is the defect. Record a table with one row per RequirementStatus -- eleven rows -- each carrying the status, whether it admits a claim, the file, the measured line number and the quoted text. The coordinator's measurements, for you to contradict: docs/product/definition.md:111 '3. 利用可能なworkerが、実行可能なRequirementを排他的にclaimする。' is the requirement that claiming asks; docs/architecture/domain-model.md:266 '`active` | 1つ以上のIncrementが進行中' is what a claim produces; :265 defines ready as '優先順位評価済みで実行可能' and lists `active` among its targets; :267 does the same for waiting and :270 for recovering; :263 defines captured as '原文を失わず登録済み' whose only non-terminal target is framing, and :264 defines framing as '分析中'; docs/product/user-facing-spec.md:270 shows what the owner is told about exactly those two, '`queued` | 永続化済みで、実行可能になるのを待っている' -- persisted and WAITING to become executable, which is the citation that makes refusing captured and framing a product requirement rather than a preference. Then state the derived conclusion explicitly: the admitting set is {s : domain.DecideRequirement admits RequirementStart from s} union {RequirementActive}, which is exactly {ready, active, waiting, recovering}, four of eleven. If your reading differs, STOP and escalate; do not implement a different set and do not implement none.",
  "A5. THE TWO CELLS WHERE THE TABLE IS WIDER THAN THE DOMAIN, recorded and NOT repaired, and the counterexample that settles them. Measure and record that docs/architecture/domain-model.md:268 lists `active` among needs-input's targets and :271 lists `active` among evaluating's, while internal/domain/model.go:486 admits the start command from neither. Then record why the table is not the authority: needs-input is a shipped counterexample. docs/product/definition.md:143 defines pause claim as '新しい仕事のclaimを止める', V2-065 built exactly that at internal/application/service.go:1139-1142, and internal/application/human_input_test.go:534-587's biconditional asserts in BOTH directions that the refusal and the displayed ScopeNewClaimsForThisRequirement agree -- so the product demonstrably does not read 'the table lists active as a target' as 'a claim is admitted'. evaluating is refused for the same reason and for a second measured one: outside model.go:486's admitted set, a claim from evaluating would put an Increment 進行中 under a Requirement the domain forbids from becoming active, so V2-084's parent transition would do nothing and the aggregate would contradict domain-model.md:266. Do NOT narrow internal/domain and do NOT edit docs/**: LOG the two-cell gap as a named finding for the tech_lead and assert it nowhere, for the reason internal/application/framing_test.go:562-564 already gives. If you conclude the gap should be closed, escalate; do not close it here.",
  "A6. THE GUARD, and the ONE FILE it is declared in. Create internal/application/claimable.go carrying exactly three things: application.ErrRequirementNotClaimable, an unexported requirementStatusAdmitsClaim(domain.RequirementStatus) bool that is ONE switch over exactly domain.RequirementReady, domain.RequirementActive, domain.RequirementWaiting and domain.RequirementRecovering, and an unexported reader that takes the UnitOfWork and the Increment, reads the parent and reports the predicate's answer. The reader REFUSES -- returns false -- when the Increment's RequirementID is empty and when the record is not found (A11). Then add to Service.Claim, in internal/application/service.go, immediately AFTER the existing needs-input guard at :1139-1142 and before the active-lease check, a four-line block that calls the reader, propagates a store error unchanged, and otherwise returns ErrRequirementNotClaimable wrapped with the increment id in exactly the shape service.go:1142 already uses. Do NOT widen requirementAwaitsHumanInput and do NOT reuse ErrAwaitingHumanInput: internal/application/human_input.go and human_input_test.go are PROHIBITED. Write a comment on the switch naming domain-model.md's four cells and stating that the set is the domain's own RequirementStart sources plus active, so a reader can check the derivation without leaving the file. The switch is the ONLY place the four statuses are written out; if you find yourself writing them a second time anywhere, the design was departed from.",
  "A7. THE DOMAIN EQUIVALENCE, which is what makes widening the set a build failure rather than a decision. In a new file internal/application/claimable_test.go, derive the COMPLETE RequirementStatus axis by parsing internal/domain/model.go with go/ast -- the idiom internal/application/allocation_test.go already uses, which declares allocDomainDir = \"../domain\" at :624 and parses that directory at :309, and which internal/application/framing_test.go:159-211 uses for a source-level count. Assert that the axis has at least eleven members and fail outright on a zero-member scan, so a mis-written matcher cannot pass vacuously, and verify the matcher against a synthetic known-positive first exactly as framing_test.go:155-158 explains. Then assert, for EVERY status on that axis, that requirementStatusAdmitsClaim(s) is true IF AND ONLY IF domain.DecideRequirement(domain.Requirement{ID, Status: s, Version: v}, domain.RequirementCommand{Kind: domain.RequirementStart, Actor, At, ExpectedVersion: v}) returns a nil error OR s == domain.RequirementActive. internal/domain/** is PROHIBITED, so the only way to make `captured` admitted is to widen internal/domain/model.go:486 in a file you may not open: a disagreement here is a STOP-AND-ESCALATE, never a switch case to add. Record the eleven answers and record which of them came out admitted.",
  "A8. THE ELEVEN CELLS OVER THE REAL SERVICE, which is how non_goals item 3 is measured rather than promised. In internal/application/claimable_test.go, over the real Service and a real memory store, for every status that can be SEEDED: seed a Requirement to that status, Plan and Prepare an Increment under it, and claim it as a runner. Assert SUCCESS for each of the FOUR admitting statuses -- and for each success assert that claiming still does exactly what it did before: a lease is issued, an Execution is created, the fencing token is non-zero, the Increment's version advances by one, the increment.claimed event is recorded and the claim-issued outbox item is staged. Assert REFUSAL for each of the other seven, with needs-input refused by errors.Is(err, application.ErrAwaitingHumanInput) and the other six by errors.Is(err, application.ErrRequirementNotClaimable), and for each refusal assert the Increment, the Requirement, the lease set and the execution set are byte-unchanged and that no idempotency record was written -- prove the last by replaying the same request_id after seeding the parent into an admitting status and asserting it executes for real. One cell needs care and it is measured: seeding `completed` requires a StableSnapshot, because internal/domain/model.go:324-328 makes a completed Requirement without ReleaseID, BundleDigest and EvidenceDigest fail Validate and internal/application/human_input_test.go:52-75's seedRequirementStatus calls domain.Validate before saving. Build that snapshot by concatenating a prefix with a suffix and record it; if any cell cannot be seeded, record it with its measured reason and status skipped, never as a pass.",
  "A9. THE TWO UNKNOWN-PARENT CELLS, asserted because no existing fixture reaches either. Measured: the permissive and the strict variants of this guard produce BYTE-IDENTICAL failing sets -- 45 tests and 60 refusals each, with `comm` over the two sorted lists empty on both sides -- so not one fixture in the repository claims an Increment whose parent link is empty or whose parent is absent, and the choice is invisible to the suite. Assert both: an Increment whose RequirementID is empty is REFUSED with ErrRequirementNotClaimable, and an Increment whose RequirementID names a Requirement not in the store is REFUSED with ErrRequirementNotClaimable. Record the reason as a citation rather than a preference: docs/product/definition.md:111's 実行可能 cannot be established for a record that does not exist. Also record the measurement that makes this a second helper rather than a widening: requirementAwaitsHumanInput returns FALSE for both cases (internal/application/human_input.go:685-687 and :691-693) and is correct to, because it asks a narrower question -- so the two questions have opposite correct answers on the unknown case and cannot share one helper.",
  "A10. THE TRANSPORT, and the measurement that makes it mandatory. Add ONE disjunct for application.ErrRequirementNotClaimable to the EXISTING 409-conflict branch at internal/api/api.go:1253-1259 -- the branch that already carries ErrAwaitingHumanInput -- and extend its comment to say that the parent Requirement is not in a state that admits work and that nothing the caller can rewrite makes the claim issuable. Do NOT add a new status code, a new error code, a new branch or an openapi change: contracts/** is PROHIBITED and contracts/openapi/openapi-v1.yaml:149 already declares 409 for claims:acquire, so contracts_changed is empty and the contracts key does not move. Record the measurement that proves this edit is required rather than tidy: with the guard in place and internal/api/api.go unedited, the coordinator measured `internal/api/api_test.go:1176: claims:acquire: status 500` with body `{\"error\":\"internal_error\"}` -- an unclassified refusal, which additionally fires V2-088's operator record for an unclassified error. Re-measure it yourself before the fix and record both the before and the after. Then assert over the composed handler that a claim whose parent admits no work returns 409 with code `conflict`, and that the needs-input case still returns 409 with code `conflict` and the same body it returned before.",
  "A11. THE PRECEDENCE, asserted in both directions so it cannot drift. The new guard sits before the canonical-target resolution and before domain.Permit, so when a Requirement admits no work AND a stop is in force the caller sees ErrRequirementNotClaimable. Assert exactly that: with a captured parent and a matching Control Intent in emergency-stop, the error is ErrRequirementNotClaimable and not domain.ErrControlDenied; and with a parent in an admitting status and the same Intent, the error IS domain.ErrControlDenied. Assert the other precedence too: with a parent in needs-input, the error is ErrAwaitingHumanInput and NOT ErrRequirementNotClaimable, so the shipped V2-065 refusal keeps its identity, its message and its position. internal/application/human_input_test.go is PROHIBITED, and its TestTheDisplayedStoppedScopeAndTheClaimRefusalAgree (measured at :534) must stay byte-unchanged and green: it is a biconditional whose refusal arm t.Fatalf's on 'Claim failed for an unrelated reason', so if the new refusal leaks into a needs-input fixture or is ordered ahead of the old guard it fails loudly and the ONLY remedy is to fix the new guard.",
  "A12. SERVICE.RENEW IS NOT TOUCHED, and the reason is recorded as a measurement rather than a scope note. Record that internal/application/service.go:284-288 keeps asking only the needs-input question, quote the comment at service.go:1131-1138 which states that the refusal is closed on the ISSUING side because no domain transition can revoke or release an active lease early, and cite docs/product/definition.md:143 for the product half: pause claim stops 新しい仕事のclaim and '実行中の仕事が進める「定義済みの境界」はcheckpointであり'. Assert permanently that a lease already held is STILL renewable after its parent Requirement leaves the admitting set -- seed the parent to paused or evaluating after a successful claim, then renew, and assert the renewal succeeds and the lease keeps its ExpiresAt. That assertion is the boundary of this task stated as a test rather than as a sentence. Do not add a Renew refusal and do not expire a lease.",
  "A13. THE MIGRATION TABLE, and it is the definition of done for non_goals item 1. Record ONE ROW PER MIGRATED FIXTURE, and a row carries: the file, the measured line number of the seeding site, the top-level test names it feeds, the status the fixture now establishes, the MECHANISM used to establish it (the product's own commands, or the named per-package seeding helper), and the product citation from A4's table that makes that status admit work. A row whose status is not one of the four is the defect. A row whose mechanism is a store write and whose test is a journey must say why the product path was not used. The coordinator's measured inventory, for you to contradict: 45 failing top-level tests in 17 files across internal/application, internal/api, internal/reconciler and internal/runner, plus TestFirestoreApplicationAtomicityConflictAndControl (internal/store/firestore/store_test.go:179) as a 46th in an 18th file and a 5th package under the real emulator; 64 refused claim attempts over 62 distinct Increments; and `parentstatus=captured` in 60 of 60 observable cases with zero in any other status and zero with a missing or empty parent. Re-measure the inventory yourself the way the coordinator did -- implement the guard, run the whole suite, and enumerate the refusals rather than only the failures -- and report every disagreement.",
  "A14. THE MECHANISM, capped at one helper per package, and every call site NAMES its status. The idiom already exists and is already parameterised: internal/application/human_input_test.go:52 declares seedRequirementStatus(t, st, id, status), which reads the Requirement, sets the status, increments the Version, calls domain.Validate and saves, and :107's buildHumanInputFixture takes the target status as its third argument and is called as buildHumanInputFixture(t, \"iff-waiting\", domain.RequirementActive) at :562. internal/api/api_test.go has it twice, seedActiveRequirement at :2382-2384 and pinMakeActive at :3670. Reuse the existing helper in internal/application (package application_test) and add AT MOST ONE new equivalent in each of internal/reconciler, internal/runner and internal/store/firestore, which have none today. Every call site passes the status as a domain constant literal -- never a variable, never a string, never a default -- so the chosen state is readable at the fixture. Record, and do not change, the measurement that internal/api/api_test.go's pin table needs NO migration: pinMakeActive already puts pin-loop into `active`, so pin row 'claim-accepted' at api_test.go:4047 still returns 200 with the guard in place.",
  "A15. THE FOUR COUNTING TRIPWIRES, each with its measured before-value, because a prohibition on weakening is only enforceable if the cheat has a signature. (i) `grep -rn 'Status = domain.Requirement' --include='*_test.go' .` returns exactly FOUR lines at 9bf0d8c, all in internal/api/api_test.go (three RequirementActive, one RequirementFraming). Report the after-count, and every increase must be one row of A13's table naming one of the four statuses. (ii) Per-file claim call-site counts must not DECREASE. Measured: `grep -c '\\.Claim(\\|\\.ClaimIncrement(' <file>` totals 46 over 18 files -- service_test.go 15, provider_live_test.go 8, human_input_test.go 3, publication_test.go 3, fencing_journey_test.go 2, stop_journey_test.go 2, stop_matrix_test.go 2, and 1 each in allocation_test.go, provider_registry_test.go, repository_test.go, runner_version_test.go, control_agent_test.go, crash_test.go, forgepublish_test.go, journey_test.go, lease_test.go, secret_broker_test.go and internal/store/firestore/store_test.go -- and `grep -c 'claims:acquire'` returns 10 in internal/api/api_test.go, 9 in internal/api/live_dogfood_test.go and 1 in internal/application/runner_version_test.go. (iii) No test may be deleted or renamed and the per-package top-level counts of A2 must not decrease. (iv) Non-test Claim callers stay at THREE. Report all four before-and-after, and treat any decrease in (ii) or (iii) as a stop-and-escalate.",
  "A16. THE SECOND WAVE, named in advance because it does not look like this task's problem. A seed or a framing pair increments the Requirement's Version, so every downstream ExpectedRequirementVersion in the same fixture goes stale. The coordinator measured it: inserting a ready-seed before internal/runner/lease_test.go:62's Plan turned the refusal into `lease_test.go:94: stale domain version` on all three TestLeaseKeeper tests, and threading the post-seed version into that Plan's ExpectedRequirementVersion turned the package green. Thread the version. Do NOT drop ExpectedRequirementVersion, do NOT pass zero, and do NOT move the seed after the Plan: each of those deletes a real assertion and each will look like tidying at the time. After V2-084 there is a THIRD wave -- Claim itself issues the ready-to-active transition, so the parent's version moves again at the claim -- and any fixture that afterwards issues a Requirement command or asserts a version must thread that too. Record every version thread as a row of A13's table and record the count of them.",
  "A17. THE PRODUCTION FILE, which is the reachability proof. internal/runner/orchestrator.go's RunFakeJourney (measured at :35-116) drives Capture at :41, Plan at :45, Prepare at :49 and Claim at :57 while the parent stays at captured, and TestOrchestratorFakeJourney fails with the guard in place -- re-measure and record it. It already holds an owner context at :39, so add Service.StartFraming and V2-084's Service.CompleteFraming between the Capture and the Plan, and thread the resulting version into the Plan's ExpectedRequirementVersion. This is the ONE place in this task where the admitting state is established by the product's own commands with no store write, and internal/runner/orchestrator_test.go must ASSERT it: the journey's Requirement reads `ready` before the claim and `active` after it. Record that RunFakeJourney has exactly one caller and it is a test (`grep -rn 'RunFakeJourney' --include='*.go' .` returns three lines: the declaration and orchestrator_test.go:56 and :63), so this production edit costs no running behaviour and buys the proof that the migration target is a state the product produces. If Service.CompleteFraming does not exist at your HEAD, V2-084 has not landed: STOP and escalate rather than seeding the orchestrator with a store write.",
  "A18. THE CLOSED-SET GUARD THAT MUST NOT MOVE. internal/application/stop_matrix_test.go's TestStopModeByKindMatrix fails with the guard in place -- measured -- and its fixture must be migrated so the matrix measures the permit again. wantStopMatrixCells stays EXACTLY 56 (measured at stop_matrix_test.go:20) and wantStopModeAllowedTotal stays EXACTLY 12 (measured at :63), because docs/product/definition.md:156-159 cites those counts by name and docs/** is prohibited. The ONLY permitted change in that file is the fixture's parent status and the version threading A16 requires; every mode row, every kind column and both constants stay byte-unchanged. A moved constant is a STOP-AND-ESCALATE. Likewise internal/application/framing_test.go is writable for exactly ONE purpose: if V2-084 left its A6 measurement -- a claim under a captured parent recorded as succeeding -- as an executable check that now fails, invert it to assert the refusal and record that inversion as this task's outcome rather than as a weakening. Re-measure whether such a check exists; if it does not, leave the file byte-unchanged.",
  "A19. WHAT IS NOT WEAKENED, measured rather than promised. Assert that every pre-existing top-level test in internal/application, internal/api, internal/runner, internal/reconciler, internal/store/memory, internal/store/firestore, internal/domain and internal/contracts keeps its NAME and keeps passing, and report before and after counts per package. In particular, and each for a stated reason: internal/application/human_input_test.go is PROHIBITED and TestTheDisplayedStoppedScopeAndTheClaimRefusalAgree, TestClaimAndRenewRefuseWhileTheRequirementWaitsForHumanInput and every other test in it stay byte-unchanged and green; internal/application/source_guard_test.go is prohibited, so the new file adds no import that would need an allowlist entry, and TestApplicationHasNoProviderProbePath stays green; internal/application/readmodels.go and readmodels_test.go are prohibited, so nextAction stays byte-unchanged. Run `git diff` over every file in allowed_paths and record that each change is one this Work Order names.",
  "A20. THE NEW REFUSAL HAS EXACTLY ONE ISSUER AND EXACTLY ONE DEFINITION, asserted by a source scan. Using the go/ast idiom internal/application/framing_test.go:159-211 already uses, assert that application.ErrRequirementNotClaimable is RETURNED from exactly one function in internal/application and that requirementStatusAdmitsClaim is declared exactly once and called from exactly the reader plus the tests. Assert that the four status constants appear together in exactly one switch in non-test code in internal/application. This is what makes 'declared once, read twice' a build failure rather than a claim: a second list anywhere fails this scan. Fail outright on a zero-file scan and verify the matcher against a synthetic known-positive first.",
  "A21. THE FULL JOURNEY OVER HTTP, in one test whose name says what it proves, in internal/api/api_test.go over the composed handler. As the owner: capture; assert the detail reports captured; Plan and Prepare an Increment; claim it as a RUNNER caller and assert 409 with code `conflict`, and assert the Increment, the lease set and the execution set are unchanged; then POST :start-framing and :complete-framing (V2-084's route) and assert ready; claim the SAME Increment again with a NEW request_id and assert 200, and assert the parent is now active (V2-084's edge); Plan, Prepare and claim a SECOND Increment and assert 200 with the parent still active. Then migrate driveRunnerRoutes (measured at internal/api/api_test.go:1093-1122, whose claim is at :1122 and whose failure the coordinator measured at :1176) so its six runner-facing operations succeed again, and record that its two callers are TestAnAbsurdRunnerVersionReportChangesNoStatusCode at :1166-1167. Assert the Requirement count never changes after the first capture. The detail route is read several times, so advance the injected clock between reads -- see A23.",
  "A22. THE THREE FIXTURE SETS YOU CANNOT EXECUTE, each handled differently and each recorded honestly. (i) internal/store/firestore/store_test.go:281's claim is inside TestFirestoreApplicationAtomicityConflictAndControl (measured at :179) and is skipped without FIRESTORE_EMULATOR_HOST (:43-44). The coordinator started the emulator for real and measured it FAIL with `parentstatus=[captured]` at store_test.go:295. Migrate it and RUN it: `devbox run --pure -- sh -c 'scripts/firestore-emulator.sh go test -race -count=1 -run TestFirestoreApplicationAtomicityConflictAndControl ./internal/store/firestore'`, and also `devbox run --pure -- make component-store-firestore`, which starts the emulator itself. It claims from two goroutines at :278-284: that existing goroutine stays and the migration adds none. A bare `go test ./internal/store/firestore` prints ok while producing 26 --- SKIP lines and measuring nothing, and a skipped test is never a pass (gate rule G7). (ii) internal/runner/provider_live_test.go has EIGHT claim call sites (:298, :411, :504, :711, :809, :886, :992, :1074) fed by ONE Capture at :237, behind the live-provider gate at :49-51. NO Provider CLI invocation is authorised. Migrate that single seeding site by inspection, prove it compiles with `go vet ./internal/runner` and `gofmt -l .`, account for all eight downstream sites in A13's table, and record the file as UNEXECUTED with status skipped. (iii) internal/api/live_dogfood_test.go's four claim journeys are fed by ONE helper, planAndPrepare at :1268-1270, called at :1296, :1469, :1496 and :1882, from Requirements captured through fx.capture at :1028-1040 and left in captured. Migrate INSIDE that one helper, additively, changing no assertion and no other line, and record it as UNEXECUTED with status skipped. Leaving (ii) or (iii) unmigrated leaves a fixture that relies on claiming not asking, which is what this task exists to remove; recording either as a pass is forbidden.",
  "A23. QUOTA AND DETERMINISM. Quota: the migration adds one to three extra mutations per fixture. quota.MutationUsage is {Reads: 32, Writes: 16} (measured at internal/quota/quota.go:62) and is reserved BEFORE the callback can stage anything, so measure each migrated fixture's mutation count and record that it fits; quota.DefaultBudget is {Reads: 25000, Writes: 10000, Deletes: 10000} (quota.go:46) and quota.ReadTransactionUsage is {Reads: 6001, Writes: 1} (quota.go:56). V2-084's four-read-transactions-per-budget-day is STALE: V2-087 landed the in-memory true-up at internal/store/memory/store.go:353. Re-measure the wall by EXECUTION, report both the stale and the measured value, and if a budget binds, advance the injected clock between reads. Do NOT weaken an assertion, drop a read, merge two assertions into one read or raise a budget: internal/quota and internal/store/memory are prohibited. Determinism: no NEW fixed sleep, no NEW wall-clock timer, no NEW goroutine and no source of randomness in any new or modified test; existing goroutines in a fixture being migrated stay as they are and none is added; every clock is injected; every wait is a bounded deadline poll reusing an idiom already in the package; a measured duration is recorded as an observation and never asserted as a threshold. Run internal/application, internal/api, internal/runner, internal/reconciler and the emulator-backed internal/store/firestore run with -race.",
  "A24. ROLLBACK, recorded as a property with its surviving artefacts named. State that rollback is deleting internal/application/claimable.go and internal/application/claimable_test.go and reverting: the four-line guard in Service.Claim, one disjunct and one comment in internal/api/api.go, two calls and one version thread in internal/runner/orchestrator.go, and the fixture migration in the twenty test files this Work Order names. Nothing else must be undone: this change adds no port, no domain edge, no status, no command kind, no document kind, no index, no collection, no environment variable, no outbox item, no event type, no response field, no openapi path and no import edge in any of the five components. Name what SURVIVES a revert: the migrated fixtures, which after a revert seed a Requirement to `ready` or `active` and then claim -- and which still pass, because claiming from an admitting status is exactly what it did before the guard existed. Verify that claim by measurement rather than by reasoning: revert the guard alone in a scratch copy, run the five packages, and record that they stay green. That is what makes this change revertible without a fixture rollback.",
  "A25. EVIDENCE, and the ledger test is the definition of done. Create exactly FIVE records: .agents/v2/evidence/V2-089-application.json (id ev-v2-089-application, component application), V2-089-api.json (id ev-v2-089-api, component api), V2-089-runner.json (id ev-v2-089-runner, component runner), V2-089-reconciler.json (id ev-v2-089-reconciler, component reconciler) and V2-089-store-firestore.json (id ev-v2-089-store-firestore, component store-firestore). Every record carries task_id V2-089 -- the PRODUCING task, never a consuming one -- and correlation_id v2-089-claim-requires-a-claimable-requirement. A13's migration table and A15's four tripwire counts go into the record of the component each migrated file belongs to, so the table is not filed in one place and lost. Every acceptance item must be traceable to a named check recording the argv actually executed, the working directory, the timeout and a summary stating the measured result. Record the execution environment for every run: `uname -srm`, the go version as resolved inside devbox run --pure, the devbox version, the worktree path, the branch and the base commit. A skipped check is recorded with status skipped and its reason and is NEVER counted as a pass (gate rule G7); the provider-live and live-dogfood migrations of A22 are recorded that way. Do not write 'capability exercised' or 'preview journey passed' and do not cite any capability id. Register all five with appended entries in .agents/v2/evidence/index.json -- entries are compact one-line JSON appended to the existing entries array, and no other task's entry may be touched -- whose evidence_hash is the sha256 of each artifact's final committed bytes. Completion is `devbox run --pure -- go test -count=1 ./internal/contracts` green, including the ledger and index checks. Any digest written in prose is 64 or 40 hex characters followed by at most one punctuation character, and never two revisions joined by a two-dot range. AFTER committing the evidence run `devbox run --pure -- gitleaks dir .agents --config .gitleaks.toml`; never `gitleaks dir .`, which reports roughly 26 findings from the gitignored build/evidence/** tree.",
  "A26. EVIDENCE KEYS, for the issued records ONLY. Each evidence_key is that component's key read with `devbox run --pure -- make evidence-keys` on the FINAL tree with every touched file already `git add`-ed, because the manifest checks derive their input set from `git ls-files` and a file not yet in the index is invisible to them. Measure and record exactly FIVE keys: application, api, runner, reconciler and store-firestore. Under gate rule G6a these checks are re-run by make check, so per-component key equality is a record of PROVENANCE and NOT a pass/fail condition: do not measure all 23 component keys, do not copy a key from any earlier record, and do not treat key movement or key stability as a verdict.",
  "A27. CHAIN POSITION AND MANIFEST, recorded as facts for the coordinator. Record that this change creates and removes NO component edge: internal/application's new file uses domain, already declared among application's dependencies in ci/components.json; internal/api's one disjunct uses application, already declared; internal/runner/orchestrator.go already imports application; internal/reconciler's and internal/store/firestore's test edits use application and domain, both already declared. Re-measure all five dependency lists. Because no edge moves, a red manifest, dependency-coverage or key-closure check means the design was DEPARTED FROM: stop and escalate, and do not edit ci/components.json, Makefile, go.mod, go.sum or devbox.* -- touching ci/** would additionally move all 23 evidence keys, because ci/** is in all_on_change. Record that contracts_changed is EMPTY and why: contracts/openapi/openapi-v1.yaml:149 already declares 409 for claims:acquire, so the contracts key and the release BundleDigest do not move and this task does not enrol in the api-contract chain. Record the ordering: AFTER V2-084 (which lands Service.CompleteFraming, without which the product path to `ready` does not exist, and which holds internal/application/service.go, internal/application/framing_test.go, internal/api/api.go and internal/api/api_test.go) and AFTER V2-090 (which holds internal/api/api.go, internal/api/api_test.go and internal/application/readmodels.go, and whose requirement-scoped-cancel claim test this guard can pre-empt -- re-measure that test's fixture status and migrate it if it is not in an admitting state), and BEFORE V2-080 under gate rule G6b, because NINE of this task's allowed paths are in the 76-path live exercise file set published at .agents/v2/evidence/artifacts/V2-075-live-exercise-files.json: internal/application/service.go, internal/runner/orchestrator.go, internal/runner/orchestrator_test.go, internal/runner/control_agent_test.go, internal/runner/crash_test.go, internal/runner/journey_test.go, internal/runner/lease_test.go, internal/runner/provider_live_test.go and internal/runner/secret_broker_test.go. Re-measure that intersection and record it. Record the residual work with the measurement that blocks each, and state that their ids are the coordinator's to issue: Plan and Prepare refusing under a non-admitting parent; nextAction's missing branch; the two-cell gap of A5; issuers for wait and recover so those two admitting statuses become journey-reachable; and the live dogfood and live provider re-observations that A22 could not execute."
 ],
 "validation": {
  "argv": [
   "devbox",
   "run",
   "--pure",
   "--",
   "go",
   "test",
   "-race",
   "-count=1",
   "./internal/application",
   "./internal/api",
   "./internal/runner",
   "./internal/reconciler",
   "./internal/contracts"
  ],
  "working_directory": ".",
  "timeout_seconds": 2400
 },
 "constraints": [
  "Work only inside the assigned isolated task worktree; never push.",
  "Never use git reset, git checkout -- <path>, git stash or git commit --amend: these are the operations that lose uncommitted work.",
  "Do not weaken a fixture to avoid the refusal. Every affected fixture is moved into a state the product describes, and the move is recorded: one row per fixture naming the file, the seeding line, the tests it feeds, the status it now establishes, the mechanism, and the product citation that makes that status admit work (A13). Do not delete a claim step, do not delete or rename a test, do not stop asserting that the claim succeeds, and do not move a claim out of the fixture. The four tripwire counts of A15 have measured before-values and a decrease in any of them is a stop-and-escalate.",
  "Do not refuse a claim the Requirement's state actually admits. The admitting set is DERIVED, not chosen: it is {statuses from which domain.DecideRequirement admits domain.RequirementStart} union {domain.RequirementActive}, which is exactly {ready, active, waiting, recovering}. {ready, active} alone is the intuitive answer and it is WRONG -- internal/domain/model.go:486 admits the start command from waiting and recovering and docs/architecture/domain-model.md:267 and :270 both list `active` as a target -- and it was measured to be INVISIBLE to the test suite, because no existing fixture claims from either. The equivalence is asserted over the complete go/ast-derived status axis (A7); a disagreement is a stop-and-escalate.",
  "Do not change what claiming does when the state admits it. For each of the four admitting statuses, assert at the Service altitude that the lease is issued, the Execution created, the fencing token non-zero, the Increment version advanced by one, the increment.claimed event recorded and the claim-issued outbox item staged (A8). Add no field to ClaimRequest or ClaimResponse, change no existing return value, no existing event, no existing outbox item and no existing error for any admitted claim.",
  "Do not widen requirementAwaitsHumanInput and do not reuse ErrAwaitingHumanInput. internal/application/human_input.go and internal/application/human_input_test.go are PROHIBITED. The two questions have OPPOSITE correct answers on the unknown-parent case -- the needs-input question is right to return false for an empty link and a missing record, the admitting question must refuse both -- so sharing one helper forces one of them to be wrong. If the new guard masks or reorders the needs-input refusal, the only remedy is to fix the new guard.",
  "Do not touch Service.Renew. A lease already held is not revoked and not refused renewal when its parent leaves the admitting set: internal/application/service.go:1131-1138 records that no domain transition can revoke or release an active lease early, and docs/product/definition.md:143 says a stop on claims carries running work to a checkpoint rather than killing it. Assert that renewal still succeeds after the parent leaves the admitting set (A12).",
  "Do not declare the four statuses twice. They appear in exactly one switch, in internal/application/claimable.go, and that switch is read twice: by the guard and by the domain-equivalence assertion. A source scan asserts the single declaration and the single issuer (A20). internal/domain/** is PROHIBITED, so the set cannot be moved into the domain and cannot be widened there.",
  "Do not add a new status code, error code, openapi path or openapi response. The refusal is classified by ONE disjunct on the existing 409-conflict branch at internal/api/api.go:1253-1259, because contracts/openapi/openapi-v1.yaml:149 already declares 409 for claims:acquire. contracts/** is prohibited and contracts_changed is empty. Leaving the refusal unclassified was measured to produce HTTP 500 internal_error and to fire V2-088's operator record; that is not an acceptable outcome.",
  "Do not extend the refusal to Plan or Prepare. This task refuses the claim only. That an Increment can still be planned and prepared under a Requirement that admits no work is MEASURED and LOGGED as a finding for the tech_lead, never asserted and never fixed here.",
  "Do not add a paused, cancelled or any other branch to nextAction. internal/application/readmodels.go and readmodels_test.go are prohibited. That the read model still reports 'run next increment' (readmodels.go:324-325) for a ready Increment whose parent admits nothing is measured and logged, not fixed: the string is a claim about what to do next and the file is held by another task.",
  "Do not narrow internal/domain to match docs/architecture/domain-model.md, and do not edit docs/**. The two cells where the table lists `active` as a target that internal/domain/model.go:486 does not admit (:268 needs-input, :271 evaluating) are recorded as a named finding and asserted nowhere, for the reason internal/application/framing_test.go:562-564 already gives.",
  "Do not move internal/application/stop_matrix_test.go's constants. wantStopMatrixCells stays 56 and wantStopModeAllowedTotal stays 12; docs/product/definition.md:156-159 cites both by name. The only permitted change in that file is the fixture's parent status and the version threading A16 requires.",
  "Do not repair a stale expected version by removing it. A seed or a framing pair increments the Requirement's Version, so downstream ExpectedRequirementVersion values go stale -- measured as `stale domain version` on three TestLeaseKeeper tests. Thread the post-seed version. Dropping ExpectedRequirementVersion, passing zero, or moving the seed after the Plan each deletes a real assertion.",
  "Do not seed the orchestrator's journey with a store write. internal/runner/orchestrator.go is production code and must reach `ready` through Service.StartFraming and Service.CompleteFraming, and internal/runner/orchestrator_test.go must assert `ready` before the claim and `active` after. If Service.CompleteFraming does not exist at your HEAD, V2-084 has not landed: stop and escalate.",
  "Do not turn a refusal into a silent no-op. Every refused claim returns an error errors.Is can match, leaves the Increment, the Requirement, the lease set and the execution set byte-unchanged, and writes no idempotency record -- prove the last by replaying the same request_id after seeding the parent into an admitting status and asserting it executes for real. The u.Idempotency replay stays first, ahead of the new guard.",
  "Determinism is acceptance, not preference: no NEW fixed sleep, no NEW wall-clock timer, no NEW goroutine and no source of randomness in any new or modified test. The existing goroutine pair in internal/store/firestore/store_test.go:278-284 stays and none is added. Clocks are injected, every wait is a bounded deadline poll reusing an existing idiom in the package, and a measured duration is recorded as an observation and never asserted as a threshold. Run the five touched packages with -race.",
  "Do not weaken an assertion, drop a read, merge two assertions into one read or raise a budget to fit a quota. internal/quota and internal/store/memory are prohibited. Advance the injected clock between reads instead, and re-measure the read-transaction wall rather than trusting V2-084's stale four-transaction number, which V2-087 invalidated at internal/store/memory/store.go:353.",
  "Do not write a module-path string literal into any source or test file: internal/ci's manifest check reads such a literal as an undeclared component edge and turns make check red, and internal/application/source_guard_test.go parses every *.go file in that package including test files. The go/ast axis derivation uses a relative directory literal in the shape internal/application/allocation_test.go:624 already uses, never a module path. Build any needed path by concatenating a prefix with a suffix, and git add every new file before checking the manifest or reading an evidence key.",
  "Never record a prompt or a raw provider response in any form, including a truncated or hashed excerpt. Never record a credential value anywhere. Build every secret-shaped fixture -- including the StableSnapshot digests A8 needs to seed a completed Requirement -- by concatenating a prefix with a suffix so the pattern exists at run time and not in the file. No Provider CLI invocation is authorised -- not claude, not codex, not opencode, not even --version or --help -- and no live provider evidence record may be issued; internal/runner/provider_live_test.go is migrated by inspection and NOT executed. No GitHub write, no push, no webhook, no email. The only external processes authorised are the local Firestore emulator scripts/firestore-emulator.sh starts and the cmd/control-plane binary this repository builds.",
  "Do not touch ci/components.json, ci/**, Makefile, go.mod, go.sum or devbox.*. This design creates and removes no import edge in any of the five components, so a red manifest, dependency-coverage or key-closure check means the design was departed from: stop and escalate with the exact edge you would have needed. Touching ci/** would additionally move all 23 evidence keys, because ci/** is in all_on_change.",
  "Do not edit contracts/** at all, do not cite any capability id in any evidence record, and do not edit .agents/v2/evidence/V2-022-release-live-dogfood.json. Write no digest value into any contract or openapi document.",
  "Do not edit .agents/v2/task-state/**, any .agents/v2/packets/** file, any other task's evidence record, any entry in .agents/v2/evidence/index.json belonging to another task, or anything under .agents/v2/evidence/artifacts/**.",
  "allowed_paths is a WHITELIST. A file not named in it is prohibited even where this Work Order's prose mentions it -- and that includes every other file under internal/application, internal/api, internal/runner, internal/reconciler and internal/store/firestore. Every writable file is named, including each of the five evidence records and .agents/v2/evidence/index.json.",
  "Do not run the full `devbox run --pure -- make check`: the coordinator runs it once at integration. Your verification is `go test -race -count=1 ./...`, `go vet ./...`, `gofmt -l .`, make component-application, component-api, component-runner, component-reconciler, component-store-firestore and component-store-memory, `go test -count=1 ./internal/contracts`, the A22(i) emulator run and the live-local run. Run `devbox run --pure -- gitleaks dir .agents --config .gitleaks.toml` after the evidence commit; never `gitleaks dir .`.",
  "Judge every devbox run by its VERDICT LINE, never by the shell exit status: devbox run's exit code is not reliable in this repository. devbox run --pure also STRIPS environment variables, so AGENTIC_LOOP_LIVE_LOCAL must be passed with -e and never as a shell prefix. And `devbox run --pure -- <name>` treats a bare name as a devbox SCRIPT, so wrap any real command line as `devbox run --pure -- sh -c '...'`.",
  "Start the Firestore emulator for real for A22(i). A bare `go test ./internal/store/firestore` prints ok while producing 26 --- SKIP lines, so that run measured nothing, and a skipped test is never a pass (gate rule G7). If the emulator cannot be started, record the skips with status skipped, set the store-firestore record to partial and STOP -- do not report a pass.",
  "Under gate rule G6a, per-component evidence key equality is not a pass/fail condition. Measure exactly the FIVE keys for the records this task issues -- application, api, runner, reconciler, store-firestore -- do not measure all 23, and do not treat key movement or key stability as a verdict.",
  "Never write a verification as passed that was not actually executed. The provider-live and live-dogfood migrations are recorded as UNEXECUTED with status skipped. If any acceptance item cannot be satisfied, STOP and escalate to the tech_lead rather than fabricating, weakening or reinterpreting it.",
  "Every line number and every count in this Work Order was measured by the coordinator at 9bf0d8c and is expected to be STALE: each of the last fourteen tasks found at least one disagreement and V2-087 found five. Re-measure each one yourself with `go test -list` and a fresh grep, use the measured value, and report every disagreement in the evidence rather than the value written here. The fixture inventory in particular must be re-measured by EXECUTION -- implement the guard, run the whole suite, and enumerate the REFUSALS rather than only the failures, because a refused claim inside a test that does not assert its status leaves the test green while measuring nothing.",
  "This task must be serialized AFTER V2-084 and AFTER V2-090 and must not run concurrently with either. V2-084 holds internal/application/service.go, internal/application/framing_test.go, internal/api/api.go and internal/api/api_test.go and lands the Service.CompleteFraming without which there is no product path to `ready`; V2-090 holds internal/api/api.go, internal/api/api_test.go and internal/application/readmodels.go. Under gate rule G6b this task is ordered BEFORE the pooled live re-observation V2-080, because nine of its allowed paths are in the 76-path live exercise file set."
 ],
 "failure_stop": true,
 "tasks": [
  "V2-089"
 ],
 "priority": 89
}
