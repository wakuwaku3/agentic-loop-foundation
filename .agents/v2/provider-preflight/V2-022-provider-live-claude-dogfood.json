{
 "schema_version": "v1",
 "id": "V2-022-provider-live-claude-dogfood",
 "kind": "provider-preflight",
 "created_at": "2026-08-26T00:00:00Z",
 "correlation_id": "v2-022-preview-local-dogfood",
 "task_id": "V2-022",
 "notes": "V2-022's own record for the preview-local dogfood exercise. It amends nothing: the bytes of V2-017-provider-live-claude.json, V2-063-provider-live-claude-refresh.json and V2-075-provider-live-claude-rebind.json are unchanged and none of their ledgers is reused, because each is bound to its own task_id and CostLedger.Reserve refuses a ledger whose recorded task_id disagrees with the run's. This record carries its own limits.ledger_path and its accounting starts from an empty ledger. The exercise run under it is TestFoundationPreviewLocalDogfood in internal/api/live_dogfood_test.go: three accounted invocations per run (the autonomous-resolution journey, a graceful stop against a live child process group, and the resumed Requirement after the Loop channel rollback), executed twice as two separate processes for the stability probe, for six planned accounted invocations. max_invocations is held at 24 rather than at the eight the design packet planned because two earlier exercises under comparable records consumed their whole allowance: V2-063 used 12 of 12 and V2-075 used 12 of 12, because provider-side transient failures cost one to three invocations per observation. The thresholds below are runaway-detection stops, never billing ceilings and never an approval question; reaching one is a fail-closed stop for inspection that is neither a success nor a capability failure.",
 "provider": {
  "name": "claude",
  "version": "2.1.241",
  "executable_path": "/home/takushi/.local/bin/claude"
 },
 "limits": {
  "max_invocations": 24,
  "max_total_cost_usd": 20.0,
  "worst_case_reservation_usd": 1.0,
  "currency": "USD",
  "window": "total",
  "ledger_path": "/home/takushi/.local/state/agentic-loop/v2/V2-022-provider-cost.json",
  "enforced_by": "internal/runner.SupervisedInvocationRunner and internal/runner.CostLedger",
  "fail_closed": true
 },
 "environment": {
  "base_names": [
   "HOME",
   "PATH"
  ],
  "granted_names": []
 },
 "rollback": {
  "trigger": "the ledger reaches max_invocations or max_total_cost_usd (runaway-detection stop), or a single settled invocation exceeds the 2.00 USD anomaly threshold and the ledger's halted flag is set",
  "argv": [
   "cat",
   "/home/takushi/.local/state/agentic-loop/v2/V2-022-provider-cost.json"
  ],
  "completion_conditions": [
   "no further claude invocations are made for this task without a new, freshly-approved provider-preflight record",
   "the ledger's halted flag, once set, is cleared only by the owner issuing a new approved record, never by editing this record or the ledger file in place",
   "reaching max_invocations or max_total_cost_usd stops the exercise for inspection and is reported as neither a success nor a failure; the limits in this record are never raised and this ledger is never re-initialised to reset the count"
  ]
 },
 "verification": [
  {
   "name": "provider-cli-version",
   "argv": [
    "/home/takushi/.local/bin/claude",
    "--version"
   ],
   "expected": "2.1.241",
   "read_only": true
  }
 ],
 "approval": {
  "approver": "takushi.yokoyama@sansan.com",
  "approved_at": "2026-08-24T23:22:47Z",
  "approval_reference": "owner instruction recorded in the coordinating session of 2026-08-25, transcribed by the coordinator; see ev-v2-055-standing-authorization",
  "scope": "The owner authorises the Loop to invoke the contracted flat-rate Provider subscriptions on the owner's own machines with no per-invocation approval and no count or monetary ceiling, for the duration of the v2 work. Preflight limits stay in force only as runaway-detection thresholds; reaching one is a stop for inspection, never a success. Transcribed by the coordinator under the owner's instruction; ev-v2-055-standing-authorization holds the verbatim instruction and the limits of this record's machine check.",
  "subject_path": ".agents/v2/packets/provider-standing-authorization.json",
  "subject_digest": "c69a1026f80e8fcd7f6dc05eb5b27c4434c90da43e44cfc0ff6030d62c75f113"
 }
}
