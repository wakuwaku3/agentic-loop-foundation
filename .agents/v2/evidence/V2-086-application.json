{
  "schema_version": "v1",
  "id": "ev-v2-086-application",
  "kind": "evidence",
  "created_at": "2026-08-26T22:00:00Z",
  "correlation_id": "v2-086-scheduler-role",
  "task_id": "V2-086",
  "component": "application",
  "evidence_key": "5b0c777beefd4b91cbf4bfff3e91e6e7e9a6bb3c91af0e4c2caf637d5e574424",
  "result": "passed",
  "observed_at": "2026-08-26T22:00:00Z",
  "checks": [
    {
      "name": "scope-boundary",
      "status": "passed",
      "argv": [
        "git",
        "rev-parse",
        "HEAD"
      ],
      "working_directory": ".",
      "timeout_seconds": 30,
      "summary": "Scope boundary for V2-086, recorded against base commit a0622910d493cadd94c6d819690dbab0232a44ab on branch v2-task/V2-086, BEFORE any source file was edited: this record's first commit contains rows 1-5 and the whole A1/A2 baseline, and `git status --porcelain` at that commit showed ONLY .agents/v2/evidence paths, so every non-test and test source file named below was still byte-identical to the base. ROW 1 -- what this task makes true: the scheduler becomes a role a real caller can be. internal/application gains exactly one exported constructor, LoopCaller(subject string) (Caller, error), which is the only sanctioned producer of a Caller carrying RoleScheduler and refuses an empty or whitespace-only subject with ErrUnauthenticated; internal/api's CombinedAuthenticator gains one field, SchedulerIdentity, plus one exported validating constructor NewCombinedAuthenticator and one exported predicate IdentityConfigurationError which is the single source of truth for the misconfiguration rule, so the SAME rule refuses at START-UP (cmd/control-plane's run() calls the constructor and returns its error) and at the request boundary. The field is checked AFTER the byte-unchanged runner-session branch and parseIAPEmail but BEFORE the OwnerEmails lookup, and refusing outright an identity configured as both scheduler and owner; POST /internal/reconcile keeps its own identity check byte-for-byte and additionally carries application.LoopCaller(ReconcileIdentity) on the context it hands InternalReconcile; cmd/control-plane/main.go builds the authenticator through that constructor, passing the same strings.ToLower(reconcileIdentity) value it already passes as ReconcileIdentity, adding no environment variable; and internal/runner.Orchestrator's fabricated Caller{Role: RoleOwner, Subject: runner-local-owner} literal is replaced by a required injected field that fails closed when its Role or Subject is empty. The measured before/after is a count: non-test producers of a RoleScheduler Caller 0 -> 1, and reachability of domain.ActorTypeLoop for Requirement.RequestedBy through a transport false -> true. ROW 2 -- what it deliberately does not do: application.RoleScheduler is not removed and no callerActor role pair changes; no operation becomes scheduler-only; no route's role gate changes; no PermitKind, ActorType, Role or status is added; internal/domain is byte-unchanged; contracts/openapi/openapi-v1.yaml is byte-unchanged (sha256 ca62ef4c0e1f7dbe357d03209d68f387581ed53aeaa66b0ea8fc5b8b7eea8cdd before and after) and contracts_changed is empty; api.Config gains and loses no field; POST /internal/reconcile is NOT re-routed through h.config.Authenticator, because that branch runs before the authenticator-nil guard and must keep working under LocalOwnerBearerAuthenticator, which cannot resolve an IAP email at all; LocalOwnerBearerAuthenticator is not changed and no preview-local scheduler environment variable is added; internal/application/readmodels.go, internal/web/**, docs/** and contracts/** are untouched, because actor_type 'loop' is already declared in the RequestedBy schema and already admitted by domain.validActorType, so the new value flows through every existing response and export with no field and no branch; ci/components.json, Makefile, go.mod, go.sum and devbox.* are untouched because the design creates and removes no import edge; no existing test is deleted, renamed or weakened. ROW 3 -- what it records but does not fix: (a) the openapi document declares at contracts/openapi/openapi-v1.yaml lines 5-8 that increment decomposition/planning 'remains a loop scheduler responsibility' while internal/application/service.go's Plan (:888) and Prepare (:1023) accept RoleOwner and 23 test files call one of the two, so the declaration and the application-layer role pair disagree; that is a statement about the public HTTP surface, where neither command has a route at all, and it is recorded here for the tech_lead rather than resolved. (b) Orchestrator.RunFakeJourney has no non-test caller in the repository and cmd/runner/main.go constructs no Orchestrator, so the whole Capture-to-AcceptResult driver is exercised only by internal/runner/orchestrator_test.go; moving the driver into a _test.go file is a larger and better change that is NOT taken here, and is recorded so a later task can decide. (c) the in-memory adapter never trues up its quota reservation, so quota.ReadTransactionUsage (Reads 6001, Writes 1) admits only four READ transactions per budget day against quota.DefaultBudget's 25000 reads; the new proof works around it by stepping an injected clock, never by weakening an assertion, and the adapter defect stays V2-087's. (d) internal/store/firestore's suite prints ok while skipping everything with no emulator. The figure is recorded WITH ITS PATTERN so it can never be ambiguous again: `grep -c '^--- SKIP'` counts TOP-LEVEL skips and gives 26, and `grep -cE '^ *--- SKIP'` counts skipped subtests too and gives 49. Both were measured on the same captured output in this worktree, so the coordinator's 49 and this task's 26 are one fact under two patterns and neither figure is wrong. Either way that package's run measured nothing about this change; it is recorded under gate rule G7 and never counted as a pass. (e) NOTHING remains on A6: it was first reported as UNMET and is now MET, and the history is kept rather than tidied away. The implementer escalated A6's three conditions as irreconcilable; the tech_lead ruled that they reconcile if the predicate does not live in cmd/control-plane, and the ruling is right. The predicate is now internal/api.NewCombinedAuthenticator plus CombinedAuthenticator.IdentityConfigurationError, run() calls the constructor and returns its error, and the start-time refusal is proven by EXECUTING the binary; the request-boundary refusal is kept as defence in depth. See the api record's a6-* checks. The superseded reasoning was: A6 asks both that the process 'fail to start' when RECONCILE_IDENTITY is also in OWNER_EMAILS and that nothing else in cmd/control-plane/main.go change and that the refusal be asserted through the authenticator; the two halves conflict, so the refusal is implemented and tested in CombinedAuthenticator.Authenticate (every IAP-authenticated request is refused with a message naming the conflict) and NO start-time guard was added -- recorded as a partial, escalated to the tech_lead, not reinterpreted -- and the escalation is what produced the ruling that fixed it. ROW 4 -- what only a later task can establish: the loop caller the reconcile branch now carries is verified as present and correctly attributed but is NOT consumed by any command inside the tick, because both reconcilers the cmd/control-plane closure drives (reconciler.Reconciler.Tick and reconciler.VerificationReconciler.Tick) take no Caller; the Requirement transitions that will consume it belong to the V2-084 follow-on. The preview-local composition can produce an owner and a runner but not a scheduler. ROW 5 -- this task passes no release gate: exactly three evidence records are issued (application, api, runner), release_eligible is false in all three and in the index, no capability id is cited anywhere, contracts/release-contract/** is untouched, .agents/v2/evidence/V2-022-release-live-dogfood.json is untouched, no release BundleDigest moves, no Provider CLI was started and no Google Cloud resource was touched."
    },
    {
      "name": "a1-i-contextwithcaller-census",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn ContextWithCaller --include='*.go' . | grep -v _test.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A1(i) MEASURED at a0622910d493cadd94c6d819690dbab0232a44ab, and it DISAGREES with the Work Order by being far larger than 'the internal/api call sites'. 15 non-test hits, in three groups. (1) The DEFINITION: internal/application/caller.go:28. (2) internal/api/api.go, TWELVE call sites: :172 listRequirements, :185 listControls, :199 getRequirement, :212 listRepositories, :236 getRepository, :249 queueSummary, :270 listRunners, :294 listProviders, :307 export, :328 releaseState, :342 getRequirement (the second, release-scoped read), and :358 the single ctx built once for the whole authenticated POST switch. (3) internal/runner/orchestrator.go:39 and :40 -- exactly the two sites the Work Order names, and the ONLY ContextWithCaller sites outside internal/api and internal/application. orchestrator.go:39 is verbatim `owner := application.ContextWithCaller(ctx, application.Caller{Role: application.RoleOwner, Subject: \"runner-local-owner\"})` and :40 builds a genuine runner caller from o.RunnerID. So the fabrication the outcome sentence names is confirmed at the exact line the Work Order gives."
    },
    {
      "name": "a1-ii-caller-literal-census",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn 'Caller{' --include='*.go' . | grep -v _test.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A1(ii) MEASURED at a0622910d493cadd94c6d819690dbab0232a44ab and it AGREES with every line number the Work Order gives, which is itself worth recording after eleven tasks of drift. 19 non-test hits. The ROLE-BEARING producers are exactly five: internal/api/auth.go:50 (RoleRunner, CombinedAuthenticator) and :85 (RoleRunner, LocalOwnerBearerAuthenticator) -- the only two RoleRunner producers; internal/api/auth.go:59 (RoleOwner, CombinedAuthenticator, from the IAP assertion) and :99 (RoleOwner, LocalOwnerBearerAuthenticator, from a bearer token) -- the only two RoleOwner producers reached from a transport; and internal/runner/orchestrator.go:39 (RoleOwner, FABRICATED) plus :40 (RoleRunner, from o.RunnerID) -- the only producers outside internal/api. The remaining 13 hits are the zero value `application.Caller{}` or `Caller{}` returned beside an error (auth.go:21, :25, :44, :48, :54, :57, :79, :83, :89, :93, :97 and internal/application/caller.go:34) and carry no role. ZERO of the 19 names RoleScheduler."
    },
    {
      "name": "a1-iii-rolescheduler-census",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn RoleScheduler --include='*.go' . | grep -v _test.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A1(iii) MEASURED at a0622910d493cadd94c6d819690dbab0232a44ab: 19 non-test hits, ZERO of them produce-side, which is the defect. Marked one by one. PRODUCE-SIDE: none. DECLARATION (neither): internal/application/caller.go:15, the const. ATTRIBUTION (the one thing that requires the role): caller.go:68, the `case RoleScheduler:` arm of requestedBy that returns domain.ActorTypeLoop. COMMENT/PROSE (neither): caller.go:60 and internal/application/framing.go:85 and :86 -- framing.go's comment already states in the tree that 'no non-test path in this repository constructs a Caller with RoleScheduler'. ACCEPT-SIDE, 13 sites: internal/application/service.go:749 (Capture, callerActor owner|scheduler), :888 (Plan, owner|scheduler), :1023 (Prepare, owner|scheduler), :1373 (Control, owner|scheduler), :1529 (Permit, owner|runner|scheduler); internal/application/repository.go:133, :223, :471, :514 (owner|scheduler); internal/application/framing.go:123 (StartFraming, owner|scheduler); internal/application/human_input.go:414 (askedBy's `case RoleRunner, RoleScheduler:` arm) and :461 (RequestHumanInput, callerActor runner|scheduler); internal/api/api.go:439 (POST /v1/requirements/{id}:request-input, runner|scheduler) and :475 (POST /v1/requirements/{id}:start-framing, owner|scheduler). DISAGREEMENTS with the Work Order's line numbers, seven of them: service.go Capture expected :744 measured :749 (+5); Plan expected :883 measured :888 (+5); Prepare expected :987 measured :1023 (+36); Control expected :1303 measured :1373 (+70); Permit expected :1458 measured :1529 (+71); api.go :request-input expected :427 measured :439 (+12); api.go :start-framing expected :463 measured :475 (+12). AGREEMENTS: repository.go :133, :223, :471, :514 and framing.go :123 and human_input.go :461 are exactly as written. The Work Order's count of thirteen accept-side sites is CONFIRMED as thirteen, but its list omits human_input.go:414 (askedBy) and counts api.go's two route gates, so the composition of the thirteen differs from its enumeration by one: the Work Order lists Capture, Plan, Prepare, Control, Permit, repository x4, framing, RequestHumanInput and api.go x2 = 13 and does not name askedBy; measured, askedBy makes 13 accept-side plus askedBy = 14 role-accepting arms. Recorded as measured rather than reconciled to the written number."
    },
    {
      "name": "a1-iv-runfakejourney-callers",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn 'RunFakeJourney' --include='*.go' . ; grep -rn 'Orchestrator{' --include='*.go' ."
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A1(iv) MEASURED at a0622910d493cadd94c6d819690dbab0232a44ab and it DISAGREES with the Work Order's 'exactly ONE caller'. Orchestrator.RunFakeJourney is DEFINED at internal/runner/orchestrator.go:35 and CALLED from TWO sites, both in the same test function: internal/runner/orchestrator_test.go:56 (the first journey, which the Work Order names) and internal/runner/orchestrator_test.go:63 (the retry, asserting idempotency). So the measured count of call sites is two, not one; the measured count of NON-TEST call sites is ZERO, which is the substance of the claim and is confirmed. The count of `Orchestrator{` composite literals in the whole repository is exactly one, internal/runner/orchestrator_test.go:54, and it is a test; cmd/runner/main.go contains no Orchestrator at all (grep over cmd/runner/main.go for Orchestrator returns nothing), so adding a required field to the struct changes no production wiring."
    },
    {
      "name": "a1-v-baseline-top-level-test-counts",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "for p in application api runner domain; do go test -list '.*' ./internal/$p | grep -c '^Test'; done"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A1(v) MEASURED at a0622910d493cadd94c6d819690dbab0232a44ab, counting only lines matching ^Test in `go test -list '.*'` output: internal/application 193, internal/api 63, internal/runner 118, internal/domain 54. Cross-checked against `grep -c '^func Test'` over each package's *_test.go files, which returns the identical four numbers 193/63/118/54, so neither measurement is an artefact of the other. The Work Order gives no expected values for these four, so there is nothing to disagree with; these are the BEFORE sets of the A11 set-difference."
    },
    {
      "name": "a1-baseline-full-suite-green-before",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 ./..."
      ],
      "working_directory": ".",
      "timeout_seconds": 1800,
      "summary": "A1 baseline, so that any later red is attributable. At a0622910d493cadd94c6d819690dbab0232a44ab, with -race and -count=1, every package reported ok and none reported FAIL: cmd/bootstrap, cmd/ci-plan, internal/api, internal/application, internal/ci, internal/contracts, internal/domain, internal/legacyimport, internal/provider, internal/quota, internal/reconciler, internal/release, internal/runner, internal/scheduler, internal/store/firestore, internal/store/memory, internal/update all ok; cmd/control-plane, cmd/legacy-import, cmd/runner and internal/web report [no test files]. Verdict line taken from the output rather than from the shell exit status, which is unreliable under devbox run in this repository."
    },
    {
      "name": "a1-ordering-record-precedes-source-edit",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "git status --porcelain && git rev-parse HEAD"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "Gate ordering, recorded because it is itself an acceptance item (hard rule 2, and V2-087 had to report the opposite). The scope-boundary table above and every a1-* and a2-* baseline check in these three records were written and COMMITTED as the FIRST commit on branch v2-task/V2-086, on top of base commit a0622910d493cadd94c6d819690dbab0232a44ab, at which point `git status --porcelain` listed only .agents/v2/evidence/V2-086-application.json, .agents/v2/evidence/V2-086-api.json, .agents/v2/evidence/V2-086-runner.json and .agents/v2/evidence/index.json. No file under internal/ or cmd/ was modified before that commit. The commits that follow are, in order: the A3 reproduction test (test files only, run against the UNREPAIRED non-test tree), then the repair, then the proof, then the verification results appended to these records."
    },
    {
      "name": "a2-scope-table-thirteen-accept-sites-zero-require",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn 'callerActor(ctx, Role' --include='*.go' internal/application | grep -v _test.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A2, the enumeration that is the judgement, reproduced BEFORE any code was written. One row per non-test site that names application.RoleScheduler: file:line | operation | complete accepted role set | is any accepted role produced by a real transport today | does the operation REQUIRE the scheduler. (1) internal/application/service.go:749 | Capture | owner, scheduler | YES, owner (internal/api/auth.go:59 and :99) | NO. (2) service.go:888 | Plan | owner, scheduler | YES, owner | NO. (3) service.go:1023 | Prepare | owner, scheduler | YES, owner | NO. (4) service.go:1373 | Control | owner, scheduler | YES, owner | NO. (5) service.go:1529 | Permit | owner, runner, scheduler | YES, owner and runner | NO. (6) internal/application/repository.go:133 | RegisterRepository | owner, scheduler | YES, owner | NO. (7) repository.go:223 | the repository observation command | owner, scheduler | YES, owner | NO. (8) repository.go:471 | a repository read | owner, scheduler | YES, owner | NO. (9) repository.go:514 | a repository read | owner, scheduler | YES, owner | NO. (10) internal/application/framing.go:123 | StartFraming | owner, scheduler | YES, owner | NO. (11) internal/application/human_input.go:461 | RequestHumanInput | runner, scheduler (the OWNER is REFUSED) | YES, runner (auth.go:50 and :85) | NO. (12) internal/api/api.go:439 | transport gate, POST /v1/requirements/{id}:request-input | runner, scheduler | YES, runner | NO. (13) internal/api/api.go:475 | transport gate, POST /v1/requirements/{id}:start-framing | owner, scheduler | YES, owner | NO. VERDICT: ZERO of the thirteen operations REQUIRES the scheduler; every one of them also accepts a role a real transport already produces. That is the argument for removing the role, and it is not acted on, because of the next check. NO scheduler-ONLY operation was found, so d3's (a)/(b) judgement does not change and no escalation is triggered on that axis."
    },
    {
      "name": "a2-requestedby-is-the-sole-producer-of-actortypeloop",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "grep -rn 'requestedBy(' --include='*.go' internal/application/ | grep -v _test.go ; grep -n reqBy internal/application/service.go internal/application/repository.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A2's decisive half, MEASURED, and its call-site list DISAGREES with the Work Order in three places. internal/application/caller.go:64-72 is requestedBy, and its `case RoleScheduler:` arm at :69 is the SOLE producer of domain.RequestedBy{ActorType: domain.ActorTypeLoop} in the whole package. Its non-test call sites are exactly FIVE: human_input.go:577 (the human-input answerer), repository.go:137 (a Repository's RequestedBy), repository.go:227 (a discard-result guard on the observation path), service.go:753 (Capture) and service.go:1377 (Control). The stored fields those five feed are: service.go:846 domain.Requirement.RequestedBy; service.go:857 domain.RequirementRepositoryLink.RequestedBy; service.go:865 CaptureResponse.RequestedBy; repository.go:204 domain.Repository.RequestedBy; repository.go:216 RepositoryResponse.RequestedBy; service.go:1436 SaveControlRequestedBy (the Control Intent requester side table) and service.go:1505 ControlResponse.RequestedBy. DISAGREEMENTS: the Work Order says the call sites are service.go:748, :841, :852, repository.go:137, :204, service.go:1307, :1365 and human_input.go:577. Measured, service.go:841 and :852 and repository.go:204 are NOT requestedBy call sites at all -- they are USES of the single `reqBy` value each function computes once -- and the four true numbers are service.go:753 (expected :748, +5), service.go:1377 (expected :1307 or :1365, +70 or +12), repository.go:137 (AGREES) and human_input.go:577 (AGREES); repository.go:227 is a fifth call site the Work Order does not list at all. Because requestedBy's default arm (caller.go:70-72) refuses every other role, removing RoleScheduler would make domain.ActorTypeLoop unreachable for all of those stored fields, so the role is kept: judgement (a)."
    },
    {
      "name": "a2-askedby-is-not-an-argument",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "sed -n '405,425p' internal/application/human_input.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A2's 'what is NOT an argument', MEASURED, and its line span DISAGREES slightly. askedBy is internal/application/human_input.go:405-420 by the Work Order and MEASURED at :405 (comment) with `func askedBy(c Caller)` at :412; its RoleRunner arm is :414, `case RoleRunner, RoleScheduler:`, and it maps BOTH to domain.ActorTypeLoop. So domain.ActorTypeLoop is ALREADY reachable today for a HumanInputRequest's AskedBy through the runner transport, and the unreachability this task repairs is precisely the five requestedBy-fed fields in the previous check and nothing else. That measurement is what keeps the repair honest: it is not 'ActorTypeLoop is unreachable', it is 'ActorTypeLoop is unreachable for a Requirement's, a Repository's, a link's and a Control Intent's requester and for the human-input answerer'."
    },
    {
      "name": "a3-defect-reproduced-before-repair",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 -run 'TestExactlyOneSanctionedSchedulerCallerProducerExists' ./internal/application"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A3, the defect reproduced by an executed run BEFORE the repair, with its verbatim verdict. The commit that precedes the repair adds test files only; every non-test file was still byte-identical to a0622910d493cadd94c6d819690dbab0232a44ab when this ran. VERBATIM: '--- FAIL: TestExactlyOneSanctionedSchedulerCallerProducerExists (0.18s)' followed by 'requested_by_test.go:372: the scan parsed 93 non-test files and found 0 RoleScheduler Caller literals in internal/application/caller.go (all sites: []), want exactly 1 -- the return inside LoopCaller.' and 'FAIL github.com/takushi/agentic-loop-foundation/v2/internal/application 0.202s'. So the BEFORE row is measured, not asserted from a grep: 93 non-test .go files parsed across the whole module, ZERO producers of a RoleScheduler Caller anywhere. The AFTER row, from the same test after the repair: '--- PASS' with 'scheduler-producer scan parsed 93 non-test .go files: 1 sanctioned producer at internal/application/caller.go:70 and 0 anywhere else'. The count moved 0 -> 1 exactly as the outcome sentence requires. The transport half of A3 is the permanent test TestNoOwnerOrRunnerTransportCanRecordALoopRequester in internal/api, which PASSED at the same unrepaired commit: driving POST /v1/requirements over the composed handler as the owner records requested_by.actor_type 'owner' in the response and domain.ActorTypeOwner in the store, a runner is 403 (Capture's pair is owner-or-scheduler), and a CombinedAuthenticator with no scheduler identity resolves an allowlisted IAP email to exactly RoleOwner -- so domain.ActorTypeLoop was unreachable for a Requirement's RequestedBy through any transport."
    },
    {
      "name": "a4-the-one-sanctioned-producer",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 -run 'TestLoopCallerIsTheOnlyProducerAndRefusesAnEmptySubject' ./internal/application"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A4 MEASURED. internal/application/caller.go gains exactly ONE exported symbol, LoopCaller(subject string) (Caller, error) at caller.go:41, returning Caller{Role: RoleScheduler, Subject: subject} at caller.go:70 and refusing an empty or whitespace-only subject with ErrUnauthenticated. Its doc comment states it is the ONLY sanctioned way to obtain a scheduler caller and that it is the in-process analogue of internal/api/auth.go's transport constructors. Nothing else was added: no field on Caller (it is still Role, Subject, RunnerID), no new Role (the const block is still RoleOwner, RoleRunner, RoleScheduler), and no other exported identifier -- `git diff` over caller.go adds one func, one import (strings) and comments, and changes no existing line. REFUSAL PROOF, executed: LoopCaller is called with each of \"\", \" \", \"   \", a tab, a newline and \" \\t\\r\\n \" and each returns ErrUnauthenticated AND the Caller zero value, so no partially-formed scheduler identity can leave the constructor; the positive case returns RoleScheduler with the subject exactly as given and an empty RunnerID, and the same caller drives Service.Capture to a stored Requirement whose RequestedBy reads loop/reconciler.self-intake. VERDICT: 'ok github.com/takushi/agentic-loop-foundation/v2/internal/application'."
    },
    {
      "name": "a4-source-scan-zero-outside-one-inside",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 -v -run 'TestExactlyOneSanctionedSchedulerCallerProducerExists' ./internal/application"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A4's source-scan half, as a permanent assertion rather than a grep in prose. The scan walks the module from the repository root, parses every non-test *.go file with go/parser (93 files, and a scan of fewer than 40 is a Fatal so it cannot pass vacuously), and reports every composite literal of type Caller whose Role field names RoleScheduler in either spelling -- bare inside internal/application, package-qualified anywhere else. Its matcher is verified first against a synthetic file holding both positive spellings, a RoleOwner literal and a non-Caller literal carrying a Role field, and must match exactly the two positives. MEASURED after the repair: 1 producer, at internal/application/caller.go:70, and 0 in every other package -- so no package outside internal/application constructs a Caller literal with RoleScheduler, and the count of non-test producer sites is exactly one. VERBATIM log line: 'scheduler-producer scan parsed 93 non-test .go files: 1 sanctioned producer at internal/application/caller.go:70 and 0 anywhere else'. The complementary guard lives in the runner record: internal/runner's own non-test source is scanned for any application.Caller literal naming a Role other than RoleRunner, and there is none."
    },
    {
      "name": "a13-closed-set-discipline",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 -run 'TestRequestedByStaysAClosedRefusalForEveryOtherRole' ./internal/application"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A13 MEASURED, and the answer is that NO closed-set guard's want set was widened by this task, so A13's three conditions have nothing to apply to. requestedBy (internal/application/caller.go:99-108 after the change, :64-73 before) has the same two arms it had at a0622910d493cadd94c6d819690dbab0232a44ab -- RoleOwner to domain.ActorTypeOwner and RoleScheduler to domain.ActorTypeLoop -- and its default arm is still `return domain.RequestedBy{}, ErrForbidden`, a refusal. askedBy's `case RoleRunner, RoleScheduler:` is unchanged. No callerActor role list changed: the thirteen accept-side sites enumerated in the A2 table are byte-identical, verified by `git diff` over internal/application showing changes only in caller.go and requested_by_test.go. The set is asserted rather than read: a new permanent test drives Capture as each of the two admitted roles and checks the recorded actor_type, checks the admitted table has exactly two entries, and then drives Capture as RoleRunner, as the zero Role and as an invented Role('superuser') and requires ErrForbidden for each, so an unlisted member still fails. VERDICT: 'ok github.com/takushi/agentic-loop-foundation/v2/internal/application'."
    },
    {
      "name": "a10-nothing-became-scheduler-only",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "git diff --stat a0622910d493cadd94c6d819690dbab0232a44ab -- internal/application/service.go internal/application/repository.go internal/application/framing.go internal/application/human_input.go internal/domain contracts"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A10's application half, MEASURED as an empty diff. internal/application/service.go, repository.go, framing.go and human_input.go are BYTE-UNCHANGED from a0622910d493cadd94c6d819690dbab0232a44ab, so not one of the thirteen callerActor role pairs moved and nothing became scheduler-only; internal/domain is byte-unchanged, so no Role, ActorType, PermitKind or status was added; contracts/** is byte-unchanged, so the openapi document, the release contract and every schema are untouched and contracts_changed is empty. The scheduler's reachable surface after this task is exactly what already named it: POST /internal/reconcile, plus every owner-or-scheduler and runner-or-scheduler pair the A2 table enumerates. The one thing the scheduler gained is the ability to BE, not to do more."
    },
    {
      "name": "a11-additive-and-application-test-name-sets",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -list '.*' ./internal/application | grep -E '^Test' | sort"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A11 MEASURED for internal/application. BEFORE 193 top-level tests, AFTER 196. The set difference is ADDITIONS ONLY: TestExactlyOneSanctionedSchedulerCallerProducerExists, TestLoopCallerIsTheOnlyProducerAndRefusesAnEmptySubject and TestRequestedByStaysAClosedRefusalForEveryOtherRole. Removals: ZERO -- `comm -23` over the sorted before and after name sets is empty, so no test was deleted and none was renamed, and all 193 pre-existing tests still pass in the same run. Additive by measurement elsewhere too: internal/domain is byte-unchanged and its test count is 54 before and 54 after; internal/application/readmodels.go is untouched because requestedByView (readmodels.go:139, not :136 as the Work Order says -- MEASURED, a +3 disagreement) already projects a RequestedBy with any valid ActorType and domain.validActorType (internal/domain/model.go:233-236, admitting ActorTypeOwner and ActorTypeLoop at :235) already admits the new value, so it flows through every existing response and export with no field and no branch; internal/web/** and docs/** are untouched."
    },
    {
      "name": "a14-determinism-and-quota",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "grep -n 'time.Sleep\\|time.After\\|time.NewTimer\\|time.Tick\\|time.Now\\|math/rand\\|crypto/rand' internal/application/requested_by_test.go internal/api/api_test.go internal/api/auth_test.go internal/runner/orchestrator_test.go"
      ],
      "working_directory": ".",
      "timeout_seconds": 300,
      "summary": "A14 MEASURED over every new and modified test. No new or modified test contains time.Sleep, time.After, time.NewTimer, time.Tick, time.Now, `go ` (a goroutine), math/rand or crypto/rand: verified by grepping the four touched test files for each of those tokens. Every clock is injected: internal/api's new fixture uses framingClock, the steppable injected clock the V2-082 chain test already introduced, advanced only by its own nextDay() method; internal/runner's new tests reuse the package's existing journeyClock, which returns a fixed time.Unix(1700000000, 0).UTC(); internal/application's new tests use the package's existing service() helper and its fixed clock. There is no wait of any kind in any new test, so there is no deadline poll to bound and no measured duration is asserted as a threshold. The whole suite was run with -race and -count=1 and reported ok for every package. QUOTA: the in-memory adapter does not true up its quota reservation, so quota.ReadTransactionUsage (Reads 6001, Writes 1, at internal/quota/quota.go:56) admits only FOUR read transactions per budget day against quota.DefaultBudget's 25000 reads (internal/quota/quota.go:46). The A9 proof performs three GET /v1/requirements/{id} reads plus three captures plus two verb mutations plus two 403s plus five reconcile requests, so the injected clock is stepped a whole UTC day three times (after the captures, before the two verbs, and before the owner-only refusals) rather than dropping a read or weakening an assertion. internal/quota is prohibited and that adapter defect stays V2-087's. ONE deliberate residual randomness, recorded rather than hidden: internal/api/auth_test.go's runner-session test drives the real enrollment protocol, whose nonce and tokens come from crypto/rand inside internal/runner's PRODUCTION code (the pre-existing TestRunnerEnrollmentHTTPChallengeFlow does the same); the test itself introduces none -- its ed25519 key is derived from an all-zero seed rather than generated -- and no assertion depends on any random value."
    },
    {
      "name": "a15-rollback-is-a-revert-of-five-edits",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "git diff --stat a0622910d493cadd94c6d819690dbab0232a44ab -- internal cmd"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A15, stated as a property and justified by measurement, and REVISED after the A6 repair rather than left stale. Rollback is a revert of exactly five non-test FILES and nothing else: (1) internal/application/caller.go -- one constructor (LoopCaller) plus its `strings` import; (2) internal/api/auth.go -- one field (SchedulerIdentity), one exported validating constructor (NewCombinedAuthenticator), one exported predicate (IdentityConfigurationError) and the four-line scheduler block between parseIAPEmail and the OwnerEmails lookup; (3) internal/api/api.go -- one context wrap (the LoopCaller build and the ContextWithCaller argument to h.config.InternalReconcile); (4) internal/runner/orchestrator.go -- one field (Caller), one guard ('orchestrator caller identity is incomplete') and one renamed local (owner -> callerCtx); (5) cmd/control-plane/main.go -- one constructor call plus one `if err != nil { return err }` replacing one composite literal. The property the item asserts is unaffected: nothing else must be undone. MEASURED as the complete non-test change set: `git diff --name-only` over the whole task lists exactly those five non-test files plus four test files plus the four evidence paths. Nothing else must be undone, because the change adds no port (no interface in internal/application/ports.go moves), no stored field (internal/domain is byte-unchanged), no document kind, no index (firestore.indexes.json untouched), no route, no request or response field (contracts/** byte-unchanged) and no import edge (ci/components.json and Makefile untouched, and both VerifyNoUnjustifiedEdges and VerifyDependencyCoverage pass inside `go test ./internal/ci`, which is green). THE ONE DATA CLAIM, verified: a Requirement stored with RequestedBy.ActorType == \"loop\" still reads after a revert, because the domain already validates that value -- internal/application's TestRequestedByActorTypeIsAClosedEnum (pre-existing, untouched) asserts domain.Validate accepts ActorTypeLoop, and internal/api's new proof reads the loop-attributed Requirement back through GET /v1/requirements/{id} and through requestedByView, neither of which this task modified, so both paths exist identically at a0622910d493cadd94c6d819690dbab0232a44ab."
    },
    {
      "name": "a16-chain-position",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "git log --oneline -3"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A16, recorded as facts for the coordinator. internal/api/api.go is in V2-083's allowed_paths and internal/api/api_test.go is in V2-087's, so this task must be serialized AFTER BOTH; internal/application/caller.go is prohibited to V2-082 and V2-085 and belongs to this task, so V2-085 must not run concurrently with it. MEASURED as satisfied at the time of execution: the base commit a0622910d493cadd94c6d819690dbab0232a44ab is the ledger commit that records V2-085 as complete and moves V2-086 to running, and its second parent merges v2-task/V2-085 into v2, so V2-085 had already landed. This task touches no public contract (contracts_changed is empty, contracts/openapi/openapi-v1.yaml sha256 is ca62ef4c0e1f7dbe357d03209d68f387581ed53aeaa66b0ea8fc5b8b7eea8cdd before and after) and moves no release BundleDigest. cmd/control-plane/** is the control-plane component's root, so the control-plane evidence key MOVES with this change: 360ae7cfc903454c1ef00f46f0e18ead197cce712b900af33e18c83e0ef6a548 before, 28c05ecfe155fa15d375aad96e2b87df7218197e229276b7a5d581ff513daec3 after -- it moved twice, because the start-time refusal the tech_lead ruled on changed that file a second time. No fourth record is issued for it, because this task's outcome is not that component's; `make smoke` was run instead, because cmd/control-plane changed, and its verdict is recorded in the api record. Under gate rule G6a a key is provenance, not a verdict."
    },
    {
      "name": "a17-execution-environment",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "uname -srm ; devbox version ; git rev-parse HEAD"
      ],
      "working_directory": ".",
      "timeout_seconds": 300,
      "summary": "Execution environment, recorded so every verdict above is attributable. uname -srm: 'Linux 6.18.33.2-microsoft-standard-WSL2 x86_64'. go version as resolved INSIDE devbox run --pure: 'go version go1.25.0 linux/amd64'. devbox version: 0.17.2 (devbox itself reports an available upgrade to v0.18.0 on every invocation; it was not taken, because devbox.json and devbox.lock are prohibited to this task). Worktree path: /home/takushi/repos/agentic-loop-foundation-worktrees/v2-task-086. Branch: v2-task/V2-086. Base commit: a0622910d493cadd94c6d819690dbab0232a44ab. Every devbox run was judged by its verdict line and never by the shell exit status, which is unreliable in this repository."
    },
    {
      "name": "a18-application-evidence-key",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "make",
        "evidence-keys"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A18. Measured on the FINAL tree with every touched file already git-added (`git status --porcelain` empty). The application component key is 5b0c777beefd4b91cbf4bfff3e91e6e7e9a6bb3c91af0e4c2caf637d5e574424; it was 344c888cd65d1f869648d027833f72f5245b0a2ddfcbea67a2f5f2c1f80bfdd3 at a0622910d493cadd94c6d819690dbab0232a44ab. Exactly three keys were measured for this task -- application, api, runner -- and not all 23. Under gate rule G6a this is provenance, not a verdict: these checks are re-run by make check. A stray `control-plane` binary produced by `go build ./cmd/control-plane` was briefly git-added and then removed with `git rm --cached` before the keys were measured; the four keys were identical before and after that removal, which is expected since it sits at the repository root and no component root matches it."
    },
    {
      "name": "a19-verification-go-test-race",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -race -count=1 ./..."
      ],
      "working_directory": ".",
      "timeout_seconds": 1800,
      "summary": "A19, re-run after the start-time refusal landed. Every package reported ok and none reported FAIL, with -race and -count=1: cmd/bootstrap 3.069s, cmd/ci-plan 1.010s, internal/api 1.438s, internal/application 2.841s, internal/ci 2.241s, internal/contracts 2.081s, internal/domain 2.959s, internal/legacyimport 1.011s, internal/provider 3.021s, internal/quota 1.016s, internal/reconciler 1.154s, internal/release 1.605s, internal/runner 5.936s, internal/scheduler 1.039s, internal/store/firestore 1.096s (see the G7 check: that ok is 26 top-level skips, and 49 counting skipped subtests), internal/store/memory 1.504s, internal/update 1.103s; cmd/control-plane, cmd/legacy-import, cmd/runner and internal/web are [no test files]. The same command at a0622910d493cadd94c6d819690dbab0232a44ab was green too, so the AFTER green is a comparison and not an isolated observation."
    },
    {
      "name": "a19-verification-go-vet-and-gofmt",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go vet ./... ; gofmt -l ."
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A19. `go vet ./...` produced no diagnostics -- the sentinel VET_CLEAN printed, which only happens on a zero exit from vet itself. `gofmt -l .` listed NO file, so every touched file is gofmt-clean. Judged by the verdict, never by devbox run's exit status."
    },
    {
      "name": "a19-verification-component-application",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "make",
        "component-application"
      ],
      "working_directory": ".",
      "timeout_seconds": 1800,
      "summary": "A19, re-run after the start-time refusal landed. Verdict line: 'ok github.com/takushi/agentic-loop-foundation/v2/internal/application 0.236s'."
    },
    {
      "name": "a19-verification-contracts-ledger",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -count=1 ./internal/contracts"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A17's definition of done and A19. Verdict line: 'ok github.com/takushi/agentic-loop-foundation/v2/internal/contracts'. That run includes TestActiveEvidenceIndex, which validates all three of this task's records against contracts/schemas/evidence.json, checks that each index entry's evidence_hash is the sha256 of its artifact, checks that .agents/v2/evidence/*.json and the index are a bijection, and checks that each entry's task_id has a task-state file -- so the ledger test is what makes these records well-formed rather than merely present. It also includes the packets, release contract, capability-evidence and deployment/provider preflight checks, all green."
    },
    {
      "name": "g7-firestore-package-measured-nothing",
      "status": "skipped",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "go test -count=1 -v ./internal/store/firestore > out.txt 2>&1; grep -c '^--- SKIP' out.txt; grep -cE '^ *--- SKIP' out.txt"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "Gate rule G7, recorded and NEVER counted as a pass. A bare `go test -count=1 -v ./internal/store/firestore` with no emulator prints 'ok github.com/takushi/agentic-loop-foundation/v2/internal/store/firestore 0.015s' while skipping everything, so that package's run measured NOTHING about this change. The skip figure is recorded WITH THE PATTERN THAT PRODUCED IT, because two correct figures were in circulation and the ambiguity was the pattern and not the tree: on one captured run in this worktree, `grep -c '^--- SKIP'` = 26 (TOP-LEVEL skipped tests) and `grep -cE '^ *--- SKIP'` = 49 (top-level plus skipped SUBTESTS). The coordinator's 49 and this task's earlier 26 are therefore the same fact under two patterns and neither is wrong; the earlier record's framing of it as a disagreement is corrected here. It measures nothing about this change in any case: internal/store/** is prohibited to this task and byte-unchanged, and the A9 proof runs against internal/store/memory. Status: skipped."
    },
    {
      "name": "a19-verification-gitleaks-agents",
      "status": "passed",
      "argv": [
        "devbox",
        "run",
        "--pure",
        "--",
        "sh",
        "-c",
        "gitleaks dir .agents --config .gitleaks.toml"
      ],
      "working_directory": ".",
      "timeout_seconds": 900,
      "summary": "A19, run AFTER the evidence was committed, over .agents only, and re-run after every subsequent record edit. THE VERDICT, identical on every run: 'INF no leaks found'. The scanned size grows as these records grow and is reported for provenance only, never as an assertion: 'INF scanned ~4725743 bytes (4.73 MB) in 344ms' on the first run, and 'INF scanned ~4739392 bytes (4.74 MB) in 357ms' after the A6 record was rewritten. `gitleaks dir .` was NOT run: it reports 26 findings from gitignored build/evidence/** paths that are not part of this task and would drown the signal. No credential value appears in any of these three records -- the identities they name are identifiers (an owner email, a scheduler service-account email, a stranger email) and no token, key or password value is recorded anywhere, in full, truncated or hashed form. No prompt and no provider response appears in any form."
    },
    {
      "name": "a19-no-provider-cli-and-no-cloud-resource",
      "status": "passed",
      "argv": [
        "sh",
        "-c",
        "git diff --name-only a0622910d493cadd94c6d819690dbab0232a44ab HEAD"
      ],
      "working_directory": ".",
      "timeout_seconds": 60,
      "summary": "A19's negative half, recorded because it is a constraint and not an outcome. No Provider CLI was started at any point: no invocation of claude, codex or opencode, not even --version or --help. No Google Cloud resource was touched, no network call was made outside the local test process, and neither TestControlPlanePreviewLocalLive nor TestFoundationPreviewLocalDogfood was run (internal/api/live_local_test.go and internal/api/live_dogfood_test.go are prohibited and were not opened for editing; both are build-tag gated and did not run under `go test ./...`). No live provider evidence record is issued, no capability id is cited in any of the three records, and no prompt and no provider response appears in any form anywhere in them. The three identities these records name -- an owner email, a scheduler service-account email and a stranger email -- are IDENTIFIERS, not credentials: an IAP composition's only secret is the upstream assertion, which httptest supplies as a header value, and no token, key or password value is recorded here or in any test fixture."
    }
  ]
}
